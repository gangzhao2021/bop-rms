# Tooling

This directory is reserved for repository-wide, workspace-consumable engineering configuration and developer tooling.

WP-0003 owns the initial TypeScript, lint, formatting, and unit-test tooling. Tooling must not become a home for domain logic, runtime application code, credentials, or machine-specific configuration. WP-0002 intentionally creates no package manifest or executable here.
