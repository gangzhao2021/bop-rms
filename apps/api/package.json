{
  "name": "@bop-rms/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "format:check": "prettier --check . --ignore-path ../../.prettierignore",
    "lint": "eslint src",
    "request-correlation:acceptance": "vitest run src/request-correlation.test.ts src/app.test.ts",
    "realtime:acceptance": "vitest run src/realtime.acceptance.test.ts",
    "structured-logging:acceptance": "vitest run src/structured-logging.test.ts",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@bop-rms/observability": "workspace:*",
    "@bop/eventing": "workspace:*",
    "express": "5.2.1",
    "helmet": "8.3.0",
    "uuid": "14.0.1"
  },
  "devDependencies": {
    "@types/express": "5.0.6"
  }
}
