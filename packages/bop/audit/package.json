{
  "name": "@bop/audit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "audit-integrity:acceptance": "vitest run --config vitest.config.ts src/tests/audit-integrity.test.ts",
    "build": "tsc --project tsconfig.json",
    "format:check": "prettier --check . --ignore-path ../../../.prettierignore",
    "lint": "eslint src",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
