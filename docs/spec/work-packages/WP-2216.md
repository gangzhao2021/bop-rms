# WP-2216 - Guest Session Cryptographic End-to-End Acceptance

## Authority and readiness

- Baseline: exact-main verified `40a1d19316c62edfb67158e274af2d198ae61d59`; branch `codex/wp-2216`.
- Composite 0.5.3 Sections 0-97; accepted WP-1003 Guest Session credential rules, WP-2208 Domain
  composition, WP-2209-2212 HTTP/persistence/lifecycle/pooled transactions and WP-2214 provider.
- Owner authorized consecutive bounded work and reviewed delivery during the nine-hour window
  ending 2026-09-08 13:01:54 UTC. Commit, push, PR, exact-head CI, reviewed squash merge and
  exact-main verification are authorized. Branch deletion, production and Provider mutation are not.
- DoR PASS: all components and public ports already exist. WP-2215 exact-head and exact-main CI
  passed; local main is clean. No missing external key source is needed for ephemeral synthetic tests.

## Problem and scope

The retained real HTTP/PostgreSQL Guest Session lifecycle matrix uses a test-only credential port
with different HMAC framing and equality behavior from the accepted Node adapter. WP-2214 verifies
that adapter independently, but does not prove its integration with stored selector bytes,
HTTP-delivered credentials and immutable lifecycle operation history.

This WP runs the existing acceptance matrix with both the retained synthetic port and the actual
WP-2214 credential provider. It adds no production wiring, contract, data model or policy.

Verification prerequisite revision: the existing database integration command still assumes the
original nine platform migrations and reserves sequence 010, which accepted migrations now use.
Update only that test harness to verify the current catalog exactly, preserve no-op object checks,
and allocate its synthetic fault fixtures after the accepted platform sequence. Retain all rollback,
orphan, drift, ordering, lock, foundation, helper, concurrency and redaction assertions.
The Section 95 foundation verifier intentionally accepts only the bootstrap shape. Exercise its
unchanged compliance/violation contract on a separate database containing the original platform
migrations 001-009; exercise migration runner lifecycle and CLI checks against the full catalog.

## Acceptance

1. Retain the full existing entry, renewal, scope/RLS, rollback, unknown-commit, idempotency,
   rotation/revocation, legacy inspection and append-only history matrix for the synthetic port.
   Run the same matrix with the public Node credential adapter and a fresh in-memory test key.
2. Actual HTTP issuance returns distinct canonical 32-byte Session/CSRF credentials and UUIDv7
   references. Stored selector fields correspond to their correct purposes, never raw credentials.
3. Recreating the provider with the same test key and reconnecting the store preserves
   authorization. A different key, swapped Session/CSRF credentials and a tampered CSRF credential
   fail closed without changing stored session facts. This is restart compatibility, not key rotation.
4. Real-provider rotation and revocation retain existing denial/replay/unknown-commit semantics.
   Every generated credential, ephemeral selector-key encoding and QR token is absent from
   session rows, immutable history and structured logs, including failed issuance paths.
5. Both modes use unique WP-owned disposable PostgreSQL scopes and existing least-privilege
   roles/pool cleanup. Frozen install, dedicated Session acceptance, root/affected tests and complete
   verification pass. Publish exact-head/main evidence only after observed success.

## Exact allowlist

- `packages/database/test/customer-entry-persistence-acceptance.test.mjs`
- `packages/database/test/integration.mjs`
- `docs/spec/work-packages/WP-2216.md`
- `docs/spec/README.md`

## Verification and boundaries

From the pinned repository root: `pnpm install --frozen-lockfile`; `pnpm exec vitest run --config
packages/database/vitest.identity-session-store.config.ts`; root `pnpm exec vitest run`; retained
`pnpm --filter @bop/identity test` and `pnpm --filter @bop-rms/api test`; affected package
format/lint/typecheck/test/build; complete `TMPDIR=/private/tmp CI=true pnpm verify`;
`TMPDIR=/private/tmp CI=true pnpm --filter @bop-rms/database test:integration`; scope/security
review and `git diff --check`; exact-head CI, disclosed Section 91 self-review, authorized squash
integration and exact-main CI. Any new source defect requires an explicit scoped brief revision.

No Domain/API/UI/runtime source, dependency, lockfile, migration, CI, grant policy or environment
configuration changes. Keys and credentials are ephemeral synthetic test inputs only and never
written to repository artifacts. No real runtime key generation/storage/rotation, Store inventory,
legacy rollout, production or external Provider action. External evidence from WP-2214 remains gated.
Rollback reverts only added test coverage and documentation; it never changes session data.

## Status

- Implementation complete; exact-head/main CI and integration remain pending.
- AC 1-4 local PASS: dedicated Session acceptance passed 2 files / 3 tests, including the complete
  synthetic and crypto profiles. Same-key reconstruction succeeds; wrong-key/CSRF misuse denies
  without persistence changes. All generated credential material is absent from rows/history/logs.
- AC 5 local evidence: frozen install and complete `pnpm verify` passed. Final affected database
  format/lint/typecheck/test/build passed (94 tests); retained Identity 135/135, API 178/178,
  root 374/374 and secret-scan 2/2 passed. Complete verify preceded the final integration-fixture
  revision; final affected checks were rerun after that revision.
- Initial database integration exposed stale bootstrap assumptions; the explicit test-harness
  prerequisite revision above preserves those contracts in separate fixtures. Initial root execution
  without the established macOS `TMPDIR=/private/tmp` failed three path/permission assertions;
  rerunning with the established environment passed all 374. No check was disabled.
- Local scope/security self-review: no Blocker/High/Medium/Low findings. Four allowlisted files only;
  no generated/dependency/migration/runtime changes. No independent reviewer approval is claimed.
- Final database integration command PASS: current-catalog migration lifecycle, bootstrap foundation,
  concurrency/redaction, parallel isolation, every retained failure injection, timeout, SIGINT/SIGTERM
  and owned cleanup. Exact-head/main CI evidence will be attached to the PR after it completes.
- Security review must trace HTTP Cookie/CSRF to purpose-separated selectors, stored rows/history
  and logs, including denial and replay paths. No production readiness or independent approval claim.
