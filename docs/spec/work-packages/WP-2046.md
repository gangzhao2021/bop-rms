# WP-2046 — Break-glass Access Runbook

## Metadata and authority

- Feature: `F20.3 — Security and Privacy`; owner: Security / Operations
- Authority: Handoff Sections 50.30, 54.6 F20.3, 86.4, 87.6, 87.7.1, 87.10.4 and 87.11.1
- Stacked local baseline: locally verified WP-2045 head `3c4b65e`; branch `codex/wp-2046`
- Real Actors, production identities, approvals and drill: unavailable and unclaimed
- GitHub publication/integration: deferred by explicit Owner direction

## Resolved local boundary

WP-2046 supplies a versioned fail-closed runbook, a deliberately unfilled restricted-evidence
template and a deterministic completeness gate. The contract covers request/preflight, two distinct
approvers where required, named recent-MFA Actors, exact purpose/scope, finite UTC expiry,
purpose-built least privilege, monitored execution, immediate revocation, reconciliation and
independent review.

The repository creates no privileged identity, role, credential, account access, Provider action or
production record. A real two-person exercise, contact tree and quarterly access review remain
External Evidence launch gates.

## Exact tracked-file allowlist

- `docs/runbooks/break-glass-access.md`
- `docs/runbooks/break-glass-access-evidence-template.md`
- `tooling/security/break-glass-runbook.test.mjs`
- root focused acceptance script
- `docs/spec/README.md`, `docs/spec/work-packages/WP-2046.md`

## Status

- Specification: `Bounded for local WP-2046 execution`
- Implementation: `Locally implemented and verified`
- External Evidence: `Unavailable and unclaimed`
- GitHub publication/integration: `Deferred by Owner direction`

## Local verification evidence

- Runbook/template completeness passes `2/2`; the accepted workforce Identity and API security
  suites pass `8/8` and `5/5`, and isolated PostgreSQL workforce/Audit acceptances pass `1/1` each.
- Frozen offline install remains current across `32` workspace projects. Root format, lint,
  typecheck and build pass across `31` packages; Repository Guidance, Module Manifest, Module
  Generator `16/16`, Import Boundary `22/22`, Database Ownership `29/29`, Database Permission
  `8/8`, Domain Layer Boundary `56/56` and Screen Registry `210` pass.
- Root Vitest is `246/250`; only the unchanged four macOS APFS case-fold/realpath fixtures fail
  before the later Turbo phase. No WP-2046 test fails.
- Security/privacy review found Blocker `0`, High `0`, Medium `0`, Low `0` inside the local boundary.
  The runbook is explicitly non-authorizing, forbids self-approval/shared identities/permanent
  elevation/evidence leakage and requires exact revocation plus independent reconciliation.
- Real named Actors, production identities, two-person approval, contact tree, provider Audit and
  exercise/quarterly-review evidence remain unavailable and unclaimed External Evidence gates.
