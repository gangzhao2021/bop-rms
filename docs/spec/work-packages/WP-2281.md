# WP-2281 - Dining Closing Current Authority and Result Integrity

## Authority and readiness

Baseline f28e602a011116b0ad31cb95fce67f32e4aba331; branch
codex/wp-2281-dining-closing-authority. Continuous bounded implementation and verified local
commits are authorized. Composite Sections 0–97, WP-1007 Closing policy, WP-2273/2274 current
Session authority and WP-2278 Guest System Audit govern this correction.

## Scope and acceptance

Load the exact current Session and authorize before history or financial/Task reads. Preserve
successful retries after version advancement; validate complete historical action, identity,
version, phase and result coherence. Validate every committed Session field. Reject unknown
actor kinds and Guest-as-User Audit; current Host uses Restricted CUSTOMER_PWA System Audit.
Capture bounded dependency data without accessors and translate synchronous/asynchronous and
hash callback failures to fresh bounded errors. Preserve existing Closing financial policy.

Tests cover current revoked authority, corrupt history/acknowledgements, scoped identity,
version advancement, private exceptions and dependency mutation/accessors. Required checks:
pinned frozen offline install, affected format/lint/typecheck/Dining tests, existing
Dining Closing acceptance, full pnpm verify, sequential pnpm test:integration --force,
secret-scan:check, repository-guidance:check and exact final LF/diff/security review.
Long verification uses process-bound caffeinate; no check may be skipped as a pass.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2281.md
- packages/rms/dining/src/application/dining-closing-service.ts
- packages/rms/dining/src/application/ports/dining-closing-ports.ts
- packages/rms/dining/src/tests/dining-closing.test.ts

## Non-goals and rollback

No persistence/migration, new permission or financial rule, shared Cart write fence, UI/API,
real Store/Provider facts, external mutation, main merge, push or deployment. External facts
remain gates. Rollback is a reviewed local correction preserving current authorization.
All scoped acceptance criteria now have observed local evidence below.

## Focused observed evidence

Pinned frozen offline install passed with CI=true. The first install stopped because noninteractive
pnpm requires CI confirmation; it was rerun with the established setting. Focused Dining Closing
acceptance passes: Dining489/489 (47 new tests), Task32/32. Affected format, lint and typecheck pass.
Initial test assertions were corrected to the existing bounded error message and fresh fixture
Audit time; forbidden test non-null assertions were replaced with explicit fixture assertions.
No production validator or repository gate was weakened.

Current Session identity and Staff/Host authority precede history and financial work. Historical
snapshots must match exact action/operation, scope, immutable start facts, requested next version
and phase. Full Session equality is required when current and historical versions coincide;
later legitimate Table/Host transitions do not erase earlier accepted results. Commit results
must match all Session fields. Financial evidence scope and Closing phase are checked before Task
requests. Guest Host evidence uses System Audit; no Guest identifier becomes a User actor.

Security review traces the internal command, current authority, history, financial/Task summaries
and Audit without new logs, URLs, storage or telemetry. Bounded frozen copies reject getters and
retain source snapshots across callbacks. Synchronous, asynchronous and crypto callback failures
return fresh owned errors without private dependency messages. No new credential, payment,
contact, health or live Store fact enters this change. The durable adapter must still perform
atomic expected-version writes; these service checks are not a cross-domain Cart write fence.
Full pnpm verify passed with exit 0, Dining489/489, Dining PostgreSQL5/5 and all40/40 build
tasks. Sequential forced integration passed with hash `ad263cec62e01f9c`, zero cache hits,
exit 0 and duration 1m58.315s: migration/concurrency, parallel isolation, fault injection,
signals, timeout, redaction and cleanup. Process-bound idle inhibitors ended with their commands.
The existing Turbo Dining no-output advisory is non-failing. No check was skipped or weakened.
Secret2/2, repository guidance, final documentation format and exact six-file LF/diff review
pass; isolated database control files are zero and main remains unchanged. No external operation
or production activation occurred. Next allowed work is a separate atomic Closing storage WP.
