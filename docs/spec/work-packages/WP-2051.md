# WP-2051 — Privacy Rights, Necessary-cookie, Consent and Retention Execution Test

## Metadata and authority

- Feature: `F20.3 — Security and Privacy`; owner: Privacy / Security / Compliance / Data
- Authority: Handoff Sections 54.6 F20.3, 86.9 and 87.10.1–87.10.2
- Stacked local baseline: locally verified WP-2050 head `5ee1d5b`; branch `codex/wp-2051`
- Legal approval, production notice/terms, real request fulfilment and deletion evidence: unavailable
  and unclaimed
- GitHub publication/integration: deferred by explicit Owner direction

## Resolved local boundary

WP-2051 adds a canonical executable privacy policy and a repository scan. The gate fixes the
necessary-cookie allowlist, prohibits advertising/tracker/analytics/session-replay source, records
the closed privacy-right types and response/export bounds, preserves financial/Audit facts during
correction or anonymization, restricts tombstones, and pins the accepted retention triggers.

This WP does not create a Customer profile or Privacy Request Domain ahead of WP-2140/WP-2146,
perform a real erasure/export, establish a legal conclusion, or claim production policy acceptance.

## Exact tracked-file allowlist

- Canonical privacy execution policy and repository validator tests
- Root focused acceptance script
- `docs/spec/README.md`, `docs/spec/work-packages/WP-2051.md`

## Status

- Specification: `Resolved for local WP-2051 execution`
- Implementation: `Locally implemented and verified`
- External Evidence: `Unavailable and unclaimed`
- GitHub publication/integration: `Deferred by Owner direction`

## Local verification evidence

- Privacy execution policy/repository scan: `5/5`; Identity browser-session contract: `10/10`;
  Customer Entry contract: `14/14`.
- Repository format, lint, typecheck and build: passed for all `31` workspace packages.
- Repository guidance, module manifest, module generator (`16/16`), import boundary (`22/22`),
  database ownership (`29/29`), database permission (`8/8`), Domain boundary (`56/56`) and Screen
  contract (`210/210`) gates: passed.
- Root Vitest boundary: `255/259` passed. The four known macOS APFS-only failures remain the two
  migration catalog case-fold fixtures and two migration configuration realpath fixtures; all
  WP-2051 tests passed.
- Legal approval, production policy publication, actual privacy-request fulfilment/export/deletion
  and legal-hold evidence remain External Evidence and are not claimed.
