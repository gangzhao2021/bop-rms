# WP-2226 - Cart Navigation and Customer Money Presentation

## Authority and readiness

Owner authorized continued implementation of the reviewed defects and as much remaining project
scope as accepted rules permit. Baseline `cedc44c9b6a6a1b389f77342078cc6f2d540dabe`, isolated branch
`codex/wp-2226-cart-presentation`. WP-2224 and WP-2225 edits remain in their own worktrees.
Composite Handoff 0.5.3 plus accepted Sections 92-97, the spec index, WP-1205 and WP-1100 govern
this existing-contract repair. No new business policy, dependency or external evidence is assumed.

## Screen and ownership contract

Canonical CUST-CART `/cart` and CUST-CHECKOUT `/checkout` retain Customer PWA, customer.public,
exact Guest/Store scope, server-owned estimates/Quotes, immutable values, existing commands and
all loading/empty/denied/not-found/stale/conflict/disabled/offline recovery states. Canonical
Screen Registry, permission and Projection ownership do not change. There is no visual redesign
or Figma update. Native accessible links remain links, using the existing Router for in-app Menu
navigation so the authorized in-memory journey context is retained. No private persistence is added.

Display accepted CAD Pilot amounts as exact major-unit decimal text, retaining explicit currency
and en-CA grouping. Use bigint/string operations only: no conversion of monetary values to binary
floating point. Missing/invalid/unsupported currency display is explicitly unavailable; the legacy
Cart DTO does not carry versioned currency exponent metadata, so generic non-Pilot display is not
inferred. No price, tax, rounding, Quote expiry or financial authority is introduced in the browser.

## Exact allowlist

- `docs/spec/README.md`
- `docs/spec/work-packages/WP-2226.md`
- `apps/customer-pwa/src/cart/CartPage.tsx`
- `apps/customer-pwa/src/cart/CartPage.test.tsx`
- `apps/customer-pwa/src/cart/format-money.ts`
- `apps/customer-pwa/src/cart/format-money.test.ts`
- `apps/customer-pwa/src/checkout/CheckoutPage.tsx`
- `apps/customer-pwa/src/checkout/CheckoutPage.test.tsx`
- `apps/customer-pwa/e2e/customer-demo.spec.ts`

## Acceptance and verification

1. Every existing Cart-to-Menu recovery/shopping link uses in-app navigation; keyboard activation
   preserves the document/journey context and reaches the Menu on desktop and mobile.
2. Cart/Checkout show exact CAD decimal amounts, including zero, negative values, cents and values
   above Number.MAX_SAFE_INTEGER without rounding. Unsupported/malformed amounts never invent zero.
3. Existing semantic labels, actions, offline read-only state, disabled gates and server authority
   remain intact. Synthetic browser checks cover navigation, responsive overflow and console errors.
4. Pinned frozen offline install; Customer PWA format/lint/typecheck/test/build and existing
   `test:browser`; root `pnpm verify`, `pnpm screen-registry:check`, `pnpm secret-scan:check`,
   final allowlist/diff review and supplemental scan of new untracked files.

Use only existing commands and exact Node 24.18.0 / pnpm 11.13.0 pins. Existing Playwright scripts
own temporary local servers and teardown. Business/database/HTTP changes and real Store/browser
data remain outside this WP. Rollback reverts presentation and Router changes, with no data edits.
No commit/push/merge/deployment or remote evidence is claimed without its separate authorization.

## Status

Implementation and local verification complete; acceptance 1-4 PASS within the nine-file scope.

- Frozen offline install passed, with no dependency or lockfile change.
- Customer PWA tests221/221, lint, typecheck and production build passed. Eight added formatter
  cases cover exact signed/minor-unit/large-value rendering and unavailable input behavior.
- Existing Playwright browser suite passed38/38 across desktop, mobile and production exclusion.
  Two new browser executions prove keyboard navigation preserves the document, Menu remains
  available, CAD amounts render exactly and no horizontal overflow or console/network violations
  occur. The initial new selector matched a shell heading and a content heading; scoping it to
  the main region fixed the test ambiguity. The full suite was rerun with zero failures/skips.
- `pnpm build` passed40/40 (39 cached initially); complete `CI=true pnpm verify` then passed with
  exit0, including architecture/contracts/migrations, format/lint/typecheck, all configured tests
  and isolated PostgreSQL acceptance, Screen Registry and all40 final build tasks (cached).
- Secret scan2/2 and supplemental scan of all nine files, including three new untracked files,
  passed. Final scope/LF/diff checks passed. No new private persistence, permissions, financial
  calculation, source-domain writes or generated tracked changes.
- React review: shared module-level formatter, existing Router and state controllers retained;
  no extra fetch, effect, dependency or client state. Production demo-exclusion tests still pass.

Generic currency metadata, Cart binding integration, real Store UAT and visual production approval
remain outside this repair. Local servers were owned and torn down by Playwright. No remote CI,
commit, push, merge, deployment or production readiness is claimed.
