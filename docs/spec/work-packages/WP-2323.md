# WP-2323 - Dining Cart History Scope and Persistence

## Authority and scope

Baseline64afbc1a74329f8c992f0b50a2d47b3a8085f2c3; branch codex/wp-2323-dining-cart-history-scope.
Composite Sections0-97, WP-1201/1205 and accepted WP-2228 govern. Continuous local work and
verified local commits are authorized. Preserve separate AGENTS/skill document edits.

Add a bound owner operation-history reader that checks minimal Cart/Guest header before retrieving
restricted snapshots. Wrong binding returns bounded idempotency conflict with no snapshot query.
Capture trusted references, preserve Brand/Store isolation and existing unbound owner reader.
Use the bound reader inside Dining item writer retry checks, so the transactional duplicate path
has the same privacy boundary as application retries. No migration/history rewrite or new authority.
Compose WP-2322 with actual scoped aggregate/history readers and atomic item writer in isolated
PostgreSQL acceptance, using explicitly synthetic Identity/Dining/Catalog providers. Verify two
participants, own/foreign edits, audit/history, original recovery and no duplicate effects.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2323.md
- packages/rms/ordering/src/infrastructure/persistence/cart-item-operation-store.ts
- packages/rms/ordering/src/infrastructure/persistence/cart-item-command-store.ts
- packages/rms/ordering/src/tests/cart-item-operation-store.test.ts
- packages/rms/ordering/src/index.ts
- packages/database/test/cart-query-store-acceptance.test.mjs
- packages/database/test/cart-item-command-store-acceptance.test.mjs

## Verification plan

Fresh Ordering format/lint/typecheck/test/build, dependent API typecheck, all Cart PostgreSQL9 cases
through the existing vitest.cart-aggregate configuration, ownership/import/Domain/migration/guidance
and secret checks. Final changed docs/integration formatting, diff/LF and owned resource cleanup.
Reuse WP-2321 frozen install only for the same unchanged installation/manifests/lock/pins; record
evidence explicitly at closeout. API/browser activation remains next work. Named full shared Cart
journey regression remains pnpm verify and pnpm test:integration without automatic cache bypass;
broaden now only for a concrete uncovered cross-module risk or affected failure. No new test script.

## Observed results

PASS: bound recovery checks only Cart/Guest header before snapshot retrieval, constrains the snapshot
query too, and rejects missing/substituted immutable rows or malformed/accessor headers. Wrong
binding produces a fresh bounded idempotency conflict without a restricted snapshot query. Dining
writer duplicate checks use this reader in their existing transaction; Pickup behavior is unchanged.

Fresh Ordering829/829 across40 files passes (+13). Initial lint found one unused typed mock argument;
moving its type to the mock signature fixed it. The affected history-reader41/41, file lint/format
and Ordering typecheck pass after that test-only correction; all other unit/lint results remain
valid and are reused, not rerun. Ordering build and dependent API typecheck pass on unchanged final
production code. Cart PostgreSQL9/9 across7 files passes in69.31s. New real owner-store composition
proves both participants' items, own update/removal, foreign-item denial, lost-commit-ack recovery,
cross-Guest operation conflict, revoked membership before owner access, and exactly5 additional
Cart operations/Audit records (13 total) with no duplicate retry effect. Identity/Dining/Catalog
providers remain explicitly synthetic; actual Cart/history/Audit persistence and transactions run.

Fresh ownership180, import22, Domain57, migration103/catalog101, guidance33 ADRs/5 skills and secret2
pass. Changed docs/integration formatting and diff checks pass. Commands are Ordering workspace
test/typecheck/lint/build/format:check, API typecheck, exec vitest run --config
packages/database/vitest.cart-aggregate.config.ts, and root database-ownership:check,
import-boundary:check, domain-layer-boundary:check, migration:check, repository-guidance:check,
secret-scan:check. Local evidence: /private/tmp/bop-wp2323-{unit,type,lint,type-recheck,focused,build,
api-type,pg,gates}.log. Tests ran on64afbc1 plus the nine allowlisted files; final file lint/type and
41 tests cover the later mock-signature cleanup. No required affected check remains missing.

Frozen-install evidence reused from WP-2321: pnpm install --offline --frozen-lockfile --store-dir
/private/tmp/bop-rms-wp2288-dependency-cache/v11, exit0/41 projects, log bop-wp2321-install.log,
tested5271460 plus the seven WP-2321 files subsequently committed26a9fe4. The same installation,
Node24.18.0/pnpm11.13.0, manifests/lockfile/pins and dependency tree are unchanged. No install,
full pnpm verify/test:integration, API runtime or PWA suite ran here; none is claimed as fresh.
Broader full regression remains at the named complete Cart browser journey milestone.

Final review: nine-file business scope, no new private-table ownership, history rewrite, credential
or unrestricted error output; no unresolved in-scope security blocker. Owned test containers/volumes
are zero; main remains cedc44c9. The three separately authorized rule documents stay excluded.
No API/UI activation, production membership or atomic Closing lease claim, external operations or
dependency/schema change. Rollback removes the additive reader and Dining-only writer selection
before API activation; no persisted history is modified. Next is API composition and browser journey.
