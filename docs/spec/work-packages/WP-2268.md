# WP-2268 - Cart Mutation and Quote Continuity Acceptance

## Authority and readiness

Owner authorizes successive bounded implementation and verified local commits. Baseline dd761b21,
branch codex/wp-2268-cart-quote-continuity. Composite Sections0–97 and WP-2262–2267 retain current
Identity/Cart authority, immutable historical Quote replies, exact-version current display, atomic
attachment/expiry fencing and explicit retry. Separate item and Quote proofs do not prove their
continuous composition. Extend the existing isolated Quote HTTP acceptance before claiming it.

## Scope and ownership

Test-only composition of existing Identity authorization, current Cart display, authorized item
HTTP Update, Pricing request/Quote persistence, Ordering attachment/expiry and Audit. Use the actual
owner stores with existing separated minimal test roles. Current Store display, Catalog validation/
description and pricing/tax facts remain explicitly synthetic. Adapt synthetic Quote candidates to
actual public Cart line references and quantities; never infer live pricing or query foreign tables.

Prove fresh Quote display on version2, item Update to version3 invalidating current Quote display,
original historical Quote replay without resurrection, and new version3 Quote using exact bigint
amounts. Pause a candidate before Pricing persistence, update Cart to version4, then resume; no
old-version attachment may become current. After expiry reconcile the pending original operation
through the existing terminal fence. Current revoked Session denies both item and Quote history.
No business-rule, API, UI, migration, dependency, grant or production behavior change. A discovered
production defect requires amending the owning allowlist before implementation.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2268.md
- docs/spec/design/business-scenario-coverage.md
- packages/database/test/quote-http-store-acceptance.test.mjs

## Acceptance and verification

1. Existing Quote HTTP persistence/expiry/revocation proofs stay green. New continuous current-view,
   Update/Quote history and delayed-candidate race checks prove immutable history, no stale current
   Quote, exact current quantity/amount, no duplicate effects and current authorization.
2. Pinned frozen install; database format/lint/typecheck/test/build; pricing-quote:acceptance;
   full pnpm verify; forced pnpm test:integration --force; Secret, exact4-file scope/LF, diff and
   owned server/transaction/database cleanup. No unrun check is a pass.

## Privacy and boundaries

Generated test credentials and null notes only. No sensitive option labels or private scope in
HTTP output, diagnostics, URLs or analytics. Internal Cart/Quote locators remain inside approved
scoped test contracts. No external facts, live Store, Provider, deployment, push or main merge.
Uncommitted fixture changes may be discarded; accepted work uses forward correction, never history
deletion. Test evidence does not activate production or complete shared DineIn/reload recovery.

## Verification record

Acceptance1 PASS: Pricing248/248, Quote API9/9 and actual Quote PostgreSQL5/5 pass. The continuous
HTTP fixture reads a current version2 Quote, updates quantity to2/version3 through authorized item
composition and observes no current Quote. Original Quote replay returns the unchanged historical
response without restoring it to the current view or appending facts. Original item replay adds
no facts. A new Quote uses quantity2 and exact subtotal18014398509481986, displayed only at version3.

The delayed candidate barrier allows an actual item write to reach version4 first. Pricing persists
its original version3 result; Ordering rejects attachment with HTTP409, leaving no attachment and
no current Quote. Only one Pricing Quote/request/Audit plus one item Audit are added. Same-request
retry adds nothing. After expiry, the original operation returns410 with original cartVersion3,
adds one expiry Audit and still cannot become a current attachment. Actual Session revocation
denies both item replay401 and Quote history404 without additional effects. Existing role isolation,
GUC cleanup, acknowledgement-loss and expiry-race proofs remain intact.

Acceptance2 PASS: full CI=true pnpm verify completed with40 successful builds. Frozen install and affected database lint pass. The first
new current-Cart request correctly received400 because the fixture omitted required Fetch Metadata;
the fixture now supplies the existing same-origin headers. A test barrier initially consumed the
response body twice and was corrected without weakening response assertions. The final owning
acceptance passes in37.70s. Full verification passes; forced uncached integration passes with cache bypass61b28e25614c78d2,
0cached,1m59.463s, including91 migrations, concurrency, redaction, parallel isolation,
fault/signal/timeout handling and owned cleanup. Secret2/2 and exact4-file scope/LF pass;
no failed/partial invocation is claimed as a pass.

Security/privacy review: no business code, grant, migration or cross-domain SQL change. Actual
Identity authorization precedes public Cart reads and mutations; current display uses Ordering's
exact-version attachment reader. Synthetic Store/Catalog/price/tax fixtures are explicit, use null
notes and do not imply live eligibility. Current output uses existing bounded public DTOs. No
unaccepted Blocker/High finding remains in this bounded acceptance. External readiness and shared
DineIn/general recovery remain unimplemented/gated independently.

Logs under /private/tmp: bop-wp2268-install.log, bop-wp2268-lint.log,
bop-wp2268-quote.log, bop-wp2268-verify.log, bop-wp2268-integration.log,
bop-wp2268-secret.log. Test HTTP servers, scoped transaction clients and generated roles/databases
are closed by the owned isolated harness; final aggregate verification passes.

Final closeout: all acceptance criteria pass locally; no required check skipped. Final document
format/guidance, scope/LF and diff review pass. No production code, dependency, migration, grant,
main merge, push, deployment or external action. The next bounded implementation is the Dining
owner's current participation query for the already accepted shared-Cart direction; it must not
infer Guest authority from locators or read another Domain's private tables.
