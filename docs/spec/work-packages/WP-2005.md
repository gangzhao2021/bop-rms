# WP-2005 — Database Permission Test

## Metadata and authority

- Feature: `F20.1 — Contract and Architecture Tests`; owner: Database / Security Architecture
- Authority: Handoff Sections 43.5, 54.6 F20.1, 79.11, 87.7.5–87.7.6 and accepted Sections 92–97
- Stacked local baseline: locally verified WP-2004 head `ed82c82`; branch `codex/wp-2005`
- GitHub publication/integration: deferred by explicit Owner direction

## Contract and ownership

The repository now owns a deterministic migration-permission gate. Every Brand-scoped business
table discovered from the immutable migration catalog must enable and force RLS, have an explicit
Brand policy, add Store scope where `store_id` exists, reject unconditional `true` policies and
revoke PUBLIC table access. Business schemas and functions must revoke PUBLIC, PUBLIC grants and
RLS weakening are prohibited, and migration-catalog failures remain fatal.

The first complete scan exposed four historical Catalog trigger functions with default PUBLIC
execute. The immutable source migrations remain unchanged; ordered forward migration
`1106_002_alter_catalog_function_permissions` revokes PUBLIC after all four functions exist.

## Exact tracked-file allowlist

- `tooling/database-permission/**`, root package scripts
- `migrations/1106-rms-catalog-allergen-provenance/1106_002_alter_catalog_function_permissions.sql`
- migration catalog test registration
- `docs/spec/README.md`, `docs/spec/work-packages/WP-2005.md`

No role/login, runtime grant, credential, non-local database, external service or deployment action
is authorized.

## Acceptance and verification

- valid forced/scoped/default-deny migration fixtures pass;
- missing schema/table/function PUBLIC revocation fails closed;
- missing Enable/Force RLS, policy, Brand/Store scope and open policy fail closed;
- PUBLIC grant and RLS weakening fail closed;
- the complete committed migration catalog passes the new permission scan;
- focused migration registration, format/lint/typecheck/build and architecture gates pass;
- actual runtime/migration/read-only principals and non-local PostgreSQL evidence remain unavailable
  and unclaimed.

## Status

- Specification: `Bounded for local WP-2005 execution`
- Implementation: `Locally implemented and verified`
- Security/privacy review: `Local static review passed; runtime principal evidence unavailable`
- External Evidence: `Unavailable and unclaimed`
- GitHub publication/integration: `Deferred by Owner direction`
