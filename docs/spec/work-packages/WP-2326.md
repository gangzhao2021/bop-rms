# WP-2326 - Shared Dining Session Binding Guard

Baseline50b877e; branch codex/wp-2326-dining-runtime-binding-guard. Composite Sections0-97,
WP-2228 and WP-2299 current consumed-admission binding govern. Continuous local implementation
and verified local commits are authorized; preserve the three separate agent-guidance edits.

Extract the existing API current Dining binding composition into a reusable guard and compose it
into local Cart Session authorization whenever Dining admission owner ports are configured.
Remove the duplicate lab guard so the real browser journey exercises the runtime-owned guard.
Keep default caller binding policy when admission is unconfigured, ContextOnly and Pickup behavior,
current scope/QR checks and bound Active/Closing identity semantics. Cart participation still owns
its stricter Active-only policy. No credential recovery redesign or cross-owner private SQL.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2326.md
- apps/api/src/customer-dining-binding-composition.ts
- apps/api/src/customer-dining-binding-composition.test.ts
- apps/api/src/local-customer-runtime.ts
- packages/database/test/local-customer-lab-acceptance.test.mjs
- packages/database/test-support/local-customer-dining-seed.mjs

## Acceptance and verification

Reuse the existing binding tests for current consumed-admission, Closing, moved assignment and
missing owner state. Add direct guard checks for current bound identity, missing consumed admission,
scope mismatch, base-policy rejection, ContextOnly and expired context after owner await. Existing
runtime tests cover default/opt-in request guards; actual customer-lab:acceptance must pass using
only base entry binding and the new runtime composition. No duplicate test-only binding remains.

Run API test/lint/typecheck/build, changed formatting, import-boundary:check,
repository-guidance:check, secret-scan:check and actual customer-lab:acceptance. Reuse WP-2325
stage evidence for unchanged owner implementations, migrations, UI and dependencies; API and lab
evidence must be fresh because authorization composition changes. Frozen install reuses the same
WP-2321 installation documented in WP-2325 with unchanged lock/manifests/pins. No full verify or
forced integration per small WP; next full milestone is another completed cross-domain journey or
release/merge gate, with existing verify and test:integration. Broaden for discovered owner/persistence
changes. Review exact diff, security and owned resources before local commit. Rollback removes the
optional runtime wrapper; append-only owner histories remain intact. Production/external launch
facts and cross-document credential recovery remain separate gates.

## Results

PASS: extracted the existing owner-query/context-expiry guard and reused it in admission binding
and optional local Cart authorization. Same-scope ContextOnly/Pickup retain base policy, unconfigured
admission retains the injected policy, and Cart's separate Active participation requirement remains.
The lab now supplies only the base QR binding; its duplicate owner guard is removed.

Fresh API571/571 in32 files, typecheck, lint and build pass. Eight new cases cover ContextOnly,
Brand/Store mismatch before base policy, base denial, valid consumed admission, missing current
admission, moved assignment and context expiry across the owner await. Existing Closing and original
credential revocation tests also pass. Actual customer-lab:acceptance1/1 passes in16.21s using the
runtime guard for desktop/mobile Add/Update/Remove. Formatting, import-boundary22/22 plus validator,
repository guidance and secret scan2/2 pass. Logs: /private/tmp/bop-wp2326-{api,type-final,lint,build,
lab,format,import,guidance,secret}.log; tested50b877e plus these seven files.

Initial invocation omitted CI=true and pnpm aborted its attempted dependency reconciliation before
checks; no dependency tree was removed. Restoring the established CI environment allowed normal
commands. Initial typecheck rejected a null admission inside a non-null owner snapshot; the test now
uses the contract's null snapshot for missing current admission, and final typecheck passes. These
failed attempts are not reported as passes. No dependency upgrade or credential policy change.

Reused unchanged owner/migration/UI stage evidence is WP-2325 at50b877e, recorded in its brief and
/private/tmp/bop-wp2325-verify-remainder.log and integration.log. Fresh API/lab coverage replaces its
now-invalid API/composition coverage. The same installation and unchanged lock/manifests/pins retain
WP-2321 frozen-install validity as documented in WP-2325. Full verify, integration and unrelated PWA
unit/build were not repeated for this composition-only WP. Final scope/security review finds no
private SQL, credential storage, new route, production registration or external mutation. Next bounded
work may verify simultaneous participants on one table; current viewport journeys are separate tables.
