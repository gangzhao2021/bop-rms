# WP-2270 - Cart Revision Boundary Correction

## Authority and readiness

Baseline9c1425730a83a09af100ceda0fc110f5aeecc3a4; branch codex/wp-2270-cart-version-boundary.
The Owner authorizes continued bounded implementation and verified local commits. Composite
Sections0–97, WP-1200/WP-1201 Cart ownership and exact optimistic concurrency remain authoritative.
Existing1300_001 cart and1300_002 operation storage use positive PostgreSQL integer revisions.
Current HTTP mutation contracts already validate revisions through2147483647. The aggregate parser
incorrectly applies the unrelated quantity limit999 to aggregateVersion, preventing the next edit.
This is an implementation defect, not a change to quantities or accepted product choices.

## Scope and allowlist

Use a separate positive signed32-bit aggregate revision validator, preserving quantity1–999.
Test exact999→1000 and subsequent Add/Update/Remove/replay, invalid bounds and upper-limit failure
before persistence. Extend the existing isolated PostgreSQL command fixture to exercise both its
original initial revision1 and a synthetic initial revision999. No existing history is rewritten;
each disposable test starts from a separate synthetic fixture.

- docs/spec/README.md
- docs/spec/work-packages/WP-2270.md
- docs/spec/design/business-scenario-coverage.md
- packages/rms/ordering/src/domain/cart.ts
- packages/rms/ordering/src/tests/cart.test.ts
- packages/rms/ordering/src/tests/cart-item-command.test.ts
- packages/database/test/cart-item-command-store-acceptance.test.mjs

## Acceptance and checks

1. Valid revisions1,999,1000 and2147483647 parse. Zero, negative, fractional, nonnumber,
   unsafe and beyond-storage revisions fail. Item and option quantities retain their limits.
2. Real command service crosses999 for Add/Update/Remove, retains immutable attribution and
   original replay, rejects stale revisions and fails before commit on signed32-bit exhaustion.
3. Existing PostgreSQL writer/reader/history/Audit matrix runs at both initial revisions, including
   atomic rollback, lost acknowledgement, competing writes and same-key convergence. Sources remain
   explicitly synthetic; no production or commercial readiness claim.
4. Pinned frozen install; affected format/lint/typecheck/test/build; pnpm ordering-cart:acceptance;
   full pnpm verify; pnpm test:integration --force; pnpm secret-scan:check; exact7-file scope/LF,
   final diff, guidance and owned resource cleanup. Record actual results and every deviation.

## Boundaries and rollback

No owner, dependency, schema, migration, API/UI, quantity, permission, attribution, Audit/Event or
runtime activation changes. No foreign private reads, real facts, secrets/PII or external mutation.
Shared DineIn persistence/current authorization remains separate. Exhaustion is fail-closed; a
future storage-width change requires its own contract and migration. Uncommitted edits may be
removed; committed work uses a forward fix. Main merge, push and deployment remain unauthorized.

## Verification record

Acceptance1–2 PASS: Ordering535/535 includes17 new revision and quantity-boundary cases.
The command service crosses999→1000→1001→1002; original Add replay returns its historical result
without resurrecting the removed item or changing current revision. Stale revision is rejected.
The maximum persisted revision remains readable; overflow is rejected before repository commit.

Acceptance3 PASS: pnpm ordering-cart:acceptance runs Ordering535/535 and PostgreSQL7/7 in51.50s.
The original initial-revision1 matrix remains; the additional synthetic initial-revision999 matrix
covers real scoped reads/writes, immutable operation recovery, atomic Audit rollback, lost commit
acknowledgement, competing revisions and same-key Add/Update/Remove convergence. Final revision is
1007 with exactly8 operation/Audit appends. Quantity, attribution, retention and role checks remain.

Acceptance4 PASS: frozen install, affected format/lint/typecheck/test/build, final full pnpm verify
and all40 build tasks pass. Secret2/2 passes. Final forced uncached integration passes with cache
bypass058903109f2b592b,0cached,1m59.268s, including91 migrations,
concurrency, redaction, parallel isolation, fault/signal/timeout behavior and owned cleanup.
The first full run failed because the new999 fixture caseId exceeded the isolated tool's20-character
limit; no business assertion ran for that case. Shortening the owned test identifier fixed it;
focused Cart acceptance then passed, followed by the final full verification and forced integration.
Exact7-file scope/LF, final diff and guidance checks pass. No checks were skipped or waived.
Existing package build-output warnings do not change the successful build exits.

Security/privacy review: this correction changes only owner-local revision validation. Existing
Brand/Store, Guest/Participant, attribution, exact-version, append-only history and atomic Audit
controls remain. No foreign fact, credential, PII, public DTO, runtime grant or deployment changes.
No unaccepted Blocker/High finding remains within this correction. Storage exhaustion is fail-closed;
this WP neither widens PostgreSQL integer storage nor changes separate display-contract bounds.

Logs under /private/tmp: bop-wp2270-install.log, bop-wp2270-format.log,
bop-wp2270-ordering.log, bop-wp2270-cart.log, bop-wp2270-verify.log (failed first run),
bop-wp2270-verify-final.log, bop-wp2270-integration.log (before fixture-name fix),
bop-wp2270-integration-final.log, bop-wp2270-secret.log.
