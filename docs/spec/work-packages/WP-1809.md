# WP-1809 — Order Exception Workbench, Projection and Authorized Compensation Actions

## Metadata and authority

- Feature: `F18.1 — Merchant Web Flow`; technical owner: shared-infrastructure/projection; source
  fact owners: Dining, Payment, Ordering and Task
- Authority: Handoff Sections 24.9, 28.6, 50.20, 54.5 F18.1, 88.4, 88.10 and 88.22–88.30;
  accepted WP-1007, WP-1310 and WP-1800–1808
- Screen: `OPS-ORDER-EXCEPTION /operations/order-exceptions`; Projection:
  `merchant_order_exception_v1`
- Stacked local baseline: locally verified WP-1808 head `22b1888`; branch `codex/wp-1809`
- GitHub publication/integration: deferred by explicit Owner direction

## Ownership and execution contract

The generated `@bop/projection` module is technical contract owner of rebuildable cross-Domain
operational views. Section 92 keeps `platform_projection` reserved; a later persistence WP must use
explicit `projection-builder` access rather than Module schema ownership. This WP owns no source
business fact and adds a strict contract-first
Order Exception projection over Dining unpaid Batch, Payment reconciliation difference, capture
deadline exceeded and paid-without-fulfillable Order sources. It accepts only safe Store-scoped
references, bounded state, version/digest and canonical instants—never amount, Provider IDs/payloads,
Customer/contact, actor details or free text.

Critical sources must be projected within 15 minutes or activation fails with `SLA_MISSED`;
cross-scope facts fail closed. Rebuild is deterministic from immutable source contracts. No database
table/migration is added in this contract-first slice; durable consumer/checkpoint/storage remains
External Evidence.

Actions are intent routing only. Acknowledge/assign/resolve route to Task; compensation/retry route
to Payment; write-off request routes to Ordering. Every action requires exact Actor permission,
source version and idempotency reference. Resolve requires owning-source final evidence. Projection,
UI and Provider pending/unknown state never become financial or lifecycle finality.

## Exact tracked-file allowlist

- generated `packages/bop/projection/**` plus its lockfile importer
- `apps/merchant-web/src/**` for the canonical workbench route and browser DTO
- `docs/product/screen-registry.yaml`
- `docs/spec/README.md`, `docs/spec/work-packages/WP-1809.md`

No Dining/Payment/Ordering/Task private table, Provider API, amount, write-off execution, migration,
real producer/consumer, external action, deployment or GitHub operation is authorized.

## Acceptance and verification

- generated Module Manifest has no owned schema; reserved `platform_projection` remains governed by
  the shared-infrastructure registry;
- closed sources enforce scope, kind/owner consistency, version/digest and final evidence;
- Critical exception visibility over 15 minutes is rejected;
- projection is initialized-empty/rebuildable and does not invent Provider finality;
- actions require per-action permission, expected source version and idempotency;
- UI disables stale/finality-ineligible actions and has no status picker;
- package and Merchant Web format/lint/typecheck/tests/build, module/architecture and Screen Registry
  checks pass; External Evidence remains unavailable and unclaimed.

## Status

- Specification: `Bounded for local WP-1809 execution`
- Implementation: `Locally implemented and verified on codex/wp-1809`
- External Evidence: `Required; unavailable and not claimed`
- GitHub publication/integration: `Deferred by Owner direction`
