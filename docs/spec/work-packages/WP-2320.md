# WP-2320 - Shared Cart Participant Controls

## Authority and readiness

Baselinea8b1ada9e0ba904af61ed11b7418a9e08ec62f79; branch codex/wp-2320-shared-cart-controls.
Composite Sections0-97, CUST-CART Section88.6, WP-1201/1205 and accepted WP-2228 govern.
WP-2318 supplies OTHER_PARTICIPANT_ITEM and omits foreign notes. WP-2319 supplies current-authorized
local API reads/selection; Dining item writes remain unavailable. Continuous local work and verified
local commits remain authorized.

On2026-09-10 the Owner explicitly approved applying the verification-flow adjustment to this running
task in the companion task: "好，按你的建议调整，并应用到当前正在跑的任务中". Reuse valid evidence
for unchanged code; run directly affected checks during iteration and one necessary closeout set.
Full uncached repository regression is reserved for explicit stage milestones or a concrete risk,
not mechanically repeated for each small WP. Root/PWA AGENTS still require affected checks and the
PWA workspace lint/type/test/build. No root test-script refactor is part of this authorization.

## Screen contract and acceptance

CUST-CART remains the standalone /cart Customer Journey screen. Preserve items/configuration/quantity,
line estimates, lifecycle, Quote, service context and existing loading/empty/permission/not-found/
conflict/failure/offline/stale/disabled states. No new route, backend authority or generic redesign.

Render OTHER_PARTICIPANT_ITEM as human-readable ownership text, not a raw warning code. Disable that
row's quantity and remove controls, associate the explanation accessibly, and suppress its note even
if an inconsistent supplied view contains one. Current-participant controls retain their existing
behavior. Server commands remain the authority; UI hints grant no permission or Host override.

Before allocating a new operation key, the Cart state controller declines new update/remove intents
for a currently foreign-owned item. Preserve original unknown-outcome retry semantics and offline
no-replay behavior; a display refresh must not silently discard a possibly committed operation.

Tests cover own/foreign rows, note suppression, accessible explanation, no foreign mutation/key,
unchanged own actions, pending original retry and offline behavior. Real browser tests use explicit
synthetic HTTP views on desktop/mobile, proving disabled foreign controls, normal own controls,
no foreign mutation, layout/touch targets and no page errors. No real membership/Provider claim.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2320.md
- apps/customer-pwa/src/cart/CartPage.tsx
- apps/customer-pwa/src/cart/CartPage.test.tsx
- apps/customer-pwa/src/cart/cart-state.ts
- apps/customer-pwa/src/cart/cart-state.test.ts
- apps/customer-pwa/e2e/shared-dining-cart.spec.ts

## Verification inventory and plan

Already complete on the unchanged backend baseline: API551/551; Ordering790/790; Cart PostgreSQL9/9;
real local customer lab1/1; full pnpm verify with40 builds; forced integration0-cache; guidance,
architecture/contracts/migrations101 and secret checks. Nothing remains running from WP-2319.
These results remain valid for backend behavior; this UI-only WP will not rerun PostgreSQL, the lab,
API/Ordering tests or full uncached integration. They are explicitly reused, not newly executed.

Run pinned frozen install once. Finish directly affected edits, then PWA format/lint/type/test once;
full PWA unit tests include all unit files named by pwa-security:acceptance. Run the selected existing
Playwright command for shared-dining-cart, customer-demo, pickup-configuration and checkout-continuity.
Its preview:demo-proof performs the production build; run the existing verify-service-worker.mjs on
that exact output instead of rebuilding it. Reuse these results for build and offline security.
Run affected import-boundary, Screen Registry, repository guidance, secret and docs-format checks;
final exact-eight-file LF/diff and resource review. Rerun only affected failures or changed code.

A full shared-Cart end-to-end milestone, a persistence/authorization change, or an unresolved concrete
cross-module risk will trigger broader regression. No blanket cache bypass is planned here. No new
migration, dependency, backend, real data, credential mechanics, analytics, Service Worker behavior,
merge, push or deployment. Dining item writer/configurator/lab activation remain subsequent work.

## Observed results

Acceptance PASS for participant controls and preserved recovery/offline behavior. Pinned frozen offline
install passes for41 projects without lockfile change. PWA639/639 across36 files passes (+5 cases).
Lint identified an unused counter accidentally added to an existing test; restoring that test's
original key factory was the only follow-up edit. Its21 state tests, file lint/format and affected
app TypeScript project were rechecked and pass. Other unit/lint/format results and the unchanged
Service Worker type-check remain valid and are reused; no full PWA test rerun was needed.

Selected Playwright50/50 passes in14.5s, including2 new desktop/mobile shared-Cart cases, existing
Cart/demo, Pickup configuration, checkout continuity and production-exclusion coverage. Foreign rows
have accessible ownership explanations, disabled44px controls and no rendered note or raw warning
code; own updates send the expected version/CSRF and only the own item reference. Offline disables
changes, no foreign mutation occurs, storage remains empty, layout does not overflow and no page
errors occur. These are synthetic HTTP UI tests, not live backend membership evidence.

The existing preview:demo-proof command built the production output used by those browser tests.
The existing generated Service Worker verifier passes on that same output, covering private-route
exclusion and no background replay without a second build. Full PWA unit coverage includes every
unit file in pwa-security:acceptance, so its composite unit/build command was not redundantly run.
Import-boundary22/22 plus validator, Screen Registry210, guidance33 ADRs/5 skills and secret scan2/2
pass. Browser listeners5174/4174 are closed. Final eight-file LF/scope/diff/docs review passes before commit.

Explicit reuse/non-execution: backend/API/Ordering/database/lab/integration evidence from baseline
WP-2319 is reused because no backend, migration, dependency, credential or database behavior changed.
Full pnpm verify and forced uncached integration were not run in WP-2320 under the Owner-approved
plan; they are not claimed as fresh results. A complete shared-Cart end-to-end milestone or concrete
cross-module/persistence risk triggers broader validation. No required affected check is omitted.

Security review has no unresolved blocker in scope: local hints prevent accidental new foreign edits
but do not replace server authorization; retained unknown operations still require server recovery.
No private cache, offline replay, analytics, new credentials or production authority is added.
Main/dependencies/migrations remain unchanged. No remote CI, push, merge or deployment is claimed.
