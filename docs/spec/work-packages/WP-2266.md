# WP-2266 - Pickup Creation Session Generation Fence

## Authority and readiness

Owner authorizes successive bounded implementation and verified local commits. Baseline9ab0b2f,
branch codex/wp-2266-pickup-creation-session-generation. Composite Sections0–97, WP-2250,
WP-2252 and WP-2265 retain foreground-only recovery, original candidate/key ownership, current
Session authority and no private persistence. The opt-in creation coordinator currently compares
CSRF values; same-value resets or change-away-and-back can leave an old asynchronous plan alive.

## Scope and ownership

Require an explicit opaque current-context capture port alongside CSRF get/set. Each private plan
captures its starting generation synchronously, checks it before and after asynchronous reads,
preparation, activation and recovery, and rejects stale plans with existing network_unknown.
Only a confirmed activation under the still-current lease may install candidate CSRF and capture
the resulting new lease. Preserve legitimate lost-response original-candidate recovery, single
flight, no automatic background replay, explicit online checks and default runtime denial.

The existing session/customer-transaction-context capture function is the browser authority;
tests must prove actual same-value reset and ABA invalidation. Synthetic fixture ports model the
same per-set invalidation. The actual HTTP/PG coordinator fixture supplies the explicit capture
port and must continue proving one binding/Cart/Audit after lost publication. No new Session
recovery policy, route, UI state/copy, renderer, screen placement, styling, public API or migration.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2266.md
- docs/spec/design/business-scenario-coverage.md
- apps/customer-pwa/src/cart/pickup-cart-creation.ts
- apps/customer-pwa/src/cart/pickup-cart-creation.test.ts
- packages/database/test/cart-binding-store-acceptance.test.mjs

## Acceptance and verification

1. PWA tests prove stale generation before execution, during Locate/Prepare/Activate/Recover/Read,
   same-value and ABA resets, completed-plan retry and no stale credential publication or next
   binding request. Legitimate own handoff, original-key lost-response recovery and single-flight
   behavior remain successful. Use the actual shared CSRF context in focused regression cases.
2. Cart PostgreSQL acceptance retains actual Identity/Ordering/Audit and browser coordinator
   behavior with the required generation port. Only explicitly synthetic Store/Catalog facts.
3. Pinned frozen install; affected PWA/database format/lint/typecheck/test/build; PWA security,
   ordering-cart:acceptance, full pnpm verify, forced pnpm test:integration --force; Secret scan,
   exact6-file scope/LF, final diff and owned cleanup. No unrun check is a pass.

## Boundaries

No notes, credentials, recovery proofs, unrestricted IDs or private Cart data in new logs, URLs,
analytics, fixtures or storage. Generated/synthetic credentials stay inside test memory. No UI or
browser layout changes, no new dependency/migration, no production/staging activation, main merge,
push, deployment or external service. No private pending plan persisted across reload. Default
page composition, current Catalog/pricing producers, selected-option prices, shared DineIn and
Provider readiness remain separate. Uncommitted work may be discarded; accepted work uses a
forward fix, never deletion of history. Whole project readiness remains unclaimed.

## Verification record

Acceptance1 PASS: PWA417/417 includes13 new generation cases. Plans capture the generation
synchronously before scheduling execution. Same-value invalidation before execution and during
Locate/Prepare/Activate blocks all subsequent requests and credential publication. Superseded
Recover plans cannot issue completion; a late completion cannot publish, and a failed completion
cannot fall back to activation in a newer generation. Own confirmed handoff captures the new lease;
final readback and completed-plan retry still check it. Real shared CSRF context tests prove
same-value and ABA reset rejection plus legitimate original-candidate response-loss recovery.
Late definitive Session failure under a newer generation remains network_unknown.

Acceptance2 PASS: Ordering518/518 and Cart PostgreSQL6/6 pass. The existing actual Identity,
Ordering, Audit and production browser coordinator acceptance supplies a per-set opaque generation
port; interrupted activation/publication and same-original recovery still produce one binding/Cart.
Synthetic current Store/Catalog evidence remains explicit. Test servers and transaction resources
close under the owned isolated harness.

Acceptance3 PASS: full CI=true pnpm verify completed with40 successful builds. Frozen install, PWA security111/111 plus generated Service
Worker policy and exact6-file scope/LF pass. Forced uncached integration passes with cache bypass ef9cb895633a518a,0cached,1m58.17s,
including91 migrations, concurrency, parallel isolation, redaction, fault/signal/timeout handling
and owned cleanup. Secret2/2 and the final full verification pass. No browser layout/renderer changed; no new visual scenario is required for this opt-in
coordinator-only change. Existing browser transport authorization remains unchanged.

Security/privacy review: opaque generation closures and pending candidates exist only in private
foreground memory. No generation value, credential, recovery proof or Cart state is serialized,
logged, persisted or placed in a URL. The authority hook is mandatory and has no permissive default.
No unaccepted Blocker/High finding remains in the changed coordinator. Cross-document/reload
recovery, default page composition and external production evidence remain separate gates.

Logs under /private/tmp: bop-wp2266-install.log, bop-wp2266-pwa.log,
bop-wp2266-pwa-security.log, bop-wp2266-cart.log, bop-wp2266-verify.log,
bop-wp2266-integration.log and bop-wp2266-secret.log.

Final closeout: all acceptance criteria pass locally, with no required check skipped. Affected
format/lint/type/unit/integration/architecture/contracts/migration/build checks pass through full
verification. Exact6-file scope/LF and final diff review pass; no unrelated files, dependency,
production change, main merge, push, deployment or external service operation. Next bounded work
connects the established Pickup-only coordinator to foreground product configuration and verifies
explicit creation/add recovery in the browser. General reload/shared DineIn remain separate.
