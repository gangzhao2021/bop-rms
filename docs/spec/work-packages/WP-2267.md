# WP-2267 - Pickup Product Configuration Creation Flow

## Authority and readiness

Owner authorizes successive bounded implementation and verified local commits. Baseline2491dc3,
branch codex/wp-2267-pickup-configure-creation. Composite Sections0–97 and WP-2248/2250/2252/
2265/2266 establish original-intent recovery, authorized server item writes and Session generation
ownership. The default product configurator still uses the unavailable legacy Cart-create route.

## Screen contract and scope

Section88.6 CUST-SELLABLE-CONFIGURE remains contextual under CUST-SELLABLE-DETAIL at the
existing /menu/items/:sellableId route; customer.public, exact Guest/Store scope and fresh public
Catalog projection apply. Preserve quantity/options/note validation, unknown/conflict/denied/offline
states, explicit retry, accessible names, keyboard/touch/reflow, privacy and absent-price messaging.
No new route, visual redesign, inferred Catalog eligibility, option prices or Payment enablement.

Add a browser Pickup Cart client composing the existing read/item transport, binding transport,
private creation coordinator, shared current-CSRF generation and browser-generated UUIDv7
preparation references. Construction performs no I/O. Only explicit createCart may locate/prepare/
activate/recover/read; only the existing explicit product submit may then add. No legacy create
POST for the Pickup path. Existing current Cart requires no binding mutation. Interrupted binding
and item responses retain the original respective operation identities on explicit retry.

Pass established journey channel through Menu screen/detail/configurator. Use Pickup composition
only for Pickup; absent/DineIn context retains existing behavior. Reset contextual UI ownership on
Store/channel changes. Preserve injected controllers and demo read-only exclusion. Do not persist
pending plans or add automatic reconnect replay. Runtime services remain opt-in and deny by default.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2267.md
- docs/spec/design/business-scenario-coverage.md
- apps/customer-pwa/src/cart/pickup-cart-client.ts
- apps/customer-pwa/src/cart/pickup-cart-client.test.ts
- apps/customer-pwa/src/menu/MenuPage.tsx
- apps/customer-pwa/src/menu/MenuPage.test.tsx
- apps/customer-pwa/e2e/pickup-configuration.spec.ts

## Acceptance and verification

1. Unit tests prove actual browser transport composition, no construction/read-only mutation,
   existing Cart reuse, fresh preparation key, shared CSRF handoff/generation, original binding
   recovery, original add retry, default-denied provider/offline and no legacy create route.
   Menu tests distinguish Pickup from DineIn and preserve explicit controller/demo boundaries.
2. Production PWA browser scenarios on desktop and390px touch use clearly synthetic HTTP facts:
   Entry→fresh Menu→detail/configure→missing Cart→binding→Add. Interrupt activation or Add;
   explicit same-operation retry restores success, reconnect does not mutate, request keys/bodies
   remain stable and no duplicate item is inferred. Verify labels, reflow, keyboard/touch and
   console errors. Missing providers must not display success. No live Store/Catalog proof claimed.
3. Pinned frozen install; affected PWA format/lint/typecheck/test/build; full browser tests,
   PWA security, full pnpm verify, forced pnpm test:integration --force, Secret scan, exact8-file
   scope/LF, final diff and owned browser/server/database cleanup. No unrun check is a pass.

## Privacy and external boundary

Only generated/synthetic credentials in private test memory; no raw notes, sensitive option
labels or private IDs in screenshots/logs/analytics/URLs. Pending recovery remains foreground-only.
No migration, dependency, production/staging activation, external service, main merge, push or
deployment. Real Catalog authority, pricing producers/selected options, shared DineIn, reload
recovery and Provider evidence remain separate. Uncommitted scope may be discarded; accepted
work uses a forward fix, never deletion of history. Whole project readiness remains unclaimed.

## Verification record

Acceptance1 PASS: PWA430/430 includes8 real browser-client composition tests and5 channel/
injection cases. Construction/read-only lookup performs no mutation; an existing Cart skips
binding. New creation uses a distinct UUIDv7 preparation identity, real shared CSRF generation
and confirmed handoff before readback. Activation acknowledgement loss recovers the original
candidate. Configure orchestration retains the Add key/body after acknowledgement loss and does
not replay on reconnect. Offline, missing binding providers and same-value Session reset deny
success. DineIn/absent context retain existing client selection; injected controllers are preserved.

Acceptance2 PASS: full production-browser48/48 in14.0s, including6 new desktop/390px touch
scenarios. Actual Entry and Menu components feed the contextual configurator; synthetic missing
Cart/prepare/activate/complete/Add responses exercise the production client and controller. Lost
activation returns unknown then original-key completion; lost Add retries the identical key/body/
version without another preparation. Missing providers never display success or invoke Add.
Offline disables both Add and retry; reconnect only enables explicit retry. Keyboard Enter and
touch actions recover and navigate to one displayed item. Reflow and unexpected-console checks
pass. Existing demo exclusion and Checkout continuity remain green. Interception is synthetic
transport evidence, separate from WP-2265/2266 actual isolated HTTP/PostgreSQL proof.

Acceptance3 PASS: full CI=true pnpm verify completed with40 successful builds. Frozen install, affected lint/type checks, PWA security
111/111 and generated Service Worker policy pass. Exact8-file scope/LF and initial diff review
pass. Forced uncached integration passes with cache bypass ef9cb895633a518a,0cached,2m1.197s,
including91 migrations, concurrency, redaction, parallel isolation, fault/signal/timeout handling
and owned cleanup. Secret2/2 and the final full verification pass. Initial channel-prop type
annotation was completed before the passing typecheck. No partial invocation is a full pass.

Security/privacy review: current Entry channel selects only the Pickup adapter; public Store/
channel keys reset contextual ownership without exposing keys in the DOM. Credentials and recovery
proofs remain in shared/plan private memory and approved transport headers/body. No persistence,
background replay, new analytics/logs, price/eligibility inference or production activation was
introduced. Existing server authorization and mandatory Catalog/Quote providers remain controlling.
No unaccepted Blocker/High finding remains in this bounded page composition. General reload,
shared DineIn and real operational/commercial evidence remain separate gates.

Logs under /private/tmp: bop-wp2267-install.log, bop-wp2267-pwa.log, bop-wp2267-types.log,
bop-wp2267-lint.log, bop-wp2267-pwa-security.log, bop-wp2267-browser.log,
bop-wp2267-verify.log, bop-wp2267-integration.log and bop-wp2267-secret.log.

Final closeout: all acceptance criteria pass locally; no required check skipped. Final exact8-file
scope/LF, documentation format/guidance and diff review pass. Playwright closes its owned demo
and production preview servers and browsers; isolated integration reports owned cleanup. No new
migration, dependency, production change, main merge, push, deployment or external action. Next
bounded work verifies continuous item-change/Quote interactions across actual local owner stores;
commercial facts and remaining shared-DineIn/reload mechanisms remain separate.
