# WP-2222 — Persisted Pickup Cart Browser Acceptance

## Authority and readiness

Clean verified baseline WP-2221 `9016678`, branch `codex/wp-2222`. Continued local implementation
and verified commits are authorized; no push, merge, deploy or real source activation. Composite
Handoff 0.5.3 Sections 24, 52, 88–97; WP-1700, WP-1205, WP-2215 and WP-2216–2221 supply contracts.
Use CUST-ENTRY, CUST-MENU, CUST-SELLABLE and CUST-CART as registered; no Screen or route redesign.

## Scope and acceptance

1. Serve normal Customer production assets over test-owned same-origin HTTPS. Real Chromium must
   enter through a signed synthetic Pickup QR, obtain an actual persisted Identity Session, browse
   the real public Catalog query, add through the existing configurator and update/remove in Cart.
   Verify matching persisted Cart/operation/Audit counts on desktop and mobile.
2. Verify offline commands stay disabled, refresh recovers persisted reads, and missing in-memory
   CSRF after refresh fails safely without storing credentials or inventing a Session-resume endpoint.
3. Inspect private cache/storage/URL/log exposure and context isolation; use disposable TLS pins,
   scoped PostgreSQL roles/databases and finite browser/listener/pool cleanup. No screenshots/traces
   or real Store/Provider facts. All public sources are explicit synthetic fixtures.
4. Add the finite browser gate after the existing Chromium installation in CI. Run frozen install,
   new and retained Entry browser gates, affected checks and full local verify. Default production
   runtime, source providers, application code and API contracts remain unchanged.

## Allowlist

- This brief, README.md, docs/spec/README.md, docs/spec/project-status.md, package.json,
  .github/workflows/bootstrap.yml
- packages/database/test-support/customer-entry-browser.mjs
- packages/database/test/customer-cart-browser-acceptance.test.mjs
- packages/database/vitest.customer-cart-browser.config.ts

The shared HTTPS helper may add only explicit Cart/Menu API and SPA routes for the new opt-in
journey; its default Entry behavior remains unchanged. Reuse existing fixtures and public services.
Generic personal customer-flow skill routes/scripts describe a different checkout; this repository's
accepted fragment entry and existing scripts take precedence. No new /c route is introduced.

## Verification and rollback

Pinned Node/pnpm, pnpm install --frozen-lockfile, existing affected checks, new
pnpm customer-cart-browser:acceptance, retained pnpm customer-entry-browser:acceptance and full
DOCKER_HOST=unix:///Users/ryanzhao/.colima/default/docker.sock TMPDIR=/private/tmp CI=true pnpm verify.
Rollback removes only new acceptance/CI wiring and opt-in helper routes. No Domain state migration.

## Status

The dedicated Cart browser gate passed (1/1 test, both 390×844 and 1280×900 contexts),
and retained Entry browser acceptance passed (2/2). Each Cart reached version 4 with exactly three
Item operations and four Cart Audit records. Refreshed nonempty reads succeed; absent in-memory
CSRF refuses modification and leaves version 3 unchanged. Offline recovery requires the existing
explicit refresh control. Service Worker activation, private storage/cache checks and separate
context Cart references passed. Complete local verification passed on 2026-09-05.

| Acceptance                       | Observed evidence                                                                                                                                                                                                            | Result |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Normal persisted browser journey | Final `pnpm customer-cart-browser:acceptance`: 1/1, both viewports; real Entry/Menu/Cart handlers, isolated PostgreSQL; Cart version 4, three Item operations, four Audit rows per context                                   | PASS   |
| Offline and refresh protection   | Offline mutation controls disabled; explicit reconnect refresh; nonempty/changed-empty reload; missing CSRF leaves persisted version unchanged                                                                               | PASS   |
| Privacy, isolation and cleanup   | Activated Service Worker; no private API cache, readable cookie, local/session storage or IndexedDB; no QR/CSRF/cookie in DOM or runtime logs; separate context Cart references; bounded shared browser/TLS/database cleanup | PASS   |
| Retained checks and CI wiring    | Entry browser 2/2; frozen install; complete `pnpm verify` exit 0, root 358/358, Ordering 227/227, API 189/189, all 40 builds (39 cached); finite CI command after Chromium install                                           | PASS   |

Initial verification found a new diagnostic-handler lint issue, fixed using the existing safe
assertion pattern without exposing raw browser/database errors. A later full run stopped because
isolated PostgreSQL could not start for the unrelated Production Batch acceptance. Docker was
healthy with no remaining containers and adequate disk; no cause was established. The unchanged
`pnpm production-batch:acceptance` passed on retry, then the entire `pnpm verify` passed. No check
was disabled or unrelated business code changed. Remote CI, deployment and real source acceptance
were not run; this task is local synthetic acceptance only.

This increment changes test infrastructure and CI only. Real Catalog/Store publication, default
runtime activation, Session-resume credentials, DineIn visibility, Quote/Pricing and Checkout remain
separate owning increments. No remote CI or production evidence is claimed.
