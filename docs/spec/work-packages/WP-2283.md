# WP-2283 - Complete Dining Move Command History

## Authority and readiness

Baseline 3fb300a27e6ec8385b8dbe1a24c7f5d4fbf888b4; branch
codex/wp-2283-dining-move-intent-record. Continuous bounded local implementation and verified
commits remain authorized. Composite Sections 0–97, WP-2112 Move policy, WP-2271 current authority
and historical object isolation govern. WP-2282 Closing persistence is complete locally.

## Scope and acceptance

Existing Move history stores its digest and resulting Session/Tables but omits the original
party-size and expected-version command. Retain a closed, copied, immutable canonical command
with each new Move record. Extract private command/record parsing so a future owner adapter can
validate original intent without reconstructing missing business facts. Preserve current command
key order, digest bytes, permissions, capacity policy, events and actor-bound authorized replay.

Validate exact command/operation/Session/source/target identity, expected next versions, coherent
resulting assignment/occupancy, scope, capacity, event and Audit. Recompute canonical intent using
the existing hash port. Historical commands must equal the currently authorized retry, even when
a dependency supplies a matching digest. No current-state or authority check is removed. Tests
cover reordered input keys, exact command retention, altered/missing command fields, getters,
source mutation, canonical digest and complete parser errors. Existing Table and Move cases remain.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2283.md
- packages/rms/dining/src/application/ports/dining-table-ports.ts
- packages/rms/dining/src/application/dining-table-service.ts
- packages/rms/dining/src/application/dining-move-record.ts
- packages/rms/dining/src/tests/dining-table-service.test.ts

## Verification and boundaries

Pinned frozen offline install, affected format/lint/typecheck/Dining tests, full pnpm verify,
sequential forced pnpm test:integration --force, Secret/guidance and exact7-file LF/diff/security
review. Long checks use a command-bound idle inhibitor. Every criterion needs observed evidence.
Rollback is a reviewed local correction retaining complete intent and current authority.

No migration or storage writer, new business action, capacity authority, credential reissuance,
Table release/cleaning, shared Cart fence, UI/API activation, dependency change, real Store/Provider
fact, main merge, push or deployment. Existing records are only injected fixtures; no persisted
Move history exists to migrate. Next allowed work after verification is a separate owner Move
transaction. Scoped acceptance evidence is recorded below.

## Focused observed evidence

Pinned frozen offline install passed. Dining547/547 includes21 new command/history cases; affected
format, typecheck and lint pass. The initial extracted timestamp type was corrected to the existing
DiningInstant type before tests. Canonical key order and digest bytes are preserved, including
reversed caller property order. Both new and historical records are independently validated.

The complete command is frozen and independent of caller mutation. Missing/altered command fields,
capacity-eligible altered party size even under a colliding synthetic hash, missing command,
forged digest, getters, non-string hash coercion and hash/equality exceptions fail closed. A hash
provider that changes its result during creation cannot cause a write. Existing current actor,
Store scope, original start facts and actor-bound historical Audit checks remain in place.

Security review confirms only already-required opaque references, expected versions, party size
and canonical time are newly retained. No raw credential, payment/health fact, client permission,
PII summary, URL, log, external write or live Store fact is added. The parser is private application
code for future owner storage; it does not grant authority or publish an Event. Owner persistence,
including a durable original acknowledgement, remains separate. Full pnpm verify passed with exit0: Dining547/547, Dining PostgreSQL6/6 and all40/40 build tasks.
Sequential forced integration passed with hash `8253e4f48c3b8f37`, zero cache hits, exit0 and
duration1m58.396s: migrations, parallel isolation, fault injection, signal, timeout, redaction and
owned cleanup. Temporary idle inhibitors ended with their commands; isolated database control
files are zero. Existing Turbo Dining no-output advisory is non-failing. No check was skipped or
weakened. Secret2/2, guidance, final documentation format and exact7-file LF/diff review pass.
All scoped criteria have observed evidence. Main remains unchanged and no external mutation,
push or deployment occurred. Next allowed work is a separate atomic Move store with original
commit acknowledgements, preserving current authorization and immutable command/result history.
