# WP-2329 - Quote Await Expiry Fence

Baseline93172e3; branch codex/wp-2329-quote-await-expiry. Composite Sections0-97, WP-1203
exact Quote expiry and WP-1204 Cart lifecycle plus WP-2328 current authorization govern.
Continuous local implementation/verified commits are authorized; preserve separate guidance edits.

The existing generic attachment verifies freshness at its captured request instant. A slow Pricing
call can cross Cart/Quote expiry while the same request remains authorized. In the Pickup entry,
retain the parsed owner Cart loaded for this request; at the fresh authorization observation before
Pricing and immediately before writing, require its lifecycle active. Before a new attachment write,
require current observed time strictly before quoteExpiresAt. Keep historical replay available under
current identity without renewing or rechecking old Quote expiry. Do not rewrite historical timestamps,
financial facts, Audit or persisted Cart lifecycle. Persistence still owns concurrent version fences;
the application check is not an atomic wall-clock lease for an in-flight commit.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2329.md
- packages/rms/ordering/src/application/pickup-cart-quote-service.ts
- packages/rms/ordering/src/tests/cart-quote-attachment.test.ts

## Verification

Add slow-Pricing Quote expiry just-before/exact/after cases, Cart expiry before Pricing and during
Pricing, and original expired-Quote replay without duplicate writes. Run affected Quote unit tests,
Ordering lint/type/build, API typecheck, the pricing-quote:acceptance components, format/import/Domain/guidance/secret
and final diff/resource checks. Reuse unchanged39 Ordering files from WP-2328 and WP-2325 full-stage
schema/UI evidence, plus same WP-2321 frozen installation with unchanged manifests/lock/pins. No
forced full regression per small WP; full verify and test:integration remain the next new cross-domain
implementation or release/merge milestone. No DineIn Quote, provider, price/tax source, schema,
credentials, HTTP DTO or external operation. Rollback removes only the application time fence.

Before execution, component inventory reuses WP-2328 Pricing248/API Quote9 from
/private/tmp/bop-wp2328-quote-pg.log at93172e3: Pricing source and API transport/fixtures are unchanged.
Run only the affected existing PostgreSQL component with pnpm exec vitest run --config
packages/database/vitest.price-quote.config.ts. Do not claim a fresh composite invocation.

## Results

PASS: the parsed owner Cart is retained only within this invocation. Fresh current-authority time
now gates its lifecycle before Pricing and before new persistence; quoteExpiresAt is exclusive at
that final write boundary. Original history bypasses new-write expiry checks and retains its original
expiry/Audit/attachment, while WP-2328 still reauthorizes result disclosure. No mutation of source
Cart, Quote or timestamps occurs and concurrent persistence version guards remain unchanged.

Fresh affected Quote unit77/77 passes, including five new just-before/exact/after Quote and Cart
expiry-during-await cases. Ordering lint/typecheck/build and API typecheck pass. Actual PostgreSQL
Quote5/5 in4 files passes in39.43s. Formatting/import22/Domain57/guidance/security2 and final scoped
diff pass. Logs under /private/tmp/bop-wp2329-{unit,lint,type,build,api-type,pg,format,import,domain,
guidance,secret}.log; tested93172e3 plus these four files. No failed run occurred in this WP.

Reused: WP-2328's unchanged39 Ordering files plus final current-authority tests, Pricing248/API Quote9
and WP-2325 unchanged full-stage schema/UI evidence, with source logs/revisions in their briefs.
There is no fresh composite pricing-quote:acceptance, full Ordering suite, full verify or generic
integration claim. The same valid WP-2321 frozen installation/manifests/lock/pins remain unchanged.
Owned isolated resources are checked at closeout. No raw credentials/notes, external services,
financial producer, migration, production activation or unrelated guidance files enter this change.
