# WP-2310 - Actual Dining Admission Browser Lab

## Authority and readiness

Baseline5367ee3c0fc0ebfc9c7d7a1830be1074a38272c7; branch codex/wp-2310-dining-browser-lab.
Composite Sections0-97, WP-1003/1006, WP-2307/2308/2309 and existing local Customer lab govern.
Continuous bounded implementation and verified local commits are authorized. Scope is an isolated
local test environment, not actual Store/Staff/Provider/production authority or deployment.

## Acceptance and design

Enable the actual Join client/journey/binding client in main.lab only when the server's existing
same-origin local bootstrap explicitly marks Dining available. Inject shared CSRF and canonical UUIDv7
operation generation. Add exactly the already approved uuid14.0.1 dependency to the PWA with the
corresponding lock importer; no upgrade or handwritten replacement generator. Production entry excludes
lab wiring. Existing contextual Screen contract, protected input and unavailable default stay intact.

Create bounded test support using public Dining Table/Staff-start/Join/consumption/current-binding and
Identity binding stores. Use isolated per-domain runtime roles, exact fixed scope and Audit adapters;
no private cross-domain runtime SQL or fabricated historical inserts. Synthetic Staff/Tenant/QR/current
context/abuse inputs are explicit fixtures. Each desktop/mobile browser receives its own Table/Session;
retain correct public-to-internal mapping for every context. The Join proof stays in Node test memory
until typed into the protected field, never in URL/bootstrap/log/storage/screenshot/trace.

Actual Chromium over temporary loopback HTTPS must establish a real Guest Cookie from QR entry, join
through UI/HTTP/Owner stores, rotate to one real DiningBound Guest, and continue to persisted menu.
Verify one Join/consumption and three Identity preparation revisions/Audit per browser, current bound
Cookie properties, no browser errors/storage and owned cleanup. Existing QR-origin/host/no-GET/stop,
menu, production-entry and resource-boundary acceptance remains. Double lost-ack recovery remains the
existing separate full-runtime acceptance, not falsely claimed as injected in this browser success case.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2310.md
- apps/customer-pwa/package.json
- pnpm-lock.yaml
- apps/customer-pwa/src/main.lab.tsx
- apps/customer-pwa/scripts/local-customer-lab.mjs
- packages/database/test/local-customer-lab-acceptance.test.mjs
- packages/database/test-support/local-customer-dining-seed.mjs

## Verification and boundaries

Intentional pinned offline lock update, then frozen install; PWA format/lint/type/unit/security/build;
actual local HTTPS/Chromium/PostgreSQL lab; existing browser53; full pnpm verify; sequential forced
integration; final nine-file scope/LF/diff/guidance/Secret/owned cleanup. agent-browser CLI absent,
existing Playwright is the disclosed fallback. No production feature activation, new Domain policy,
real key/abuse/TTL facts, Ordering/shared bills/Payment, Figma, external services or merge/push/deploy.
Forward fix; normal lab stop and temporary database/TLS cleanup remain mandatory.

## Observed implementation and verification

PASS: main.lab opts into the actual Join/binding journey only through explicit local bootstrap;
canonical uuid14.0.1 is the sole dependency/importer addition. No production activation or upgrade.
PASS: public Table creation/publishing and Staff start seed two independent contexts. Dining and
Identity runtime roles remain separate; actual owner adapters persist Join, consumption and binding.
PASS: HTTPS Chromium desktop and mobile-touch QR entry to DiningBound to persisted menu; two Join
and consumption operations, six preparation revisions and corresponding Audit rows. Old Cookie rotates;
only one Secure/HttpOnly/SameSite=Lax main Cookie remains. No storage, page errors or URL proof.

Pinned frozen install passes. Initial offline add lacked existing fsevents metadata; explicitly added
only the already locked UUID importer and package declaration. Frozen install required network for
unchanged supply-chain policy revalidation after the lock change; it passes without downloading packages.
PWA typecheck/lint,612/612 tests in36 files; browser53/53 in16.0s; security111/111 and generated
Service Worker policy/build pass. Final actual browser/database lab1/1 in9.76s.
Initial full verification rejected a temporary-Cookie family literal in the lab verifier. Replaced it
with the stronger assertion that only the registered main Cookie remains; privacy8/8 and lab pass.
Final full pnpm verify exits0, including40 builds (38 cached),100 immutable migrations and all gates.
Sequential forced integration exits0: d15abda22f877fe5, zero cache,1m59.166s; isolation/failure injection/
signal/timeout/redaction/owned cleanup pass. Final nine-file scope/LF/diff review, guidance and Secret2/2 pass. Owned integration/TLS
roots, containers and volumes are zero; main remains cedc44c9b6a6a1b389f77342078cc6f2d540dabe.
No whole-project, production provider or operational readiness claim.
