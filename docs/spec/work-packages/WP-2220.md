# WP-2220 - Customer Menu HTTP Persistence Acceptance

## Authority and readiness

- Composite 0.5.3 Sections 0-97; accepted WP-1025/1026/1028 menu projection/public-query contracts,
  WP-2207 runtime injection and WP-2219 candidate adapter. Existing HTTP and Domain policies remain.
- Local dependency baseline `c641d148aef3273fe945fcf1e646f38f963a2b85` (WP-2219 / PR194), branch
  `codex/wp-2220`. Predecessor exact-head/main verification is pending, not claimed complete.
- User explicitly authorized local development while waiting for predecessor CI. One WP per branch;
  retain PR194 unchanged. Before publishing this WP, align only its own commits to the predecessor's
  verified main and rerun affected checks. No out-of-order integration.
- Existing authorization ends Toronto09:00 / 2026-09-08 13:00:00 UTC. No new WP after08:30.
- DoR local PASS: existing handler, runtime, query service, persisted adapter and isolated fixtures
  exist. Synthetic public Store resolution is explicit; no external Store/runtime readiness claim.

## Scope and acceptance

Extend the existing isolated published-menu acceptance to exercise real loopback HTTP through
CustomerMenuHandler, the existing Catalog query service and WP-2219's actual PostgreSQL reader.
All routing, validation, policy and persisted behavior remain in the existing production components.

1. Canonical HTTP menu GET returns the persisted menu with option/disclosure/projection metadata,
   explicit unavailable pricing and public Store scope, while excluding private Brand/Store and
   generation identifiers, digest, authoring fields and SQL internals.
2. Malformed/unknown query input returns400 before database access; unresolved public Store or
   unmatched Channel/OrderType returns404 without leaking internal scope.
3. Stale/Rebuilding/Failed projections return503 and existing bounded retry guidance. Building,
   missing selected content/checkpoint corruption and injected database failure return503 without
   fallback or raw cause. No policy, error DTO or transport behavior change.
4. Retain all WP2219 database/RLS/transaction tests, assert bounded structured logs and shut down
   the real server plus owned database resources on both success and failure.
5. Frozen install, retained API/Catalog and dedicated real HTTP/PG acceptance, affected checks,
   full verify and exact-head/main CI pass before delivery is complete.

## Exact allowlist

- `packages/database/test/published-menu-projection-acceptance.test.mjs`
- `docs/spec/work-packages/WP-2220.md`
- `docs/spec/README.md`

## Verification and boundaries

Pinned root: `pnpm install --frozen-lockfile`; `pnpm catalog-published-menu-projection:acceptance`;
`pnpm --filter @bop-rms/api test`; affected database format/lint/typecheck/test/build;
`CI=true pnpm verify`; `pnpm secret-scan:check`; final diff/scope review. Run predecessor and this
WP's exact-head/main checks in dependency order; disclose local/dependency/CI status separately.

No API/Catalog/runtime production source, public contract, Screen, Store resolver, dependency,
migration, permission policy, environment or CI change. No real Store/Provider/key/personal-health
facts. Loopback HTTP fixtures and public product allergen snapshots are synthetic. No production
activation or broad business decisions. Rollback is test/docs only, never menu history mutation.

## Status

Local implementation is complete within the three-file allowlist. Frozen install, API178/178,
Catalog75/75, Event contracts28/28, dedicated real HTTP/PostgreSQL acceptance1/1 and database94/94
passed. Affected database format/lint/typecheck/build and diff whitespace checks passed. Initial
ESLint global-name errors were corrected using the Node URLSearchParams import and globalThis.fetch.
Complete CI=true pnpm verify passed (all40 builds), and final secret scan passed2/2. AC1–4 PASS;
AC5 local PASS, delivery pending. Final three-file scope/security self-review: Blocker/High/Medium/Low
0/0/0/0, disclosed solo review rather than independent approval. No production or contract changes.
Predecessor PR194 exact-head run34206701060 passed all17 steps and was merged at
6e105ce6ed35e310b9e8756712ce9692d337471c. Its exact-main verification remains pending; this WP
must be aligned and reverified before publication. No delivery completion is claimed.
