# WP-2244 - Independent Pickup Cart Read Adapters

## Authority and readiness

Owner authorizes continued bounded implementation and verified local commits. Baseline c1481b7,
clean branch codex/wp-2244-cart-read-adapters. Composite Sections0–97 and WP-2236/2239/2241/2243
supply Ordering-owned binding, Quote history, current Identity authorization and safe display.

The display query currently requires full write adapter construction, including Cart creation policy
and write-only reference providers. Export independent scoped read factories from the existing owned
persistence files and reuse the same implementations in full stores. The read-only objects expose
only current binding or latest Quote. No invented policy, audit producer, write reference provider,
new SQL owner or capability escalation is needed to construct them.

Current Identity authorization remains required before binding reads; the reader validates supplied
Guest shape, scope, Pickup context and time but cannot independently prove current revocation.
The authorized application reader remains the boundary. Exact Brand/Store RLS, immutable creator,
Cart version, attachment observation time, bigint hydration and bounded errors stay unchanged.
No new Command, Event, migration, dependency or history mutation. CUST-CART has no UI change.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2244.md
- packages/rms/ordering/src/infrastructure/persistence/pickup-cart-binding-store.ts
- packages/rms/ordering/src/infrastructure/persistence/cart-quote-store.ts
- packages/rms/ordering/src/tests/pickup-cart-binding-store.test.ts
- packages/rms/ordering/src/tests/cart-quote-store.test.ts
- packages/rms/ordering/src/index.ts
- packages/database/test/cart-binding-store-acceptance.test.mjs
- packages/database/test/cart-quote-store-acceptance.test.mjs

## Acceptance and verification

1. Reader factories require only scoped transaction runners, expose no mutation methods, and acquire
   no resources during construction. Existing full-store behavior uses the same read implementation.
2. Invalid scope, Guest/time, missing or malformed evidence and driver failures remain fail-closed;
   exact money, latest attachment selection and absence remain unchanged.
3. Existing real PostgreSQL/HTTP display exercises independent readers; existing write, concurrency,
   RLS, history and cleanup tests remain intact. No browser or production evidence is inferred.
4. Pinned frozen install, affected Ordering format/lint/typecheck/unit, ordering-cart:acceptance,
   full pnpm verify, uncached pnpm test:integration, secret scan and final scope/LF review pass.

Rollback is a local source revert with no stored-data change. Runtime/PWA wiring, new recovery or
DineIn behavior and real Store/Provider/operational evidence remain outside this package. No main
merge, push, deploy or external service mutation; no remote CI or overall completion claim.

## Final local results

Acceptance1–4 PASS locally. Ordering370/370 includes both full-store and independent latest-Quote
matrices, construction without resource acquisition, restricted method surfaces, foreign Guest and
duplicate binding denial, and bounded driver failures. Cart PostgreSQL6/6 passes: the existing real
Identity/Ordering/HTTP display now uses independent binding/Quote readers; latest Quote real reads
retain exact bigint amounts, absence, expired history and unchanged row/audit counts. Existing write,
concurrency, RLS and cleanup cases pass. Store/QR/policy branding remains synthetic as before.

Pinned frozen offline install, affected Ordering typecheck/lint/unit checks, full CI=true pnpm verify
(40 successful builds), uncached CI=true pnpm test:integration (cache missefa68f7edbe78110), Secret2/2,
and final9-file scope/LF/documentation checks pass. Existing Turbo output warnings are informational.
An edit-script syntax error made no source changes; an initial cross-Domain branded-reference type
comparison was corrected to explicit string comparison before final checks. No failed run is counted.

Logs: /private/tmp/bop-wp2244-install.log, bop-wp2244-cart.log, bop-wp2244-verify.log,
bop-wp2244-integration.log and bop-wp2244-secret.log in the same directory. No migration, dependency,
external action or production acceptance. Runtime and PWA wiring remain the next integration work.
