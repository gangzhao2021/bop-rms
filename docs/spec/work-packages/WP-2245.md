# WP-2245 - Opt-in Local Pickup Cart Read Runtime

## Authority and readiness

Owner authorizes continuous bounded implementation and verified local commits. Baseline bee6152,
branch codex/wp-2245-cart-read-runtime. Composite Sections0–97, WP-1205, WP-2221 and WP-2241–2244
supply the existing HTTP Cart contract, local runtime, authorized reads and independent adapters.

Compose existing public Owner exports into the opt-in development/test loopback runtime. An optional
Cart transaction runner enables reads; the caller still owns bounded database transactions and
resource cleanup. Reuse current Identity binding/credential/store ports, historical Catalog reader,
and scope-guarded Store public profile service. Identity resolution uses Background activity only;
the private read-session instance has deny-all creation admission and is never exposed for mutation.
No new Session, policy, credential or real Store evidence is supplied by this composition.

Add a narrow API bridge from Ordering safe display queries to CustomerCartPort. Map absence to
NotFound and current authorization denial to SessionExpired; bound all other dependency failures.
Forward only credential and optional Cart reference, with trusted server time owned by the reader.
All mutation methods return Unavailable without invoking the query. Existing handlers retain Cookie,
origin, input, no-store and safe DTO checks. Default runtime stays unavailable without configuration.

CUST-CART keeps its canonical route/fields/permissions/states and UI unchanged. No browser acceptance,
PWA recovery, DineIn support, Cart/item writes or Quote generation is claimed. One workspace-only
API dependency on the existing Ordering public package and matching lock entry is authorized; no
version upgrade, new third-party dependency, migration or private-table access.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2245.md
- docs/spec/design/business-scenario-coverage.md
- apps/api/package.json
- pnpm-lock.yaml
- apps/api/src/customer-cart-read-composition.ts
- apps/api/src/customer-cart-read-composition.test.ts
- apps/api/src/local-customer-runtime.ts
- apps/api/src/local-customer-runtime.test.ts
- packages/database/test/cart-binding-store-acceptance.test.mjs
- packages/database/test/local-customer-runtime-acceptance.test.mjs

## Acceptance and verification

1. Optional runtime composes actual Owner queries under copied exact scope; no DB access at
   construction, no creation admission, no writes/Session renewal and no production activation.
2. API bridge preserves Found/NotFound/authorization/unavailable behavior without private errors,
   request-time override or mutation side effects. Existing default and HTTP protections remain.
3. Actual HTTP/Identity/Ordering PostgreSQL tests use the production bridge; local runtime evidence
   exercises configured authorization and bounded failure. All Store/QR/policy fixtures are synthetic.
4. Frozen install after the workspace-only lock update; affected API format/lint/typecheck/tests,
   Cart and existing Session PostgreSQL configurations, full pnpm verify, uncached integration,
   secret/privacy and final scope/LF checks pass before local completion.

Rollback removes the optional composition and dependency with no data change. Caller-provided real
Store, admission, credentials and operational evidence remain external gates. No main merge, push,
deployment, remote CI or overall project-completion claim.

## Local evidence and limits

Acceptance1–3 PASS locally. API244/244 includes13 new bridge/runtime cases: safe display/absence,
authorization and bounded errors, mutation non-execution, default unavailable behavior, no resource
acquisition, no admission/credential generation and no downstream access after authorization denial.
Existing production-environment, loopback, input, Cookie, same-origin and no-store checks remain.

Session PostgreSQL5/5 across4 files passes. The actual local runtime denies unsupported DineIn before
Ordering, issues a synthetic signed-QR Pickup Session through real Identity persistence, reads its
absent binding through a SELECT-only Ordering role in a read-only transaction, returns404, bounds
injected Cart database failure with503 and preserves the exact Session record. Temporary clients,
roles and isolated databases are cleaned by the existing lifecycle. Cart PostgreSQL6/6 passes;
its actual Identity/Ordering HTTP scenario now uses the production API read bridge for Found,
other-reference and revoked-credential responses. Its Store/QR/branding/policy facts remain synthetic.
A complete nonempty-Cart browser journey or production Store readiness is not established.

Frozen offline install and final API typecheck/lint/format checks pass. The only lockfile change is
the existing Ordering workspace link. Initial offline lock generation could not find optional Turbo
metadata; the exact local-link entry was added without re-resolving third-party packages, then frozen
install validated it. An initial contextual parameter typing issue was corrected. The first API test
attempt could not bind loopback in the sandbox and was interrupted (exit130); the correctly permitted
rerun passes. New HTTP test requests initially omitted mandatory Fetch Metadata and correctly received
400; adding the required same-origin headers fixed the test without weakening production guards.
No failed or interrupted attempt is counted as evidence.

Acceptance4 PASS locally: complete CI=true pnpm verify (40 successful builds), final uncached
CI=true pnpm test:integration (cache miss7438662ca7db38c3), Secret2/2, and final11-file
scope/LF/documentation checks pass. The additional sandboxed documentation format invocation
triggered pnpm environment validation and aborted before dependency removal; its consistent-environment
rerun passed with the lockfile unchanged. Logs are retained
at /private/tmp/bop-wp2245-api-final.log, bop-wp2245-session-final.log, bop-wp2245-cart.log,
bop-wp2245-install.log, bop-wp2245-verify.log, bop-wp2245-integration.log and bop-wp2245-secret.log
in the same directory. All four acceptance criteria pass locally. Local commit only; main, remote
services and production are unchanged. PWA creation/recovery, DineIn shared ownership, complete
order/payment composition and real operational evidence remain unfinished.
