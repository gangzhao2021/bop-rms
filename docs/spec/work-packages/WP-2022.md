# WP-2022 — Duplicate Webhook Scenario

## Metadata and authority

- Feature: `F20.2 — E2E and Failure Injection`; owner: QA / Payment
- Authority: Handoff Sections 52.7, 52.25–52.28, 54.5 F13.1, 54.6 F20.2, 87.5 and
  accepted WP-1303–1305, WP-2021
- Stacked local baseline: locally verified WP-2021 head `c8b535c`; branch `codex/wp-2022`
- Provider / External Evidence: unavailable and unclaimed
- GitHub publication/integration: deferred by explicit Owner direction

## Resolved test boundary

WP-2022 extends the existing Payment Webhook Inbox fixture with one complete duplicate-delivery
scenario. Concurrent byte-identical verified deliveries must reduce to one durable receipt; both
receipt results must address that same record; concurrent and later processing must invoke the
Payment mapper exactly once and return the stored completion thereafter.

Provider Account + environment + Provider Event identity and evidence digest remain the dedupe
boundary. Conflicting evidence is not a duplicate and continues to fail closed. No real signature,
secret, Provider API, database, HTTP route or terminal Payment fact is introduced.

## Exact tracked-file allowlist

- `packages/rms/payment/src/tests/payment-webhook-inbox.test.ts`
- root focused acceptance script
- `docs/spec/README.md`, `docs/spec/work-packages/WP-2022.md`

## Acceptance and verification

- simultaneous identical deliveries return one `Accepted` and one `Duplicate`;
- both results share the first receipt reference and only one Inbox record exists;
- simultaneous processing returns one `Completed` and one `AlreadyCompleted`;
- a later duplicate and processing replay reuse the stored receipt/completion;
- mapper side effects occur exactly once and temporary raw-evidence copies remain cleared;
- affected/full local gates pass; GitHub and real Provider evidence are skipped and unclaimed.

## Status

- Specification: `Bounded for local WP-2022 execution`
- Implementation: `Locally implemented and verified`
- External Evidence: `Unavailable and unclaimed`
- GitHub publication/integration: `Deferred by Owner direction`

## Local verification evidence

- The focused WP-2022 scenario passes `1/1` with the other `12` file tests intentionally filtered:
  concurrent delivery returns exactly `Accepted` + `Duplicate`, concurrent processing returns
  `Completed` + `AlreadyCompleted`, and a later duplicate/process replay returns the stored records.
  Inbox size, completion size and mapper calls are each exactly `1`.
- Full Payment passes `194/194`. Root format, lint, typecheck and build pass across `31` packages;
  frozen offline install passes across `32` workspace projects.
- Repository Guidance, Module Manifest, Import Boundary `22/22`, Domain Layer Boundary `56/56`,
  Module Generator `16/16`, Database Ownership `29/29` and Database Permission `8/8` pass.
- Root Vitest remains `238/242`; only the unchanged four macOS APFS case-fold/realpath fixtures
  fail before the later Turbo phase. No Payment or WP-2022 test fails.
- Security/privacy review found Blocker `0`, High `0`, Medium `0`, Low `0`. All Provider Event,
  Account, evidence and raw-body facts are synthetic and remain inside the existing Inbox boundary;
  no secret, real payment/PII, log/URL persistence, Provider call or external service is added.
