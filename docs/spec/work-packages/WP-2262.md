# WP-2262 - Quote HTTP PostgreSQL Acceptance

## Authority and readiness

Owner authorizes successive bounded work and verified local commits. Baseline631c0ad, branch
codex/wp-2262-quote-http-postgres-acceptance. Composite Sections0–97 and WP-2237/2255–2261
retain exact Pickup ownership, complete Quote history, current Session/CSRF, separate owner-local
transactions and original-result retries. This WP proves the actual composition with isolated data.

## Scope and evidence boundary

Add one PostgreSQL/HTTP acceptance using real Identity credential/session storage, Ordering binding,
Cart item/Quote attachment stores, Pricing request/Quote stores and Audit. Use separate least-privilege
Identity, Ordering and Pricing roles with bounded transactions and transaction-local scope. Seed a
nonempty Cart through public owner services. Pricing candidate and Catalog/Store/QR/tax evidence
are explicitly synthetic fixtures; never claim real commercial or professional evidence.

Inject lost acknowledgement after Pricing request COMMIT and after Ordering attachment COMMIT.
Assert HTTP503 followed by same-key original success, exact money beyond Number safe range,
single Quote/request/attachment/Audit effects, concurrent retry, unchanged history/expiry, initial
CSRF/Cart/version denial and actual Session revocation on replay. Verify no credential/scope echo,
transaction-local context cleanup, closed clients/server and owned database/role cleanup.
No production change, new migration, dependency, business policy, main merge, push or deployment.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2262.md
- docs/spec/design/business-scenario-coverage.md
- packages/database/test/quote-http-store-acceptance.test.mjs
- packages/database/vitest.price-quote.config.ts

## Acceptance and verification

1. pricing-quote:acceptance passes the existing cases plus the real multi-owner HTTP scenario and
   fault matrix above. Do not call synthetic policy ports actual Store evidence or infer rollout.
2. Pinned frozen install, affected database format/lint/typecheck/test, full pnpm verify, forced
   pnpm test:integration --force, Secret/privacy, exact five-file scope/LF and owned cleanup.
   No required check skipped or unrun result claimed. If the test exposes a production defect,
   amend the owning WP scope before changing production code; never weaken the assertions.

## Verification record

Acceptance1 PASS: pricing-quote:acceptance passes Pricing248/248, HTTP8/8 and PostgreSQL5/5.
The new case uses actual Identity credential storage/authorization, Ordering binding/item/Quote
stores, Pricing request/Quote stores, Audit and the production API composition/HTTP handler.
Distinct no-login, non-bypass roles explicitly fail foreign private-table SELECT. All transactions
set local scope and verify it clears after COMMIT; clients and roles are cleaned.

A lost Pricing acknowledgement returns503 after exactly one Quote/request/Audit commit. Same-key
retry returns the original201 result and attaches it once. Concurrent retries return the same body
without new facts. A lost Ordering acknowledgement returns503 after its attachment/Audit commit;
retry recovers that exact Quote without another candidate or write. Decimal-string money exceeds
Number.MAX_SAFE_INTEGER without loss. Historical replay preserves original expiry, initial CSRF/
Cart/version failures perform no write, and actual stored Session revocation denies further replay.
Responses omit credentials, Session identity and internal Brand/Store scope. Candidate, Catalog and
Store/QR/tax policy evidence is explicitly synthetic. Five-file scope/LF and privacy review pass.

Acceptance2 PASS: pinned frozen install, database94/94, affected format/lint/typecheck/build,
full CI=true pnpm verify (40 successful builds, including final Quote PostgreSQL5/5), forced
uncached CI=true pnpm test:integration --force (cache bypass07f67f9a4f090ab6,1m57.015s), Secret2/2
and owned cleanup pass. Initial lint required the test to call globalThis.fetch explicitly; that
reference was corrected and final lint/type and full tests passed. No gate disabled, skipped check,
production code change, dependency, migration, main merge, push or deployment. This is local
multi-owner persistence/HTTP evidence, not production or complete browser-journey acceptance.

Logs under /private/tmp: bop-wp2262-install.log, bop-wp2262-quote.log,
bop-wp2262-database.log, bop-wp2262-database-final.log, bop-wp2262-verify.log,
bop-wp2262-integration.log and bop-wp2262-secret.log. All criteria pass locally.

Static review identifies a remaining recovery boundary outside this matrix: if Pricing commits but
Ordering has not attached before Quote expiry, attachment validation denies the expired Quote while
the PWA preserves an already-unknown operation. A future owning WP must supply authoritative
terminal reconciliation, including concurrent late writers, before safely releasing that intent.
The successful historical replay above already has an attachment; it does not prove this boundary.
