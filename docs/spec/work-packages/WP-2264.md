# WP-2264 - Authorized Quote Expiry Recovery

## Authority and readiness

Owner authorizes successive bounded implementation and verified local commits. Baseline24556d8,
branch codex/wp-2264-quote-expiry-recovery. Composite Sections0–97, Section88 CUST-CHECKOUT,
and WP-2253/2254/2258/2260–2263 retain exact Session/CSRF/Cart scope, original-operation replay,
immutable Quote validity and the committed Ordering expiry fence. No accepted price policy changes.

## Vertical scope and contracts

Ordering supplies a current-authorized reconciliation service over public Identity, Pricing request/
complete Quote history and the WP-2263 expiry store. It verifies current Session/CSRF and Pickup
binding before history, checks complete immutable source identity/times, reauthorizes before writes
and before returning a terminal result. Cart version may have advanced, but Cart/Guest/scope may
not change. No source absence, generic error, expired credential or client clock proves expiry.
Original attachment wins. A committed matching expiry returns only after fresh authorization;
unknown/failure remains unknown. No Quote recalculation, original key reuse or history mutation.

API composes this capability with explicit expiry Audit and bounded owner transaction runners.
Quote POST returns410 only for an authorized persisted original expiry, using a closed v1 receipt
bound to operationReference, cartReference and cartVersion. It exposes no Guest/Brand/Store,
credentials, digests, monetary facts or free text. Other rejection/uncertainty semantics remain.
OpenAPI describes the actual existing Quote body-version/CSRF contract and new terminal response;
it corrects the older generic If-Match description for this operation only. No route is added.

PWA accepts only a complete matching410 receipt under the unchanged Session context and deadline.
It can then release the original unknown plan, refresh current Cart and offer explicit new pricing.
Malformed/mismatched/late/changed-Session responses and generic422/409 retain unknown intent.
No automatic mutation, offline replay, local persistence, Service Worker or payment enablement.

## Screen contract

CUST-CHECKOUT is the existing standalone customer_pwa /checkout route, Ordering owner, phase_1,
customer.public with exact Guest Session/Store/journey scope. This refines existing Stale/Command
Failed recovery; loading/empty/permission denial/offline/conflict/unknown states remain. Original
Quote/tax truth remains server owned. Show a clear expiry status and an explicit new-quote button
only after terminal proof; keyboard, named controls, live alert, narrow reflow and non-color status
must work at390px touch and1440px desktop. No route/Registry/Figma/analytics redesign. Contact,
capacity, receipt/tip, selected-option prices and Payment/Provider evidence remain gated.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2264.md
- docs/spec/design/business-scenario-coverage.md
- packages/rms/ordering/src/application/pickup-cart-quote-expiry-service.ts
- packages/rms/ordering/src/index.ts
- packages/rms/ordering/src/tests/pickup-cart-quote-expiry.test.ts
- packages/rms/ordering/src/tests/pickup-cart-quote-expiry.fixture.ts
- apps/api/src/customer-quote-composition.ts
- apps/api/src/customer-quote-composition.test.ts
- apps/api/src/customer-quote.ts
- apps/api/src/customer-quote.test.ts
- apps/api/src/local-customer-runtime.test.ts
- packages/contracts/rest/catalog.ts
- packages/contracts/rest/catalog.test.ts
- packages/contracts/rest/generated/openapi.json
- apps/customer-pwa/src/checkout/checkout-client.ts
- apps/customer-pwa/src/checkout/checkout-client.test.ts
- apps/customer-pwa/src/checkout/types.ts
- apps/customer-pwa/src/checkout/CheckoutPage.tsx
- apps/customer-pwa/src/checkout/CheckoutPage.test.tsx
- apps/customer-pwa/src/cart/types.ts
- apps/customer-pwa/e2e/checkout-continuity.spec.ts
- packages/database/test/quote-http-store-acceptance.test.mjs

## Acceptance and verification

1. Ordering tests cover closed snapshot inputs, current authorization/revocation/binding scope,
   original request/Quote identity, canonical source history, clock/time bounds, advanced Cart,
   existing attachment, expiry commit loss/replay and post-write denial without terminal leakage.
2. HTTP tests and actual isolated PostgreSQL prove authorized410 only after the fence/Audit,
   original201 history, no additional Pricing candidate or attachment, revocation denial and safe
   unknown results after lost acknowledgements. OpenAPI source/generated artifact stay aligned.
3. PWA tests prove matching complete receipt releases one original unknown plan, refreshed Cart
   supplies a fresh explicit key, generic errors/mismatch/late/session/offline races remain safe.
   Desktop/narrow production-browser interception verifies named recovery controls, no automatic
   mutation, exact original retry and fresh explicit re-quote without raw credential display.
4. Pinned frozen install; affected Ordering/API/contracts/PWA/database format/lint/typecheck/test/
   build/security; pricing-quote:acceptance; PWA browser/security checks; full pnpm verify; forced
   pnpm test:integration --force; final Secret/privacy, exact23-file scope/LF and owned cleanup.

## Boundaries and rollback

All Store/Catalog/tax/candidate browser evidence is explicitly synthetic. No new migration,
dependency, production grants, Provider, real Store source, external services, main merge, push,
deployment or Figma mutation. Full project/production readiness is not implied. Uncommitted
scope can be discarded locally; accepted work uses a forward fix. Never delete immutable facts.

## Verification record

Acceptance1 PASS: Ordering490/490 includes35 new reconciliation cases. Current Session/CSRF
and binding are checked before original history, before expiry persistence and after the result.
All six authorization checkpoints deny revoked evidence; post-write failure remains unavailable.
Advanced Cart versions retain the original request version, changed identity/clock/binding and
malformed source/receipt are denied, source copies resist mutation across awaits and original
attachment takes precedence. Closed complete Quote codec validation precedes every expiry.

Acceptance2 PASS: API326/326, Quote HTTP9/9, Pricing248/248 and Quote PostgreSQL5/5 pass.
Actual current Identity/Ordering/Pricing/Audit stores prove expired original requests return410
only after committed fencing, lost expiry COMMIT acknowledgement returns503 then same-key410,
no extra attachment/Quote/Audit occurs and revoked Session cannot retrieve the terminal result.
Receipts omit private scope, Guest identity, credentials, Quote details and digests. Contracts29/29,
OpenAPI generation/reference validation, affected lint/type and database94/94 pass. The older
shared500 error remains generic; Quote409 and terminal410 have their precise response contracts.

Acceptance3 PASS: PWA404/404 and production-browser42/42 pass. Original unknown retries keep
the same key/body; a matching complete410 releases the plan, refreshes Cart and allows only an
explicit new operation. Missing/mismatched/invalid receipts, generic errors and superseded Session
contexts remain unknown. A failed Cart refresh cannot replay the released operation. Desktop
keyboard and390px touch scenarios pass reflow, named controls, no automatic mutation and new-key
checks. Final desktop proof verifies focus moves to the new-quote action when the removed control
left focus on the body. Browser transport interception and all commercial facts are synthetic.

Acceptance4: full CI=true pnpm verify PASS with40 successful builds. Frozen install, affected
checks, PWA security111/111 and Secret2/2 pass. Final closeout checks follow below. Forced uncached integration passes with cache bypass
b0978bf5f63e7a32,0cached,1m59.459s, including91 migrations, concurrency, redaction, fault/signal/
timeout checks and owned cleanup. Exact23-file scope/LF passes. Initial fixture Session expiry
values were corrected to the existing exact4h/24h contract. A sandboxed API run could not bind
local listeners and was stopped; the authorized local-listener rerun passed326/326. Initial
contract-test mapping and one formatting omission were corrected before final full verification.
No blocked/partial invocation is represented as a full pass.

Logs under /private/tmp: bop-wp2264-install.log, bop-wp2264-static.log, bop-wp2264-api.log,
bop-wp2264-quote.log, bop-wp2264-contracts.log, bop-wp2264-database.log, bop-wp2264-pwa.log,
bop-wp2264-pwa-security.log, bop-wp2264-browser-final.log, bop-wp2264-verify.log,
bop-wp2264-integration.log and bop-wp2264-secret.log.

Final closeout PASS: documentation format, guidance, final Secret2/2, exact23-file scope/LF and
diff review pass. All acceptance criteria pass locally; no required check skipped. Existing
no-output warnings for five typecheck-only build tasks do not change the successful40-task exit.
No migration, dependency, production change, main merge, push, deployment or external action.
