# WP-1101 — Store Tax Configuration

## Status and authority

- Status: `Implemented, verified, integrated and cleaned`
- Branch: `codex/wp-1101-implementation`
- Exact predecessor: integrated WP-1100 squash
  `main@5a19f44837c5496770301e7033a02b6b2958ceac`
- Owner source: `BOP-RMS-HANDOFF` version `0.5.3`
- Governing sections: `27.6`, `44.9`, `50`, `54.5 F11.1`, `80.11`, `86.8.3 IDR-0024`
  and `88`
- External Evidence: `NOT AVAILABLE / NOT CLAIMED`

## Purpose

Define the Pricing-owned Store Tax Rule Set that resolves a version-pinned, effective rule from
Store jurisdiction/registration references and Catalog tax classification. Published snapshots
cover exact order type and charge type contexts; preserve tax component, treatment, inclusion,
rounding, compound order, exception evidence and receipt presentation; and produce WP-1100
resolved rule inputs without performing Quote calculation.

## Ownership and boundaries

- Pricing owns rule versions and resolution. Store/Operating Entity own identity, jurisdiction and
  registration facts; Catalog owns classification.
- The accepted Pilot scope is explicitly `CA-ON`, `CAD`, `America/Toronto`.
- Drafts may be incomplete. Published versions require current registration applicability,
  professional review bound to the exact snapshot digest, approved fixture references and complete
  deterministic rule coverage.
- No real Ontario rate, classification, rebate, registration, accountant approval or receipt
  conclusion is seeded or claimed. All acceptance fixtures are synthetic.
- Price Books, Quotes/API, Order/Payment, UI (owned by WP-2103), providers and events are absent.

## Implementation

- strict closed Domain contracts for versioned configuration, evidence and resolution;
- fail-closed conflict, contiguous-order, exception and coverage validation;
- four `rms_pricing` tables for aggregate, immutable versions/rules and append-only operations;
- exact Brand + Store forced RLS, public ACL revocation and database ownership manifest; and
- dedicated Domain and isolated PostgreSQL acceptance wired into the root suite.

## Local acceptance

Verified on `2026-08-01` from exact predecessor `5a19f448`:

- Pricing tests: `37/37 PASS`; package lint and typecheck: `PASS`;
- WP-1101 isolated PostgreSQL acceptance: `1/1 PASS`, proving full migration, evidence constraints,
  immutable version/rule/operation rows and exact Store RLS;
- canonical migration catalog accepts `31` migrations and Pricing schema ownership;
- the two migration-catalog case-fold fixture failures and two config realpath fixture failures on
  macOS/APFS remain known platform limitations; pinned Linux exact-head CI is authoritative.

GitHub evidence:

- PR `#117` exact head `c6c43c2e034771bc1e596fbe606569c1d8860180` passed run/job
  `30739647570 / 91474624538` in `11m55s`;
- squash merge `61f20b46463cab02b99df8307c6a1e04e81c47a1`;
- exact-main run/job `30740044027 / 91475700769` passed in `13m25s`.

## Next action

WP-1102 may consume this contract from exact predecessor
`main@61f20b46463cab02b99df8307c6a1e04e81c47a1`.
