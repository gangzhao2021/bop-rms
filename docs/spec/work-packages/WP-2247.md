# WP-2247 - Opt-in Local Pickup Cart Binding Runtime

## Authority and readiness

Owner authorizes sustained bounded implementation and verified local commits. Baseline9638df6,
branch codex/wp-2247-binding-runtime. Composite Sections0–97, WP-2221/2234–2237/2240/2245/2246
supply local runtime, immutable Pickup ownership, credential handoff, reads and browser transport.

Add an API composition factory using only public Identity and Ordering services/stores. Exact
copied Brand/Store scope, Identity Session transactions, separate Ordering write transactions,
explicit versioned lifecycle policy, source channel/reference factory, Identity transaction audit,
Ordering audit descriptor producer, credential/recovery providers, preparation lifetime and clock
are required. No fake policy, key, audit or permissive admission default is provided. The private
Session service only authorizes existing Sessions; unrelated Session creation admission denies.

Expose this composition as optional cartBinding configuration in the development/test loopback
runtime. Cart read configuration must also exist before enabling binding. Existing default routes
remain unavailable; readiness and environment guards stay unchanged. The caller owns bounded
transactions, permissions, pools and cleanup. All business validation, history, credential handling,
idempotency and compensation remain in their existing owners. API adds no private SQL or policy.

The existing BFF retains exact Origin/Fetch Metadata/CSRF validation, candidate cookies and no-store
responses. Existing browser transport exercises the new production composition in the isolated
HTTP test, including failed owner activation after Identity commit and original-candidate completion.
CUST-CART UI/route/fields/accessibility remain unchanged. Page orchestration and full recovery remain
separate; no complete browser or production journey is claimed.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2247.md
- docs/spec/design/business-scenario-coverage.md
- apps/api/src/customer-cart-binding-composition.ts
- apps/api/src/customer-cart-binding-composition.test.ts
- apps/api/src/local-customer-runtime.ts
- apps/api/src/local-customer-runtime.test.ts
- packages/database/test/cart-binding-store-acceptance.test.mjs

## Acceptance and verification

1. Explicit scoped composition acquires no resources at construction; invalid configuration/input
   denies and no missing dependency is replaced with fabricated operational evidence.
2. Local runtime requires read capability alongside binding, keeps default/environment/HTTP guards,
   bounds dependency errors, and never issues new entry credentials as a side effect of binding setup.
3. Actual browser-client/HTTP/Identity/Ordering PostgreSQL scenario uses the production composition;
   prepare creates no Cart, interrupted activation preserves history and completion converges without
   duplicate Cart. Existing ownership, Audit rollback, concurrency and RLS acceptance remains intact.
4. Pinned frozen install, API format/lint/typecheck/unit/build, ordering-cart:acceptance, full
   pnpm verify, uncached pnpm test:integration, secret/privacy and final scope/LF checks pass.

No dependency, migration, permission grant, new business policy, PWA UI, external service, main merge,
push or production activation. Rollback removes optional composition with no stored-data change.
Real Store/policy/key/audit providers and operational approval remain external configuration gates.

## Final local verification

Acceptance1–2 PASS: API255/255, lint/typecheck/build pass. Construction is side-effect free,
invalid lifetime/input denies before persistence, binding requires reads, default routes remain
unavailable, and Origin/dependency failures do not issue credentials or call Ordering.

Acceptance3 PASS: Ordering370/370 and Cart PostgreSQL6/6 pass. The real browser transport, HTTP,
production composition and Identity/Ordering stores preserve prepare-without-Cart, predecessor
revocation after interrupted activation, completion after preparation expiry and stable replay.
Existing actual Audit rollback, concurrency, ownership and RLS tests remain intact. Store/policy
and cookie-boundary evidence is synthetic; no actual browser/TLS or complete UI journey is claimed.

Acceptance4 PASS: pinned frozen install, affected formatting, API checks, full CI=true pnpm verify
(40 successful builds), uncached CI=true pnpm test:integration (cache missbe6f420caf4fd37e,
1m57.114s), Secret2/2 and final8-file scope/LF/privacy/documentation review pass locally.
Logs: /private/tmp/bop-wp2247-install.log, bop-wp2247-api.log, bop-wp2247-build.log,
bop-wp2247-cart.log, bop-wp2247-verify.log, bop-wp2247-integration.log and bop-wp2247-secret.log
in the same directory. No migration or dependency changed.

All acceptance criteria pass locally. Page orchestration, item writes, DineIn sharing, general
recovery and real operational evidence remain unfinished. Local commit only; no main merge, push,
deploy or remote CI claim.
