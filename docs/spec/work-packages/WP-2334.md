# WP-2334 - Catalog Availability Rule Query Store

Baselinea805513; branch codex/wp-2334-availability-query-store. Owner authorizes sustained local
implementation and verified commits. Composite Sections0-97, WP-1023 and existing Brand-only
availability RLS govern. Preserve three guidance edits. Apply the work-package, domain-change,
security/privacy and verification skills already read in this task.

## Scope and acceptance

Add Catalog-owned PostgreSQL read adapter for existing SKU availability rules. Fixed Brand/Store
scope is supplied by the authorized caller; closed request supplies exact matching scope, SKU,
channel/order codes and UTC observation. Capture before awaits. Dedicated caller-owned read-only
transaction sets Brand RLS and clears Store GUC because existing policy is Brand-only; SQL still
explicitly restricts Store-or-Brand-default, SKU, Active lifecycle, effective interval and selectors.
Parse full owner rules and recheck all selected scope/time predicates; reject corrupt, duplicate or
inconsistent rows with bounded dependency error. Preserve both Store and Brand candidates so the
existing domain resolver owns priority/ambiguity. No availability approval, public HTTP access,
Inventory/KillSwitch assertion, Product/Bundle support, published menu substitute or private SQL
outside Catalog. Construction does no I/O; no schema/runtime grants or authoring changes.

Unit tests cover parameters, early denial, captured input, empty/current/default/overlay rules,
wrong rows, malformed/duplicate results and failure redaction. Extend existing actual PostgreSQL
availability acceptance with a dedicated least-privilege reader, read-only transaction, effective
selector/Store filtering, Brand default inclusion, conflicting decision preservation, transaction
context cleanup and no write permission. Synthetic rules are not real Store evidence.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2334.md
- packages/rms/catalog/src/index.ts
- packages/rms/catalog/src/infrastructure/persistence/availability-query-store.ts
- packages/rms/catalog/src/infrastructure/persistence/database-access.manifest.ts
- packages/rms/catalog/src/tests/availability-query-store.test.ts
- packages/database/test/availability-rule-acceptance.test.mjs
- tooling/database-ownership/validate.mjs
- tooling/database-ownership/validate.test.mjs

The existing persistence gate requires an exact owner/schema/table/path registration. Register only
this adapter and test wrong owner/schema/table, other files and direct driver imports still deny;
this follows existing per-adapter admission and does not disable the gate.

## Verification plan

Run Catalog unit/lint/typecheck/build, catalog-availability:acceptance (actual PostgreSQL),
Ordering tests/typecheck, API typecheck, changed-file Prettier, import/domain/ownership/permission
checks, repository-guidance:check, secret-scan:check and final scoped diff/LF/resource review.
No schema/HTTP/browser behavior changes; no new full browser or generic integration claim.
Reuse same WP-2331 frozen installation at13eedf7 plus its four files, unchanged manifests/lock/pins
Node24.18.0/pnpm11.13.0; source log /private/tmp/bop-wp2331-install.log. Full pnpm verify and
pnpm test:integration remain the next complete operational journey/release milestone; this owner
adapter is a bounded prerequisite, not that milestone. Historical WP-1023 integration gates apply
to its original integration. No remote/deployment/external grants. Rollback removes the read adapter;
no history or database changes. Results and failed/skipped checks must be explicit.

## Results

PASS: the new exported internal Catalog reader captures exact authorized scope/request values,
uses one dedicated read-only transaction and Brand-only RLS context, and filters Store-or-default,
SKU, lifecycle, channel/order and exclusive expiry in SQL. Parsed returned rules are frozen and
rechecked; malformed, foreign, duplicated or future-observed rows deny with a bounded error.
Both default and overlay candidates are preserved for the existing domain resolver. Only the exact
owner/schema/table/path is admitted by governance; direct pg imports and unrelated assets still deny.
No migration, write permission, commercial fact, safety approval, production activation or HTTP DTO.

Fresh catalog-availability:acceptance passes: Catalog141/141 and actual PostgreSQL1/1 (10.13s).
The database test uses the real adapter and a separate SELECT-only role; proves target Store/default
inclusion, other Store/Brand isolation, inactive/channel/order/expired/future exclusion, conflicting
Store decision preservation, read-only transaction and cleared context. It proves no INSERT grant
and no Product SELECT grant. The resolver's empty safety input is used only to prove Indeterminate
ambiguity, never to claim sellability. Owned local Docker inventory is empty after acceptance.
Final unit-only fixture typing cleanup passes the affected24/24, lint and typecheck; source code and
PostgreSQL fixture remain identical to the successful acceptance run.

Fresh Ordering879/879, Catalog lint/typecheck/build, Ordering/API typecheck, changed-file Prettier,
changed tooling/database-test ESLint, import22/22, Domain57/57, ownership184/184, permission8/8,
guidance and secret2/2 pass. Testeda805513 plus these nine WP files and the preserved guidance
edits. Logs: /private/tmp/bop-wp2334-{acceptance,unit-final,type,lint,build,ordering,ordering-type,
api-type,format,tooling-lint,import-boundary,domain-layer-boundary,database-ownership-final,
database-permission,repository-guidance,secret-scan}.log. Same valid frozen-install evidence is
reused as scoped above. No fresh full verify, generic integration, browser or remote CI is claimed.

Initial failures were limited to fixture generic mock typing/unused arguments and the expected
unregistered persistence-asset gate. The precise gate registration initially used an empty-table
negative fixture without a persistence directory; using a different owned table exercises the
intended missing-availability ownership rejection. All final checks pass without bypasses. Final
nine-file scope/LF/privacy review passes; historical and unrelated files remain untouched.
Next bounded feature is current safety/availability composition through public owner providers;
conditional publication and final selection/commercial sources remain separate explicit prerequisites.
