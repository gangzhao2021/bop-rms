# WP-2215 - Native macOS Local Environment Compatibility

## Authority and readiness

- Baseline `main@ac08c14d076ca76b71f55571605b61ef3ed1efde`; branch `codex/wp-2215`.
- Composite Handoff 0.5.3 Sections 0-97, root AGENTS.md, accepted onboarding support for native
  Linux/macOS and WP-0006 local lifecycle/security contracts remain authoritative.
- Owner requested new-computer setup and explicitly accepted the recommendation to repair macOS
  startup compatibility. This authorizes this bounded brief, implementation and local verification.
  It supersedes WP-0006's Linux-only host restriction for macOS only. Windows still requires WSL2.
- Clean local baseline matches remote main. Existing pinned install, complete verification,
  PostgreSQL lifecycle and both demo browser gates passed on this Mac before implementation.
- DoR PASS. After local completion, the Owner asked to continue the recommended review/delivery
  step. This authorizes the WP-scoped commit, branch publication, PR, exact-head CI and reviewed
  squash integration followed by exact-main verification. Deployment and Provider changes remain
  excluded. GitHub Free governance is Process-enforced; self-review is not independent approval.

## Acceptance and evidence map

1. Native macOS and Linux/WSL2 validate; unsupported hosts, Windows-mounted checkouts/tools,
   wrong tool versions and non-Linux Docker engines still fail closed. Add platform regressions.
2. Supervisor and Worker identity checks work without `/proc` on macOS, reject invalid/unrelated
   PIDs and script/argument lookalikes, and retain Linux process inspection. Cover both branches
   with tests and actual host process evidence; do not log process arguments or environments.
3. Existing self-cleaning `pnpm environment:verify` proves startup, status, stop, localhost health,
   unchanged API `503/not_configured` readiness, secret protection, environment isolation and
   process/port/container/network/volume cleanup on macOS. Retain Linux checks without skipping
   environment-isolation assertions. Repeat the lifecycle check for reproducibility.
4. README and onboarding/tooling instructions describe native shells, fixed toolchain setup,
   first-checkout build and platform behavior accurately. No machine-specific paths or credentials.
5. Frozen install, complete `TMPDIR=/private/tmp CI=true pnpm verify`, PostgreSQL lifecycle,
   affected uncached checks and final scope/diff review pass. Linux execution evidence is only
   claimed if actually run; GitHub Actions remains a separate, untriggered delivery gate.

## Allowlist

- `tooling/environment/config.mjs`
- `tooling/environment/local.mjs`
- `tooling/environment/process.mjs`
- `tooling/environment/config.test.mjs`
- `tooling/environment/process.test.mjs`
- `tooling/environment/verify-local.mjs`
- `tooling/environment/README.md`
- `README.md`
- `docs/onboarding/developer-setup.md`
- `docs/spec/README.md`
- `docs/spec/work-packages/WP-2215.md`

## Verification and boundaries

Use existing commands: `pnpm install --frozen-lockfile`, `pnpm exec vitest run
 tooling/environment/config.test.mjs tooling/environment/process.test.mjs`, `pnpm verify`,
`pnpm environment:check`, `pnpm environment:verify`, `./tooling/postgres/verify-local.sh`,
`pnpm exec turbo run format:check lint typecheck test build --force`, and `git diff --check`.
Run from the repository root with pinned tools, Docker Linux containers and local synthetic secrets.

No dependency/lockfile, CI workflow, Domain, API/UI, database/migration, permission, runtime key,
Provider, production or business behavior changes. Existing security checks are not disabled.
Rollback restores the scoped code/documentation changes after stopping this WP's own local services;
normal stop preserves local PostgreSQL data. No destructive Git or unrelated Docker cleanup.

## Status

- Acceptance 1-2 PASS: 28/28 focused regressions and 374/374 final root tests. Tests cover
  accepted hosts, rejected hosts/paths/versions/engines, exact script/start matching, invalid PIDs,
  real macOS child processes with both Node invocation forms, paths containing spaces, environment
  key boundaries and error-output redaction. An ephemeral official Node 24.18.0 Linux container
  also passed real `/proc` process identity and environment checks for both invocation forms.
- Acceptance 3 PASS: `pnpm environment:check` accepted the pinned macOS toolchain; actual `pnpm dev`
  reported all five services healthy via `pnpm local:status`; `pnpm local:stop` exited cleanly and
  preserved the local volume. Self-cleaning `pnpm environment:verify` passed repeatedly, including
  after the final error-redaction change. The retained PostgreSQL lifecycle verification passed.
- Acceptance 4 PASS: native-shell onboarding, random local-only secret creation, initial build and
  platform/process behavior documented; repository-guidance validation passed.
- Acceptance 5 PASS: frozen install and complete `TMPDIR=/private/tmp CI=true pnpm verify` exited
  zero. Final root tests and environment lifecycle were rerun after the last error-redaction change.
  Forced package format/lint/typecheck/test/build passed 200/200 tasks with zero cache hits. Final
  scoped formatting, lint, guidance and whitespace checks passed. The exact 11-file allowlist is
  respected; no lockfile/dependency, generated, business, migration, permission or credential change.
- Resource review PASS: default and verification ports are free, both runtime-state files are gone,
  no running Docker container or verification-project container remains. The default local database
  volume is preserved; isolated verification resources were removed by their owning scripts.
- Local implementation is complete on `codex/wp-2215`; delivery is in progress.
- Early local failures were a lint-only empty test stub and the documentation validator requiring
  explicit `chmod 0600`; both were fixed without changing validators or disabling checks.
- Local scope/security self-review found no Blocker/High/Medium/Low findings. Full Linux lifecycle,
  exact-head/main Actions and integration evidence belong in the PR and remain pending until
  observed. Local Linux evidence above covers process inspection only. Existing Turbo output-path
  warnings are unchanged and do not indicate failed tasks.
- External runtime/key/Store/Provider evidence remains gated and unclaimed.
