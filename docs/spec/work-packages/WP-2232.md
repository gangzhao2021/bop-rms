# WP-2232 - Local Integration Acceptance

## Authority and readiness

On 2026-09-08 the Owner explicitly authorized local integration merges and joint acceptance on an
independent branch. Verified local commits were already authorized. Composite Handoff authority
and decision precedence remain as in the specification index. Root checkout was clean at a7f90a9.
Branch: codex/wp-2232-local-integration. No main merge, push, deployment or external mutation.

## Sources and scope

Integrate the following exact, individually verified local source tips, preserving their history:

- a7f90a9, codex/wp-2231-cart-item-writer: WP-2225, WP-2229, WP-2230 and WP-2231.
- 657cf90, codex/wp-2228-cart-decisions: WP-2224 and WP-2228 design records.
- a92dab0, codex/wp-2226-cart-presentation: Customer navigation and exact CAD presentation.
- 726e30f, codex/wp-2227-runtime-recovery: API route telemetry and bounded Realtime drain.

Source allowlist is the exact changed-path union of those four tips against cedc44c. Integration
edits are limited to docs/spec/README.md and this brief. Source runtime, tests and design contracts
remain byte-identical to their owning source tip. Only README overlaps between sources. No new
business behavior, dependency, migration, permission grant, credential or production configuration.

## Acceptance and verification

1. Preserve every source as an ancestor; retain source branches/worktrees and main. Resolve index
   conflicts by retaining source statuses and explicitly superseding historical separation claims.
2. Confirm the source-owned file union matches the corresponding source blobs, with only the two
   integration documentation paths excepted. Inspect diff, LF, secrets and generated/lockfile drift.
3. Use pinned Node 24.18.0 and pnpm 11.13.0; run frozen offline install and complete pnpm verify.
   This includes affected format, lint, types, unit, database integration, architecture, contracts,
   permissions, migrations, Screen registry and build checks.
4. Run pnpm test:integration, pnpm customer-demo-browser:acceptance, pnpm realtime:acceptance,
   pnpm core-telemetry:acceptance, pnpm request-correlation:acceptance and pnpm secret-scan:check.
   Record joint results and cleanup. Local synthetic checks do not establish Store UAT or deployment.

Rollback is switching back to the preserved source branch; no persisted history is rewritten.
External Handoff availability, Store/Provider evidence and remote CI remain gates; none is invented.
Cart creation/current binding, one-time credential recovery and HTTP activation remain unfinished
business work. This WP establishes local integration evidence, not whole-project completion.

## Results

Acceptance 1-4 PASS locally. The three incoming histories were merged sequentially; only the
specification index conflicted. Its source statuses were retained with an explicit superseding
integration entry. All 43 source-owned files match their exact source blobs. The total difference
from cedc44c is 45 files, including only two integration documentation paths. No runtime rewrite,
lockfile, dependency, migration, production configuration or generated drift was introduced.

Pinned frozen offline installation passed. Complete CI=true pnpm verify exited 0: ownership56/56,
Screen Registry210, Ordering228/228, API197/197, Customer PWA221/221, Cart PostgreSQL3/3 and all other
owning checks passed. All40 build tasks passed using existing valid Turbo cache entries. Dedicated
pnpm test:integration passed uncached, including migration/concurrency, parallel isolation, failure
injection, signal/timeout handling, redaction and owned cleanup.

Customer browser acceptance passed38/38 across desktop, mobile and production demo exclusion.
Realtime passed15/15; core telemetry passed Observability48/48 plus SDK smoke, API13/13 and
Worker5/5; request correlation passed HTTP11/11 and PostgreSQL1/1. Secret scan passed2/2.
Browser ports5174/4174 were released. Final documentation format, guidance, exact source/scope/LF
and diff review passed. No owning check was disabled or failed during this integration.

Local logs are /private/tmp/bop-wp2232-install.log, bop-wp2232-verify.log,
bop-wp2232-integration.log, bop-wp2232-browser.log, bop-wp2232-realtime.log,
bop-wp2232-telemetry.log and bop-wp2232-correlation.log in the same directory. Source branches and
worktrees remain available; main remains cedc44c. Fresh remote CI, real Store UAT, external Provider
acceptance, main merge, push and deployment were not performed. Source-local historical statements
about separate branches or missing merge authorization are superseded by this authorized integration.

Next business work remains Cart creation/current binding and the accepted credential recovery
protocol, followed by runtime activation under their own bounded WPs. Their absence is not hidden
by the passing integration checks and this record does not claim the entire project is complete.
