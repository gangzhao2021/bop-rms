# WP-2213 - Guest Session Legacy Rollout Inspection

## Authority and scope

- Baseline `main@ec6650ce239602566cf5abeaf12a6c923bc51ea1`; branch `codex/wp-2213`.
- Composite 0.5.3 Sections 0-97 and WP-1003/WP-2211 legacy fail-closed boundary.
- Owner authorizes this second bounded WP, local services, commit/push, reviewed merge and complete
  exact-head/main verification. Identity owns the inspection query and facts; DoR PASS.

## Acceptance

1. Add an opt-in Identity-owned inspector over the existing scoped transaction runner. At one
   validated UTC observation instant, return only `NoLegacyRows`, `InactiveLegacyRowsOnly` or
   `LiveLegacyRowsPresent`; no counts, identifiers, hashes, SQL, rows or causes cross the boundary.
2. A row is legacy unless its current operation ID has an exact same-scope, same-session,
   same-intent immutable result. Live uses the accepted half-open status/idle/absolute/closure
   rules. Query is read-only, parameterized and explicitly Brand/Store scoped; forced RLS remains.
3. Classification is evidence, never production readiness or permission to backfill/delete. No
   history is invented. Live legacy blocks lifecycle rollout for that exact scope; inactive legacy
   still needs separately authorized retention handling. Store inventory and old-writer shutdown
   evidence remain external and unavailable.
4. Unit and real pooled isolated-database tests cover empty, live, inactive boundary, complete
   journal, cross-scope denial, legacy lifecycle denial and sanitized failures. Add a concise
   rollout runbook defining per-scope execution, stop conditions and missing external evidence.

## Allowlist and verification

- `docs/spec/README.md`
- `docs/spec/work-packages/WP-2213.md`
- `docs/runbooks/guest-session-lifecycle-rollout.md`
- `packages/bop/identity/src/index.ts`
- `packages/bop/identity/src/infrastructure/persistence/database-access.manifest.ts`
- `packages/bop/identity/src/infrastructure/persistence/guest-session-entry-store.ts`
- `packages/bop/identity/src/tests/guest-session-entry-store.test.ts`
- `packages/database/test/customer-entry-persistence-acceptance.test.mjs`

No mutation/migration/dependency/API/UI/CLI/role/Provider/production change. Frozen install,
Identity/API/database tests and full `TMPDIR=/private/tmp CI=true pnpm verify`; secret/diff review;
full exact-head/main Actions. Restore Colima stopped. Rollback removes the opt-in inspector and
runbook; stored Session/history facts remain unchanged.

## Status

- Acceptance 1-4 and full local verification PASS. Identity 130/130, API 178/178, root 346/346,
  real pooled Session database acceptance 2/2 and all 40 package checks/builds passed.
- Unit coverage proves three bounded classifications, malformed/contradictory result rejection and
  pre-query input denial. Real SQL proves empty/complete history, live/inactive legacy, exact
  half-open time, cross-scope denial and legacy lifecycle fail-closed behavior.
- Initial test-only tuple typing and formatting failures were fixed without skipping or weakening
  checks. Existing unrelated Turbo output-path warnings remain. No migration/dependency changes.
- Final solo scope/security review: Blocker/High/Medium/Low 0/0/0/0. Eight allowlisted files;
  read-only parameterized owner query, no returned count/reference/hash and no external action.
- Exact-head/main Actions, merge and cleanup evidence belong in the PR. No real Store inventory,
  writer shutdown, inspection result, retention decision or production readiness is claimed.
