# WP-2321 - Dining Cart Command Read Scope

## Authority and readiness

Baseline52714606600452f971b2ad0a452de5a6d890a127; branch codex/wp-2321-dining-cart-command-read.
Composite Sections0-97, WP-1201/1205 and accepted WP-2228 govern. Continuous local implementation
and verified local commits are authorized. The three separately authorized AGENTS/skill edits are
preserved and excluded from this business commit. Apply their current evidence-reuse policy.

## Scope and acceptance

Before Dining item command composition, supply an Ordering-owned read adapter that constrains the
aggregate SQL by trusted Brand, Store, DiningSession, DineIn and Customer Qr/Web source in addition
to Cart reference. Never fetch another session's restricted item notes to decide authorization.
Current Identity/CSRF/Cart-purpose participation remains a required caller responsibility; this
adapter grants no membership. Return legacy/expired/terminal owner aggregates for original-operation
reconciliation; the existing command lifecycle check still prohibits new invalid writes.
Capture and validate trusted references before asynchronous work, revalidate returned aggregates,
and preserve bounded failures and existing Pickup query behavior. No migration, write, API or UI
activation, permission inference, attribution change, money, event, credential or external change.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2321.md
- packages/rms/ordering/src/infrastructure/persistence/cart-query-store.ts
- packages/rms/ordering/src/index.ts
- packages/rms/ordering/src/tests/cart-query-store.test.ts
- packages/database/test/cart-query-store-acceptance.test.mjs

## Verification plan

Fresh Ordering format/lint/typecheck/test/build, API dependent typecheck, and existing Cart PostgreSQL
configuration cover changed query behavior and all consumers. Fresh ownership, import/domain
boundaries, migration, repository guidance and secret checks cover persistence/security scope.
Run existing pinned frozen offline install to avoid relying on uncertain installation evidence.
Final exact-file diff/LF, bounded-data and isolated-resource review. No test scripts are changed.
The full shared Dining Cart browser journey is the next full-repository regression milestone:
pnpm verify and pnpm test:integration, without automatic cache bypass. A failed affected check or
concrete cross-module risk broadens coverage now. Full regression is not claimed for this adapter.
No PWA input changed since WP-2320; no fresh PWA behavior claim is required here.

## Observed results

PASS: additive scoped query captures trusted DiningSession and filters it together with Brand,
Store, Cart, DineIn and Qr/Web in the aggregate SQL. Faulty returned scope/channel is rejected.
No lifecycle filter hides history needed by original-operation reconciliation. The unfiltered
internal owner reader retains its exact prior behavior; neither reader grants application authority.

Fresh results on baseline5271460 plus the seven listed WP files: Ordering796/796 across39 files,
format/lint/typecheck/build and dependent API typecheck pass. Cart PostgreSQL9/9 across7 files
passes in68.55s, including exact session exclusion, foreign Brand/Store, Pos exclusion, shared
items and expired/legacy/Abandoned history, least-privilege read-only transactions and unchanged
Cart command/binding/lifecycle/Quote consumers. Fixtures are explicitly synthetic owner facts.

Fresh pinned frozen offline install passes for41 projects with unchanged lockfile. Ownership180,
import boundaries22, Domain boundaries57, migration103 and catalog101, guidance33 ADRs/5 skills,
secret2 and changed docs/integration formatting pass. Commands are the existing Ordering workspace
scripts, API typecheck, exec vitest run --config packages/database/vitest.cart-aggregate.config.ts,
and root database-ownership:check, import-boundary:check, domain-layer-boundary:check,
migration:check, repository-guidance:check, secret-scan:check. All use Node24.18.0/pnpm11.13.0 and
config.store-dir=/private/tmp/bop-rms-wp2288-dependency-cache/v11. Local logs are
/private/tmp/bop-wp2321-{install,unit,static,pg,gates}.log. No failing check was hidden or rerun.

Full pnpm verify/test:integration and PWA checks were not executed for this adapter; no fresh
full-repository/PWA result is claimed. The named full Cart journey milestone remains planned.
Security review finds no unresolved in-scope blocker: restricted data is session-scoped in SQL,
driver errors remain bounded, no credentials/private foreign tables or new writes are introduced.
Rollback is removal of the additive adapter before activation; no persisted state or migration
is changed. Dining mutation authority and API composition remain subsequent work.
