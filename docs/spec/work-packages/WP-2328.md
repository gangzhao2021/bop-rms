# WP-2328 - Current Authority Across Pickup Quote Awaits

Baseline dd24b85; branch codex/wp-2328-quote-current-authority. Composite Sections0-97,
WP-1203 immutable attachment and WP-2260 current Pickup Session/CSRF/binding authority govern.
Continuous local implementation and verified commits are authorized; preserve three guidance edits.

Inspection finds that the entry authorizes before binding/history but captures that result through
later asynchronous Cart/Pricing/write operations. Recheck the exact current Session with the same
captured credentials and a monotonic server clock before Pricing, after Pricing, before attachment
write and before returning a fresh or historical result. Preserve initial scoped binding checks,
original-operation replay, immutable history and existing business validation. A post-commit denial
does not roll back or delete the committed attachment; current-authorized original retry can recover.
These are current observations, not an atomic cross-Domain revocation lease. No financial producer,
DineIn quote, schema, HTTP DTO, credential storage or new business decision.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2328.md
- packages/rms/ordering/src/application/pickup-cart-quote-service.ts
- packages/rms/ordering/src/tests/cart-quote-attachment.test.ts

## Verification

Add behavior tests for revocation during history/Cart/Pricing/write waits, changed current Session
version, backwards time after awaits and original recovery without duplicate effects. Run Ordering
test/lint/typecheck/build, API typecheck, existing pricing-quote:acceptance (Pricing/API/actual PG),
format/import/Domain/guidance/security and final scope checks. Reuse WP-2325 unchanged owner schema,
UI and full-stage evidence, WP-2326 API coverage and same WP-2321 frozen installation with unchanged
lock/manifests/pins. No forced integration or full verify per bounded WP; next full milestone is new
cross-domain implementation or release/merge. Broaden if persistence/contract changes become needed.
No external facts/actions, remote Git or deployment. Rollback reverts only the application guard.

## Results

PASS: current authority is checked before/after Pricing, immediately before attachment persistence,
and before returning fresh or historical results. The complete immutable Session snapshot must remain
identical and the clock must not regress, including across authorization awaits. Expiry during the
identity query is rejected. Credentials remain confined to Identity authorization. A typed guard
failure is preserved through the generic attachment service's dependency-error boundary.

Seven new behavior cases cover revocation during history/Cart/Pricing, current version drift,
backwards time, historical result disclosure and post-write denial with original retry recovery.
The first Ordering suite had835 passing and1 failing fixture case: Vitest's original spy supplied no
getMockImplementation. Capturing the original fixture persistence method before spying fixes that
case; final affected cart-quote-attachment72/72 passes. Other39 files remain valid from the same run,
covering836 total tests across40 files with final affected evidence; no second full unit run is claimed.
Ordering typecheck/lint/build and API typecheck pass. pricing-quote:acceptance passes Pricing248/248,
API Quote9/9 and actual PostgreSQL5/5. Static format/import22/Domain57/guidance/security2 and final diff
checks pass. Logs under /private/tmp/bop-wp2328-* identify each command: ordering,
quote-unit-final,type,lint,build,api-type,quote-pg,format,import,domain,guidance,secret.

Reused evidence: WP-2325 at50b877e for unchanged UI/schema/owner persistence and full-stage components;
WP-2326 atffed14e for unchanged API beyond fresh type and Quote tests; WP-2321 same frozen installation
as documented by WP-2325. Source, manifests, lock, toolchain and installation remain unchanged outside
these four files. No fresh full verify, generic integration, API full suite or PWA build is claimed.
Initial two-call-only mock clock/call-count expectations were updated to represent the added current
observations, preserving original Audit/request time assertions. No history is deleted after denial.
Current observation is not an atomic revocation lease and this WP does not claim one. Quote/Cart
expiry while Pricing waits remains a separate freshness review, not a financial-source assertion.
