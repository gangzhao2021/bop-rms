# WP-2269 - Dining Current Cart Participation Query

## Authority and readiness

Owner's accepted WP-2228 decision requires a shared Dining Session Cart with Participant-owned
edits. Successive bounded implementation and verified local commits remain authorized. Baseline
c2fd33a8, branch codex/wp-2269-dining-participation-query. Composite Sections0–97 and existing
Dining Session/Participant/Table contracts (WP-1006, WP-1007 and WP-2112) retain Dining ownership. Ordering has no authority to
infer current membership from a Cart/Table reference or to inspect Dining private state.

## Scope and ownership

Add a Dining-owned internal public Query for purpose Cart, using an explicit exact Brand/Store
scope and Dining Session/Participant locators supplied by a currently authorized caller. The Query
is factual evidence, not authentication, an editing permission or a transferable access grant.
Identity remains responsible for proving the caller's current Dining-bound Participant; Ordering
will separately enforce Cart scope/version and item attribution. No public HTTP route is added.

A required owner repository reads Session, Participant and current Table assignment as one coherent
current snapshot. Validate/copy closed request and result before later callbacks/awaits. Server
clock bounds the snapshot observation; no client-supplied time, invented freshness duration or
future lease. Return a frozen versioned scalar fact only when the exact scoped Session is Active,
Participant is Active, Table assignment is eligible/current and all IDs/versions/times agree.
Missing, inactive or cross-scope evidence returns null. Malformed input fails before I/O; malformed
or failed dependencies use the existing redacted Dining dependency error. No host override or
shared-Guest ownership rewrite. Every query performs a fresh read; no cache, writes or renewal.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2269.md
- docs/spec/design/business-scenario-coverage.md
- packages/rms/dining/src/application/dining-cart-participation-query.ts
- packages/rms/dining/src/tests/dining-cart-participation-query.test.ts
- packages/rms/dining/src/index.ts

## Acceptance and verification

1. Dining unit tests prove exact purpose/scope/reference input, one coherent read, immutable public
   result, current states/assignment, cross-scope and association denial, start/join/observation
   chronology, clock drift, malformed/getter/extra-field redaction and copies across callbacks.
   Null/unavailable never grants authority. Concurrent queries retain their own request evidence.
2. Pinned frozen install; affected Dining format/lint/typecheck/test/build; full pnpm verify;
   forced pnpm test:integration --force; Secret, exact6-file scope/LF, final diff and owned cleanup.
   No unrun check is a pass.

## Boundaries

No dependency/Module owner change, migration, database adapter, cross-domain private access,
Identity credential processing, Audit/Event writes, UI, API activation, production change, main
merge, push, deployment or external service. No unrestricted IDs, credentials or participant data
in logs/URLs/analytics. Test references are synthetic and query results are internal scoped facts.
A later owning Dining persistence WP must supply the coherent repository; shared Cart creation,
current caller authorization/composition and closing/write race policy remain separate work.
Uncommitted changes may be discarded; accepted work uses forward fixes. This Query does not
complete shared DineIn or infer real Store/production readiness.

## Verification record

Acceptance1 PASS: Dining97/97 includes39 new query cases. One coherent owner read receives copied
scope and request locators with server time. Results are frozen minimal version facts, not Guest or
Host authority. Every call rereads; missing/left/Closing/Closed/Cancelled, cross-scope references,
wrong assignment and invalid chronology cannot return active evidence. Getter/shape/private error
cases are redacted. Request/scope mutation and mutation during the next clock callback cannot
change the result; concurrent participant queries retain their own locators.

Acceptance2 PASS: frozen install, affected format/lint/typecheck/test, full pnpm verify and all40
build tasks pass. Forced
uncached integration passes with cache bypass61b28e25614c78d2,0cached,2m0.411s, covering91 migrations,
concurrency, redaction, parallel isolation, fault/signal/timeout handling and owned cleanup. Secret2/2
passes. The first full run found a test mock's unused parameter; its type is now declared on the
mock instead, and the final full verification passes. No failed/partial run is claimed as a pass.
Final exact6-file scope, LF, diff, guidance and Secret checks pass; no checks were skipped.
The existing Dining build output warning does not change its successful exit. No generated,
dependency, migration or unrelated changes are included.

Security/privacy review: the query accepts no credentials or Actor override and exposes no HTTP
route. Callers must use current Identity-bound locators; these internal facts are not authorization.
Dining owns all Session/Participant/Table assignment facts; no foreign table is read. No Audit,
Event, cache, renewal, ID generation or persistence side effect occurs. Results describe their
explicit observation time, not a future validity lease or cross-domain closing/write fence.
No unaccepted Blocker/High finding remains in this bounded Query. A current-state repository,
caller integration and closing/write coordination remain separate prerequisites.

Logs under /private/tmp: bop-wp2269-install.log, bop-wp2269-dining.log,
bop-wp2269-types.log, bop-wp2269-verify.log, bop-wp2269-integration.log,
bop-wp2269-secret.log. No new server or database resource is created by this Query.
