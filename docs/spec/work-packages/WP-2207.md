# WP-2207 - API Runtime Dependency Wiring and HTTP Regression

## Authority and readiness

- Baseline: `main@ffbb5417ea95413dcc81607690a0367aa1d74df7`.
- Branch: `codex/wp-2207`.
- Authority: Specification Index composite `0.5.3`, Sections 0-97; accepted WP-0043 runtime
  lifecycle, WP-1004/1700 Customer Entry, WP-1703 Quote and existing Merchant BFF contracts.
- Owner authorized the recommended software WP, verification and delivery in this task.
- DoR: `PASS`. The clean baseline has existing typed handlers and an Express composition root,
  but `ApiServerRuntimeOptions` omits Customer Entry, Quote and Merchant BFF dependencies.
  No external source or Provider credential is needed to forward existing typed dependencies.

## Acceptance

1. The runtime factory accepts and forwards optional `customerEntry`, `customerQuote` and
   `merchantBff` dependencies without constructing adapters or changing handler contracts.
2. Real loopback HTTP tests demonstrate requests reach each injected handler/service with accepted
   synthetic input and retain the owning handler's response behavior.
3. Origin/Fetch Metadata/Host denials occur before injected business ports; no authentication,
   authorization, Cookie, CSRF, validation or no-store control is weakened.
4. Omitted dependencies preserve Customer 503 and Merchant 404 behavior. Separate runtimes do not
   share injected dependencies; existing readiness, shutdown and telemetry behavior is retained.
5. Test servers use ephemeral loopback ports and are shut down. Synthetic credential/body values
   do not appear in captured structured logs. No external requests, real identities or data exist.

## Exact allowlist

- `apps/api/src/server.ts`
- `apps/api/src/server.test.ts`
- `docs/spec/work-packages/WP-2207.md`
- `docs/spec/README.md`

No Domain, migration, persistence, Screen, fixture module, dependency, lockfile, CI, environment
activation, Provider or deployment change is allowed. The CLI still starts unconfigured by default.
This WP does not claim the entire Customer or Merchant journey is connected.

## Verification and delivery

- Frozen install using pinned Node/pnpm.
- Existing API test, format, lint, typecheck and build scripts; real HTTP regressions live in the
  existing `server.test.ts` suite and are included by root verification.
- Complete `TMPDIR=/private/tmp CI=true pnpm verify`, final scope/security/diff review,
  exact-head GitHub Actions, Section 91 solo review, authorized squash merge and exact-main CI.
- Acceptance 1-2: typed compilation and injected-port HTTP assertions. Acceptance 3-4: denial,
  default and isolation regressions plus retained lifecycle tests. Acceptance 5: cleanup/log checks.
- Local Docker/Colima may be started when needed and restored afterward. External evidence remains
  unclaimed; missing IDR-0037 does not prevent provider-neutral software verification.

## Risks and rollback

Forward only supplied dependencies; never introduce permissive defaults or test flags in production.
Existing handlers continue to own trust checks. Revert this bounded integration to remove the new
factory options and regressions; no persistent or external state is created. Stop on allowlist
expansion or any required change to an accepted business/security contract.

## Status

- Specification: approved for bounded execution.
- Implementation/local verification: `PASS`.
  - Frozen install passed with no lockfile change. API tests passed `143/143`, including real
    HTTP dispatch/denial/isolation and retained runtime lifecycle coverage.
  - Complete `TMPDIR=/private/tmp CI=true pnpm verify` exited 0, including architecture,
    contracts, permissions, format/lint/typecheck, isolated PostgreSQL and all 40 package builds.
  - Final scope/security review found only the four allowlisted files, no private Domain access,
    real credentials, PII, production configuration or generated drift. Synthetic body/Cookie/CSRF
    values are excluded from captured logs; test servers close and no test containers remain.
  - Two initial test-harness issues were corrected: Quote errors use `error.code`; exact Merchant
    Host tests use native HTTP requests like the existing BFF tests. No handler control changed.
- Delivery evidence: the WP-2207 pull request records the exact candidate, CI run/job, Section 91
  review, merge SHA and exact-main outcome. A local pass alone does not establish integration.
- Production/Provider action: excluded.
