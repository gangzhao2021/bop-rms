# WP-2295 - Durable Dining Guest Binding Preparation

## Authority and scope

Baseline14cbb00; branch codex/wp-2295-dining-binding-store. Continuous bounded local
implementation and verified commits remain authorized. Composite Sections0-97, WP-2228
safe recovery, WP-2294 Dining preparation and WP-2293 immutable writer commands govern.

Persist the independent DiningSessionBinding protocol in Identity. Existing Pickup tables,
formats and services are unchanged. Admission remains an opaque public owner reference;
there is no foreign key or private query into Dining.

## Acceptance

Migration0200_014 adds guest_dining_binding_preparation: immutable scoped revision chain,
closed Dining record/nested snapshot shapes, exact purpose/admission/Guest/candidate scope,
current predecessor FK, candidate uniqueness, forced Brand/Store RLS and PUBLIC denial.
No update/delete/truncate; transitions preserve immutable facts and original acknowledgement.
Catalog becomes100 migrations in23 namespaces; applied migration bytes are preserved.

The writer captures complete scope/commands/proofs/owner evidence before asynchronous work.
Prepare/acknowledge retain current predecessor and exact immutable history. Activate uses the
WP-2294 plan under current locks, then atomically revokes the predecessor, installs the exact
DiningBound candidate with public Table and Dining Session/Participant, appends Session history,
preparation and public Audit. Audit failure/read-only/unknown acknowledgement preserve atomicity.
Complete authenticates the exact active candidate, including after preparation expiry.

Actual isolated PostgreSQL acceptance covers prepare/retry, proof/time/scope/current denials,
concurrent activation, rollback/lost acknowledgement, immutable history, scoped FK/RLS and
stable completion. Owner admission evidence is explicit synthetic fixture evidence in this
package, not actual Dining producer/production readiness. No Host or Ordering authority.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2295.md
- migrations/0200-bop-identity-tenancy/0200_014_create_guest_dining_binding_preparation.sql
- packages/bop/identity/src/infrastructure/persistence/guest-dining-binding-store.ts
- packages/bop/identity/src/tests/guest-dining-binding-store.test.ts
- packages/bop/identity/src/index.ts
- packages/bop/identity/src/module.manifest.ts
- packages/bop/identity/src/infrastructure/persistence/database-access.manifest.ts
- packages/database/src/catalog.test.ts
- packages/database/test/workforce-identity-security-acceptance.test.mjs
- packages/database/test/identity-session-store-acceptance.test.mjs
- packages/database/test/isolated-database-acceptance.mjs
- packages/database/test/guest-dining-binding-store-acceptance.test.mjs
- packages/database/vitest.identity-session-store.config.ts
- tooling/database-ownership/validate.mjs
- tooling/database-ownership/validate.test.mjs

## Verification and boundaries

Frozen install; affected lint/type/unit, ownership/permission/migration/catalog, actual new
PostgreSQL acceptance; full pnpm verify and sequential forced integration. Final guidance,
Secret/exact17-file LF/diff/security/cleanup checks. No unrun check is a pass.
No raw credential generation/transport, application service, actual Dining producer, HTTP/UI,
new TTL/provider facts, dependency, main merge, push or deploy. Forward-fix rollback only.
Implementation and required verification passed locally.

The existing Identity schema inventory acceptance must list the new owner table. Its exact
expected inventory is added to this allowlist; least-privilege checks remain unchanged.

The workforce Identity acceptance also enumerates the same schema and function count;
its inventory is synchronized without changing PUBLIC-denial or sensitive-column checks.

## Observed evidence

- Frozen offline install, Identity lint/typecheck and431/431 tests across13 files passed,
  including29 new store boundary cases. A typed owner fixture now uses the public evidence
  parser before a mutable test copy; production checks were not weakened.
- Actual new PostgreSQL acceptance passed. Full Identity persistence config passed6/6 across
  five files; workforce Identity security passed separately. New tests cover wrong Guest,
  admission/operation/public Table/Session/Participant, stale/future/expired owner evidence,
  concurrent activation, Audit/read-only rollback and lost acknowledgement completion.
- Candidate snapshots round-trip exactly as DiningBound with their public Table and stable
  Dining references. Invalid in-flight proof/evidence/CSRF mutations deny; constructor scope
  mutation cannot redirect completion. Final new preparation/Audit counts are10/10.
- Actual SQL rejects foreign predecessor scope, foreign-context inserts, wrong purpose,
  raw credential extras and update/delete/truncate. No PUBLIC privileges are introduced.
- Initial full checks identified fixed Identity/workforce schema inventories requiring the
  new table, RLS count7->8 and function count4->5. Only those exact inventories were updated;
  PUBLIC-zero, denied-role, sensitive-column and existing trigger checks remain intact.
- Final full pnpm verify passed, including ownership162/162, migration/catalog/permission,
  format/lint/typecheck, all suites, Dining785/785 and PostgreSQL9/9, and40/40 builds.
- Forced integrationbb82d21d5394418d passed, zero cached tasks,1m58.846s:100 migrations,
  23 namespaces, isolation, injected failure/signal/timeout, redaction and owned cleanup.
- Admission evidence in the new store test is explicit synthetic owner evidence. Actual
  Dining producer composition, credential coordinator and browser transport remain separate.
- Final guidance/Secret2/2 and exact17-file LF/diff review passed; isolated containers, volumes
  and roots are zero. Main remains cedc44c9b6a6a1b389f77342078cc6f2d540dabe.
