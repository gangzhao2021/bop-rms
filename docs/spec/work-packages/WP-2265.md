# WP-2265 - Authorized Pickup Cart Item Composition

## Authority and readiness

Owner authorizes successive bounded implementation and verified local commits. Baseline77c0a70,
branch codex/wp-2265-pickup-cart-item-composition. Composite Sections0–97, existing Cart item
commands/persistence and WP-2231/2248/2251/2260/2264 retain current authorization, exact Pickup
ownership, server time, expected version, immutable operation/Audit and original-key recovery.

## Scope and ownership

Ordering supplies an authorized Add/Update entry over existing CartItemCommandService rules.
Capture closed immutable credentials, Cart/item/sellable identity, quantity, selected options and
customer note before awaits. Authorize current Session/CSRF and exact Pickup binding, reauthorize
before delegation, reject Guest/scope/binding drift and use only server time. Existing history is
resolved after authorization and before revalidating Catalog or allocating another item. Current
Catalog validation is a mandatory public owner port; never infer eligibility from displayed menu
facts or assume conditional rules/option prices. No DineIn/shared participant authorization added.

API supplies owner-only composition and post-command current Cart reads. Applied/AlreadyApplied
receipts require matching Cart/item/version; failed or stale post-write reads stay unavailable.
Version conflict may read an authorized current version; bounded selection issue codes contain no
notes, credentials or private facts. Optional local runtime wiring requires Cart reads and explicit
Catalog/reference/Audit/write providers. Preserve configured removal and read handlers. Missing
providers remain unavailable, and production/staging runtime use remains prohibited.

No UI route, body, success/error vocabulary or money policy changes. Existing Cart/configuration
unknown-intent and Session-epoch protection remain. This enables existing Add/Update contracts;
authoritative current Catalog snapshot producers, conditional-option publication, option pricing,
real Store sources, shared DineIn recovery and Payment/Provider evidence remain gated separately.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2265.md
- docs/spec/design/business-scenario-coverage.md
- packages/rms/ordering/src/application/pickup-cart-item-service.ts
- packages/rms/ordering/src/index.ts
- packages/rms/ordering/src/tests/pickup-cart-item.test.ts
- apps/api/src/customer-cart-item-composition.ts
- apps/api/src/customer-cart-item-composition.test.ts
- apps/api/src/local-customer-runtime.ts
- apps/api/src/local-customer-runtime.test.ts
- packages/database/test/cart-binding-store-acceptance.test.mjs

## Acceptance and verification

1. Ordering tests prove closed input ownership before awaits, current Session/CSRF/scope/binding,
   identity/clock denial, expected-version/lifecycle, authoritative Catalog rejection, add/update,
   original replay, no repeat allocation/validation and isolated concurrent request evidence.
2. API tests prove explicit composition, exact current readback, bounded failures and optional
   runtime prerequisites/default denial. Actual PostgreSQL/HTTP acceptance uses real Identity,
   binding, Cart operation/writer and Audit, with explicitly synthetic Catalog/display sources.
   Prove lost write acknowledgement and failed readback recovery, concurrent original retries,
   changed-key intent conflict, stale version, current CSRF/revocation denial and one set of facts.
3. Pinned frozen install; affected Ordering/API/database format/lint/typecheck/test/build;
   ordering-cart:acceptance; full pnpm verify; forced pnpm test:integration --force; Secret/privacy,
   exact11-file scope/LF, final diff and owned resource cleanup pass. No unrun check is a pass.

## Privacy and external boundary

Customer notes and options stay in approved private Cart/operation persistence and authorized
Cart responses; never in Audit summaries, diagnostics, URLs or telemetry. New fixtures contain
no raw notes or sensitive option labels.
Use null notes in persistence proof. Audit remains minimal System/Restricted and append-only.
No migration, dependency, live Store/Catalog fact invention, production grants, external service,
main merge, push or deployment. Uncommitted scope can be discarded; accepted changes use a
forward fix, never deletion or rewriting of transaction history. Whole project readiness unclaimed.

## Verification record

Acceptance1 PASS: Ordering518/518 includes28 new authorized item cases. Add and Update retain
original receipts after Cart advancement, authorize every retry, and perform only one allocation
and one Catalog validation per original Add. Changed intent, missing/foreign binding, Session/CSRF,
Guest/QR/scope drift, backwards time, malformed dependencies, stale version, elapsed lifecycle and
Catalog rejection deny writes. Closed input/accessor and selected-option mutation tests verify
ownership before awaits. Concurrent authorized/denied requests retain isolated authorization.

Acceptance2 PASS: API360/360 includes31 item bridge cases and3 runtime prerequisites/default/guard
cases. Exact one-version receipts are closed and copied before authorized readback. An advanced
view may contain subsequent edits; missing/stale/inconsistent views and post-write permission loss
remain unavailable. Failure mapping is bounded, input/receipt mutation is isolated, and configured
read/removal functions are preserved. Actual Cart PostgreSQL6/6 passes with real Identity, binding,
Cart writer/history and Audit. Add COMMIT acknowledgement loss returns503; three concurrent
original retries return200 with one item/operation/Audit and one allocation/Catalog validation.
Update readback failure returns503, then the original key recovers current version3. Changed Add
and Update intents return409. Wrong CSRF401, foreign Cart404, stale version409 and actual Session
revocation401 cause no new item facts. Catalog acceptance/display are explicitly synthetic;
private credentials, Guest and Brand/Store identifiers do not appear in successful HTTP output.

Acceptance3 PASS: full CI=true pnpm verify completed with40 successful builds. Pinned frozen install passes; forced integration
passes with cache bypass8cdaa11a0205b065,0cached,2m0.128s, covering91 migrations, optimistic
concurrency, redaction, parallel isolation, fault/signal/timeout injection and owned cleanup.
Secret2/2 and exact11-file scope/LF checks pass. Initial full verification identified two new-test
lint violations (sparse-array syntax and non-null assertion); both were corrected without removing
behavior coverage, and the final full verification passes. No partial run is claimed as a pass.

Security/privacy review: public HTTP credentials pass only to the current Identity authorization;
Ordering receives copied scoped intent and explicit Catalog evidence. Approved private item/operation
persistence remains unchanged. Audit descriptors contain minimal action/scope/operation metadata,
never customer notes or selected-option labels. No new log, URL, analytics, Event, grant or migration
path was introduced. Local fixtures use generated credentials and null notes. No unaccepted
Blocker/High finding remains within this bounded composition; real Catalog eligibility, commercial
pricing and production readiness still require their own external evidence and owning work.

Logs under /private/tmp: bop-wp2265-install.log, bop-wp2265-ordering.log, bop-wp2265-api.log,
bop-wp2265-cart.log, bop-wp2265-api-types.log, bop-wp2265-owner-types.log,
bop-wp2265-verify.log, bop-wp2265-integration.log and bop-wp2265-secret.log.

Final closeout: all acceptance criteria pass locally; no required check skipped. Exact11-file
scope/LF and final diff are reviewed. HTTP servers, transaction clients and owned isolated database
resources are closed by the passing acceptance harness. No UI/browser behavior changed, so no
new browser scenario was required. Existing typecheck-only build no-output warnings do not change
the successful40-task exit. No migration, dependency, production change, main merge, push,
deployment or external action. Next bounded work: prevent stale Session generations in the
foreground Pickup creation coordinator before connecting it to default page flows.
