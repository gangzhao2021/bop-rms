# WP-2228 - Accepted Cart Sharing and Recovery Decisions

## Authority and readiness

On 2026-09-08 the Owner answered `1.允许 2.采用 3.采用` to the three explicit
questions in this task: local commits after verification; a shared Dining Session Cart with
Participant-owned item edits; and a one-time safe credential recovery mechanism preserving the
basket. This is product decision evidence, not acceptance of unspecified credential mechanics.
Composite Handoff 0.5.3 Sections 0-91 plus accepted Sections 92-97 remain authoritative.
WP-1003 and WP-1201 preserve Session rotation and immutable item attribution.

Baseline is WP-2224 local commit `9715bf5`, branch `codex/wp-2228-cart-decisions`.
WP-2225 `d425bc9`, WP-2226 `a92dab0` and WP-2227 `726e30f` are separately committed,
verified work packages; they have not been integrated into this branch or main. Their earlier
uncommitted-status statements describe their pre-authorization snapshots. No remote CI is claimed.

## Exact allowlist

- `docs/spec/work-packages/WP-2228.md`
- `docs/spec/README.md`
- `docs/spec/design/README.md`
- `docs/spec/design/customer-order-payment-handoffs.md`

## Acceptance and verification

1. Record the two accepted product choices and explicitly supersede the old fresh-Entry preference.
2. Preserve Domain ownership, revoked-credential denial, immutable attribution and source gates.
3. Enumerate concrete implementation and failure cases without claiming unexecuted tests passed.
4. Run pinned frozen offline install, `pnpm format:check`, `pnpm repository-guidance:check`,
   `pnpm screen-registry:check`, `pnpm secret-scan:check`, and scope/whitespace review.

This package is documentation only. No runtime, migration, API or Screen changes; application tests,
browser acceptance and full `pnpm verify` are not required and are not claimed. Local commit is
explicitly authorized. Push, merge, deployment and external mutations remain outside authorization.
Rollback removes these documentation changes without modifying runtime or persisted facts.

## Implementation handoff

Ordering owns shared Cart selection, revision and item attribution. Identity owns recovery proof,
credential lifecycle and public activation receipts. API coordinates public ports and transports
credentials; it acquires no Domain ownership. Implement the owner-local producer contracts before
HTTP activation, then scoped persistence and end-to-end synthetic acceptance in separate bounded WPs.
The detailed mechanism must resolve repeat loss of the recovery response, finite expiry, consumption
atomicity and preservation of the Pickup creator before a runtime activation can be considered ready.
These are engineering obligations under the accepted continuity goal, not another request to choose
between shared and individual carts or accept basket loss.

## Verification results

Acceptance 1-4 PASS for documentation. Frozen offline install passed without lockfile changes;
root format and all 40 package format tasks passed (package tasks cached); repository guidance
validated 33 ADRs and five skills; Screen Registry validated 210 records; secret scan passed 2/2.
Final four-file scope, LF, whitespace and supplemental secret-pattern review pass. No runtime
acceptance, browser test, full verification or fresh remote CI was run for this documentation WP.

Security review preserves revoked-session denial, exact scope, one-time consumption and immutable
item attribution. Recovery protocol completeness remains an implementation prerequisite, explicitly
including loss of the recovery response itself; this document does not claim runtime readiness.
