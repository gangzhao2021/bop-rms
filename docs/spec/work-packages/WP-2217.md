# WP-2217 - Canonical Temporary Paths in Architecture Acceptance

## Authority and readiness

- Composite 0.5.3 Sections 0-97, accepted WP-0011 generator and Section 93 / WP-0014 Domain
  architecture contracts, plus WP-2215 native macOS support. Existing path-safety decisions remain.
- Exact verified baseline `db8c229cdb7c93a0c716dd8ed1101d31a1ede57b`, branch `codex/wp-2217`.
- WP-2216 exact-head run34188210162 and exact-main run34190154186 passed all17 steps; PR191
  closeout is posted. Local/fresh origin main matched and were clean before branch creation.
- Owner's nine-hour authorization ends 2026-09-08 13:01:54 UTC and permits scoped implementation,
  verification, commit/push/PR, reviewed squash merge and exact-main CI. No branch deletion,
  production, Provider, credential or resource purchase action.
- DoR PASS: three failures were reproduced in WP-2216's default macOS temporary directory.
  Existing tools canonicalize roots, but two test fixtures retained the symlink spelling returned
  by mkdtemp. No external evidence is required for synthetic filesystem fixtures.

## Scope and acceptance

Canonicalize only the two architecture-test fixture roots. The generator already returns a real
canonical target, and the scanner already calls its readSource port with canonical absolute paths.
The tests must compare and inject failures against those accepted identities on both Linux and
macOS, including when the system temporary directory is reached through a symlink.

1. The existing two generator success cases and unreadable-source denial case pass using default
   macOS TMPDIR, without an environment override or weakened assertions.
2. Add portable explicit root-alias coverage: the generator returns the canonical target and writes
   its expected skeleton; scanner unreadable-source injection returns exit2 and the exact bounded
   UNREADABLE_DOMAIN_SOURCE diagnostic through both direct and aliased roots.
3. Preserve existing rejection of symlinked layer paths, escaping/colliding paths, unsafe dependencies,
   deterministic results and cleanup. Only owned temporary roots are removed.
4. Frozen install, focused checks and default-environment root suite pass. Complete verify uses
   the established canonical database temporary root; exact-head CI must prove retained Linux
   checks, then exact-main CI must pass before the next WP.

## Exact allowlist

- `tooling/module-generator/generate.test.mjs`
- `tooling/domain-layer-boundary/validate.test.mjs`
- `docs/spec/work-packages/WP-2217.md`
- `docs/spec/README.md`

## Verification and boundaries

Use pinned tools and existing commands from the root: `pnpm install --frozen-lockfile`;
`pnpm module-generator:check`; `pnpm domain-layer-boundary:check`; `pnpm exec vitest run`;
`TMPDIR=/private/tmp CI=true pnpm verify`; final `git diff --check`, format/lint and scope review.
No affected runtime/UI/database behavior or new integration command. Full verify retains existing
integration/architecture/contract/migration/security/build checks; exact-head CI adds existing
Linux startup/browser/Eventing/isolated-cleanup coverage. Report failures or skips truthfully.

No generator/scanner production source, public contract, dependency, lockfile, policy, environment,
CI, business code, migration or external evidence changes. No relaxation of symlink rejection.
Rollback is tests/docs only. This does not grant Windows-native support; Windows remains WSL2.

## Status

Implementation and local verification complete; exact-head/main CI pending. No independent review
or whole-project completion claim.

- AC 1-3 PASS: default macOS temporary directory, generator 17/17, Domain 57/57 and default root
  376/376 passed, including explicit alias regressions and retained unsafe-path denial/cleanup.
- AC 4 local PASS: frozen install, full `TMPDIR=/private/tmp CI=true pnpm verify` and final
  format/lint/diff review passed. Existing full test/build/architecture/contract/migration checks ran.
- Four-file scope/security self-review found no Blocker/High/Medium/Low findings. No production
  source, generated files, lockfile, secrets/PII, policies or external services changed.

The default-TMPDIR complete verify attempt passed architecture/root checks but failed at database
isolation with ISOLATED_DB_LEASE_FAILED: the separate lease allocator rejects an aliased parent.
This database source contract is outside the allowlist and is not changed or bypassed. The
verification plan is corrected to retain the established canonical TMPDIR for database checks;
default-TMPDIR root acceptance remains mandatory and passed 376/376. Database default-path
compatibility needs a separate scoped WP after this delivery; this WP does not claim to solve it.
