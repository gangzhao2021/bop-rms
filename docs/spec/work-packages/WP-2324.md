# WP-2324 - Dining Cart Item API Composition

## Authority and scope

Baseline2ebc8710f49c592fc52978d26bc39d7a064a12e3; branch codex/wp-2324-dining-cart-item-api.
Composite Sections0-97, WP-1201/1205, CUST-CART Section88 and accepted WP-2228 govern. Continuous
local implementation and verified local commits are authorized; preserve separate rule documents.
Opt in Dining item Add/Update/Remove using WP-2322 authority, WP-2321 session-scoped aggregate
reader, WP-2323 Cart/Guest-scoped history and the existing atomic writer. Reuse existing transport
bridges and safe Dining views; capture removal input before awaits and reject wrong-channel views.
The optional configuration inherits local runtime fixed scope/current Identity without opening a
new route or choosing authority from client IDs. Default without item configuration stays unavailable.
No UI activation, migration, new Domain rule, price, Provider, atomic Closing lease or offline replay.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2324.md
- apps/api/src/customer-dining-cart-composition.ts
- apps/api/src/customer-dining-cart-composition.test.ts
- packages/database/test/dining-cart-selection-store-acceptance.test.mjs

## Acceptance and verification plan

Test transport mapping, default disablement, safe post-effect views, wrong-channel suppression,
current CSRF/own-participant authority, version conflict, immutable removal input and recovery.
Extend existing actual API/Ordering PostgreSQL fixture with explicit synthetic public providers;
prove two-participant API edits and minimized notes with real Cart/history/Audit writes. Fresh API
lint/typecheck/test/build, changed format, Cart PostgreSQL configuration, import/guidance/secret
checks; affected tests after final fixes only. No Ordering source changes; reuse WP-2323 evidence
only for those unchanged modules, not for the new composition. Frozen install reuse requires the
same verified installation/pins/manifests/lock. Full pnpm verify plus pnpm test:integration remains
the named complete Cart browser journey milestone, without automatic cache bypass. Broaden now
for concrete uncovered cross-module risk. Final exact diff/LF/security and owned resource cleanup.

## Observed results

PASS: optional item configuration composes actual scoped aggregate/history readers and atomic writer
with current Identity/CSRF/Dining authority. Existing create/read and default unavailable item behavior
remain intact. Reused item/removal transport bridges publish current safe Dining views; the Dining
wrapper rejects wrong-channel views and captures removal identifiers before awaiting effects.

Fresh API563/563 across32 files passes (+12). Typecheck found unbranded references in the new synthetic
receipt fixture; parsing them with the existing reference parser corrected that test-only issue.
Final affected12/12 passes (40 other cases explicitly unselected in that targeted file run); their
unchanged behavior retains the successful full563 evidence, not a second full run. Final API
typecheck/lint/build, changed formatting, import22, guidance33 ADRs/5 skills and secret2 pass.
Cart PostgreSQL9/9 across7 files passes in68.77s, including the actual optional API composition.
Two participants add to one Cart; the owner's note is visible only to that participant, foreign
notes are null with ownership warning, foreign removal is denied, and own update/removal succeed.
An update commits while Store-profile reading fails; its original retry returns the current view
without another Audit row. The scenario produces exactly4 item operations and5 Audits including
initial Cart creation, with no duplicate recovery effects. Restricted identifiers/credentials and
foreign notes are absent from the returned view. Public Identity/Dining/Catalog/Store providers
are explicit synthetic fixtures, while API/Ordering/Cart/history/Audit composition is real.

Evidence commands: API test/typecheck/lint/build; targeted test
src/customer-dining-cart-composition.test.ts -t "opt-in Dining Cart item API"; exec vitest run
--config packages/database/vitest.cart-aggregate.config.ts; changed prettier --check; root
import-boundary:check, repository-guidance:check, secret-scan:check. Logs are
/private/tmp/bop-wp2324-{unit,type,lint,build,focused,pg,gates}.log. Full API/PG evidence tested2ebc871
plus the six WP files; final targeted12/type/lint/build cover the fixture-reference correction.
The initial type failure is disclosed above even though its log now contains the passing rerun.

Install evidence reused from WP-2321: frozen offline install, exit0/41 projects, tested5271460 plus
the seven WP-2321 edits committed26a9fe4, /private/tmp/bop-wp2321-install.log. Same installation,
Node24.18.0/pnpm11.13.0, store-dir /private/tmp/bop-rms-wp2288-dependency-cache/v11, manifests,
lockfile/pins and dependency tree remain unchanged. No install, full pnpm verify/test:integration,
Ordering unit suite or PWA suite ran here. No new claims are made for those commands. WP-2323
Ordering and architecture/migration evidence remains applicable to unchanged owner source and
migrations; the fresh PG run covers new composition. Full journey regression remains planned.

Final six-file diff/format/security review passes; owned test containers/volumes are zero. No
unresolved in-scope security blocker, new route, domain fact, credential leakage or history rewrite.
The separate AGENTS/skill edits remain excluded; main remains unchanged. No remote CI, push, merge,
deployment or real provider evidence. Rollback removes optional item configuration. Next is browser
configuration/journey activation and milestone regression, with external gates kept explicit.
