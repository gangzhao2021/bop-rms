# WP-2236 - Guest Binding Credential Service

## Authority and scope

Owner authorizes sustained implementation and verified local commits. Baseline1ef842d (WP-2235),
branch codex/wp-2236-binding-service. WP-1003 current authorization and WP-2234/2235 preparation,
single activation, immutable candidate identity and transactional persistence govern this service.
Composite authority is unchanged. This completes the Identity application producer before any HTTP
activation. Existing GuestSessionService.authorize supplies current Store/QR/Session/CSRF authority.

Only the service handles raw credentials ephemerally. Caller input carries operation and credentials,
never an authoritative Cart/Store/Session identity or timestamp. A server clock and required bounded
preparation lifetime govern time. Trusted owner ports reserve an eligible initial Pickup target,
prepare it only after candidate credential acknowledgement, and idempotently activate from the exact
Identity completion receipt. These ports expose no credential hashes to Ordering and create no
BOP-to-RMS import. Concrete Ordering and same-origin HTTP/browser transport remain later WPs.

## Allowlist

- docs/spec/README.md and docs/spec/work-packages/WP-2236.md
- packages/bop/identity/src/application/ports/guest-binding-ports.ts
- packages/bop/identity/src/application/guest-binding-service.ts
- packages/bop/identity/src/tests/guest-binding-service.test.ts
- packages/bop/identity/src/index.ts
- packages/database/test/guest-binding-store-acceptance.test.mjs (real store/service composition)

## Acceptance

1. Authorize current credential/CSRF before any owner reservation or credential generation, verify
   exact loaded Session, and validate closed scoped owner receipts. Server-generated candidate and
   purpose-separated recovery proof go only to ephemeral credential transport; persistence gets hashes.
2. Activation proves receipt of all candidate credentials, validates current predecessor, prepares
   Ordering through its public port and activates Identity once. Verify returned state before proceeding.
3. Completion reauthorizes the active candidate and finishes owner activation idempotently, including
   failure after Identity commit and loss of repeated completion responses. Never reuse revoked proof
   or rotate the candidate identity again. Unknown owner outcomes remain unavailable, not success.
4. Unit and real PostgreSQL composition tests cover denial before effects, wrong scope/receipt,
   credential purpose, stale authority, substituted port results, no plaintext persistence and recovery.
5. Pinned frozen offline install, Identity tests/lint/typecheck, existing Identity PostgreSQL suite,
   full pnpm verify, pnpm test:integration, secret scan and exact scope/diff/privacy review.

No new migration, dependency, transport route, runtime registration, Store policy value or external
mutation. Browser and remote evidence remain unclaimed. The selected lifetime is injected, bounded
by15 minutes and current Session/owner deadlines; production configuration is not invented. Local
commits are authorized; main merge, push and deployment are not. Rollback removes additive service
exports and preserves all persisted historical operations.

## Results

Acceptance 1-5 PASS locally. Identity194/194 across11 files includes18 new service tests, with
authorization before effects, closed input/scope, no plaintext persistence, owner-stage failures,
substituted persistence/completion results, invalid policy bounds and repeat completion. Identity
lint/typecheck passed. Existing Identity PostgreSQL acceptance passed5/5 across4 files with the
actual GuestSessionService, credential providers, binding service and scoped stores composed.
The owner ports in that test are explicit synthetic business evidence, not a concrete Ordering
implementation. It proves failure after Identity commit, revoked predecessor denial, service/store
recreation and repeated completion after preparation expiry without changing candidate identity or
duplicating history/Audit. The combined database scenario ends with10 preparation and10 Audit rows.

Frozen offline install, complete CI=true pnpm verify and uncached pnpm test:integration passed.
All40 build tasks succeeded; existing no-output Turbo warnings remain. Secret scan2/2, final seven-file
scope/LF/diff, documentation format and guidance checks passed. No new migration, dependency,
raw-credential persistence, credential-to-owner disclosure, production configuration or transport.
Local logs: /private/tmp/bop-wp2236-identity-final.log, bop-wp2236-pg.log,
bop-wp2236-install.log, bop-wp2236-verify.log and bop-wp2236-integration.log in the same directory.
Browser, fresh remote CI, real Store/Provider evidence and active HTTP recovery remain unclaimed.
Next: implement Ordering's target preparation/activation/current-binding owner before HTTP composition.
