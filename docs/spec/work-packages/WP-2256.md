# WP-2256 - Scoped Complete Price Quote History Reader

## Authority and readiness

Owner authorizes successive bounded implementation and verified local commits. Baseline34ea5f3,
branch codex/wp-2256-price-quote-history-reader. Composite Sections0–97, ADR-0029/0031 and
WP-1103/2255 retain Pricing ownership, immutable historical Quote facts, exact money, forced Store
RLS and the existing migration runner. The complete v1 codec is verified. No real Store or external
Provider input is required to implement a historical reader using isolated synthetic evidence.

Add one forward expand migration in Pricing's existing namespace and table: nullable
complete_snapshot_text on rms_pricing.price_quote, with bounded versioned JSON-envelope checks.
TEXT preserves the canonical codec representation; jsonb must not rewrite its bytes. Existing
historical rows stay null and append-only update/delete protection remains intact. No backfill,
old-migration edit, history repair, runner bypass, down migration or live database alteration.
The accepted runner still refuses new migrations below an applied high-water mark. Verification
uses fresh owned databases; this WP is not an existing-environment rollout approval or proof.

Add an owner-scoped read-only adapter. Its constructor snapshots exact Brand/Store references;
each transaction sets local RLS context and every query also filters Brand/Store and Quote identity.
No rows returns null. Present-but-partial, malformed, inconsistent or unexpected results return a
fixed dependency error without database details. Decode with WP-2255 and compare every existing
root summary field, including exact UTC microsecond rendering, against the complete snapshot.
No current config is queried. Reads preserve expiry rather than manufacturing Current validity.

The adapter only reads the owning aggregate root. Full snapshot text is the complete historical
representation; legacy child tables are not used to reconstruct omitted evidence. The future write
service must atomically retain root/child facts, full evidence, idempotency and Audit. Caller owns
Actor/purpose/permission/current Session authorization. A read adapter does not confer access rights.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2256.md
- docs/spec/design/business-scenario-coverage.md
- migrations/1200-rms-pricing/1200_007_alter_price_quote_snapshot.sql
- packages/database/src/catalog.test.ts
- packages/database/test/price-quote-acceptance.test.mjs
- packages/database/test/isolated-database-acceptance.mjs
- packages/rms/pricing/src/infrastructure/persistence/price-quote-query-store.ts
- packages/rms/pricing/src/tests/price-quote-query-store.test.ts
- packages/rms/pricing/src/index.ts
- tooling/database-ownership/validate.mjs
- tooling/database-ownership/validate.test.mjs

## Acceptance and verification

1. Unit tests prove snapshot ownership, closed inputs/results, bound identity and parameterized
   scope, context-before-query, missing/partial/corrupt data, summary mismatches, historical expiry,
   fixed error redaction and infrastructure failures. No connection is opened by construction.
2. Existing Quote PostgreSQL acceptance retains its original RLS/immutability tests and adds real
   owner-adapter complete bigint round-trip, exact Store/Brand isolation, legacy unavailability,
   invalid envelope rejection and immutable null/full snapshot behavior. Only synthetic test inserts.
3. Frozen pinned install, affected format/lint/typecheck/test/build, migration/ownership/permission,
   pricing-quote:acceptance, full pnpm verify, forced pnpm test:integration --force, Secret/privacy,
   final twelve-file scope/LF review and owned cleanup. No skipped check is counted as passed.

No application writer, Audit mutation, new table/ownership, runtime or Screen wiring, current
Catalog eligibility, Payment/Provider readiness or external service change. Remove unapplied code
by reviewed revert; applied migration recovery is forward-only. No main merge, push or deployment.

Readiness correction before editing: the existing isolated-database acceptance pins the catalog
count at87. Include its expected count update to88 for this one new migration; retain all isolation,
failure and cleanup assertions. This is required verification fixture maintenance, not runner logic.

Persistence readiness correction: WP-0013 correctly denies Pricing's first concrete SQL adapter.
This owning Persistence WP adds the same exact-file admission used by WP-2219/2223, limited to
@rms/pricing, rms_pricing.price_quote and price-quote-query-store.ts. Tests retain denial for another
owner/schema/missing table, adjacent assets and driver imports. No general persistence exemption.

## Verification record

Acceptance1 PASS: Pricing175/175, including31 new history reader cases. Tests prove construction
without I/O, immutable copied scope, local context before parameterized identity lookup, owned frozen
snapshot output, historical expiry preservation, missing rows, partial/malformed/accessor results,
invalid identity before I/O, foreign identity/scope, summary mismatches and redacted transaction/
context/query failure. SQL microsecond timestamps are compared exactly to canonical snapshot time.

Acceptance2 PASS: existing pricing-quote:acceptance passes (Pricing175/175, API8/8, PostgreSQL2/2).
The original Quote RLS/immutability scenario remains intact. A new real adapter scenario uses an
isolated read-only role and synthetic complete snapshots with unit amount9007199254740993. It proves
full round-trip, foreign Brand/Store isolation, empty results, legacy null rejection, root-version and
microsecond mismatch rejection, invalid envelope denial and unchanged null/full values after denied
UPDATE/DELETE. All clients and owned database resources are cleaned. This proves fresh isolated
bootstrap, not an existing-environment migration rollout or real Store/tax/provider validity.

Acceptance3 PASS: pinned frozen install, affected format/lint/typecheck/build, database94/94,
migration84/84 with88 immutable migrations, ownership85/85, permissions8/8, full CI=true pnpm verify
(40 successful builds), forced uncached CI=true pnpm test:integration --force
(cache bypass54b28c44d60544c3,2m0.412s), Secret2/2 and final twelve-file scope/LF/privacy review pass.
Initial test unused arguments/generic mock and one architecture fixture directory error were corrected.
The scanner's initial first-persistence rejection was resolved by explicit scoped admission plus four
negative/positive tests, following the existing owning-Persistence-WP mechanism. No guard was disabled.
The isolated database catalog expectation changed87 to88; runner/high-water/cleanup behavior did not.
No old migration, dependency, tracked generated artifact, main merge, push, deployment or external
service changed. Required checks are not skipped. Existing no-output Turbo build warnings are unchanged.

Logs under /private/tmp: bop-wp2256-install.log, bop-wp2256-pricing.log, bop-wp2256-quote.log,
bop-wp2256-migration.log, bop-wp2256-ownership.log, bop-wp2256-permission.log,
bop-wp2256-database.log, bop-wp2256-verify.log, bop-wp2256-integration.log and bop-wp2256-secret.log.
All criteria pass locally. Atomic Quote/line/Audit writes and application idempotency remain separate.
