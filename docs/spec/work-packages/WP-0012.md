# WP-0012 — Import Boundary Architecture Test

## Identity and authority

- Work Package: `WP-0012`; Epic: `EPIC-00 / F00.2 Architecture Enforcement`; Priority: `P0`
- Owner role: Architecture / Platform Engineer
- Authorized baseline: `main@c93f5e46ff0d9d88dd567af68875ebee2da84080`
- Canonical source: `BOP-RMS Complete Handoff Package.md`, `BOP-RMS-HANDOFF` version `0.5.3`, architecture baseline `v1.0`
- Explicit execution authorization: 2026-07-16

Bounded authority is Sections 0, 46.3–46.11, 48.1–48.2, 48.6–48.9, 54.4 F00.2, 56.1–56.6, 56.19, 56.27–56.28, 89–91, and direct precedence rules. Later Section 56.4 supersedes the older `public.ts`, `interface/`, `test/`, adapter, and migration layout: canonical Modules live at `packages/bop|rms/<moduleName>`, use `src/index.ts`, `interfaces/`, `messaging/`, `providers/`, and `src/tests/`. Section 56.5 and integrated WP-0010 establish `@bop/<moduleName>` and `@rms/<moduleName>` identities.

## Bounded responsibility

Build a deterministic TypeScript-aware architecture test. Discover canonical Module roots, reuse the WP-0010 Manifest validator and WP-0011 layout, and compare Manifest identity/public exports with directory and `package.json` identity/export map. Scan `.ts`, `.tsx`, `.mts`, `.cts`, `.js`, `.jsx`, `.mjs`, and `.cjs`, excluding dependency/cache/build output.

Edges include static import, re-export, type-only import, import-type, import-equals, string-literal dynamic import, and `require`. Computed import/require fails closed. Same-Module relative imports are allowed; cross-Module relative imports and paths leaving the owning Module fail. Generated `src/module.manifest.ts` may use only WP-0011's exact WP-0010 authoring-contract import.

Cross-Module package imports must match exact-case discovered identity, exact `allowedSynchronousDependencies`, and a subpath declared identically by Manifest `publicExports` and `package.json.exports`. BOP may depend only on BOP; RMS may depend on BOP or RMS. Private `src`, Domain, Infrastructure, Persistence, ORM, Database, Migration, Adapter, private, or internal paths are never public.

CLI diagnostics include stable path, line, code, and message ordering and exit nonzero on violations or unresolved facts. Tests use only temporary synthetic Modules.

## Scope, verification, and status

Owned files are `tooling/import-boundary/`, root/CI invocation, this brief, spec index, and bounded README guidance. WP-0013 database ownership, WP-0014 Domain-to-ORM/Infrastructure checks, real business Modules, persistence artifacts, Provider, production/deploy/Figma, and a vertical slice are non-goals. Real owners/contracts and Store/Provider/legal/production facts remain External Evidence. Rollback is a reviewed Git revert.

Verification requires exact tool pins, two frozen installs with unchanged lockfile, focused/WP-0010/WP-0011/root/forced-uncached checks, Screen Registry, guidance, JSON/YAML, graph, unique CI, PostgreSQL and five-service lifecycles, diff/security/scope review, and cleanup. Synthetic acceptance covers legal BOP→BOP, RMS→BOP, RMS→RMS; all supported syntax; BOP→RMS; relative/private/unexported/undeclared edges; Manifest/package/export/layer mismatch; case conflict; path escape; and determinism.

- Specification: `Bounded and Active`
- Preflight: `PASS` for protected-evidence-only main, exact local/fresh remote/authorized SHA, WP-0011 PR #10 squash merge, successful main push run `29543565591` job `87770759221`, Canonical `0.5.3` / architecture `v1.0`, and WSL.
- Next allowed after verified integration: `WP-0013 — Database Schema Ownership Test`.
