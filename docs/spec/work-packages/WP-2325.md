# WP-2325 - Dining Admission to Cart Browser Journey

## Authority and scope

Baselinea660e0fd55f308874bead1ff3034714052102730; branch codex/wp-2325-dining-cart-browser-journey.
Composite Sections0-97, accepted WP-2228, CUST-ENTRY/CUST-MENU/CUST-SELLABLE-DETAIL,
CUST-SELLABLE-CONFIGURE (contextual) and CUST-CART under Section88.6 govern. Continuous local
implementation and verified local commits are authorized. Preserve the separate AGENTS/skill edits.
The existing Menu configurator already uses the standard Cart client for DineIn; no replacement UI
or route is needed. Activate Cart only in the isolated local lab and verify the existing flow.

Compose real current Dining participation and consumed-admission binding reads, scoped Cart owner
transactions, existing local API runtime and browser. Existing synthetic Staff/QR/abuse/Store and
explicit synthetic Catalog selection/lifecycle policy remain labeled; no real producer/price claim.
Menu display comes from its persisted published projection. Test desktop/mobile QR, Join, credential
binding, menu detail, configure/add, Cart quantity/remove, empty state, no storage/URL secret,
no page errors and responsive layout. This browser run covers each participant's complete journey;
actual two-participant shared-store semantics and foreign-control UI already have WP-2323/2324/2320
evidence. Do not claim simultaneous two-browser shared-table coverage from separate viewport runs.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2325.md
- apps/customer-pwa/src/main.lab.tsx
- apps/customer-pwa/src/cart/CartPage.tsx
- apps/customer-pwa/src/cart/CartPage.test.tsx
- apps/customer-pwa/scripts/local-customer-lab.mjs
- packages/database/test/local-customer-lab-acceptance.test.mjs
- packages/database/test-support/local-customer-dining-seed.mjs
- packages/database/test-support/local-customer-cart-seed.mjs

## Verification and stage milestone

Run existing customer-lab:acceptance during iteration to verify the actual browser/API/database
path. Once it passes, this is the named Dining admission-to-Cart end-to-end regression milestone:
run pnpm verify and pnpm test:integration, without automatic cache bypass. This consolidates the
broader regression previously deferred by WP-2320 through WP-2324. Do not pre-run duplicate full
workspace checks: pnpm verify supplies existing affected format/lint/type/unit/architecture/contract/
migration/build checks. Run secret-scan:check and generated Service Worker verifier on the same
build artifact if not covered by the composite; no second build. Record any skipped integration
tasks/cache reuse exactly. Final changed-file/LF/diff, private-data/permission and owned-resource
cleanup. Reuse frozen installation only with unchanged same installation/manifests/lock/pins and
recorded WP-2321 evidence; otherwise run fresh frozen install. No external services or new scripts.

## Observed results

The first complete lab run reached Add and quantity Update, then failed waiting for the empty-Cart
heading after Remove. Inspection confirms a real UI gap: a non-null server Cart with zero items
renders zero rows but no empty-state guidance. Extend this WP's explicit allowlist with CartPage and
its tests to render a ready, active, non-stale empty Cart without discarding its controller identity.
Preserve pending/unknown, offline, terminal and feature-disabled states; this is the existing
CUST-CART Empty contract, not a new route or business rule. The failed run remains recorded.

PASS for the local admission-to-Cart journey. The successful customer-lab:acceptance run passes1/1
in15.08s, exercising desktop1440/mobile390 QR entry, actual Join/credential activation, current
consumed-admission and participation reads, persisted menu detail, configure/add, quantity update,
remove and empty-state guidance. Each viewport has its own DiningSession/participant; this is not
simultaneous two-browser shared-table evidence. Real shared multi-participant persistence and safe
foreign-note/control behavior remain covered by WP-2323/2324/2320 evidence at their stated scopes.
The lab verifies each Cart stays Active at version4 with no lines, exactly3 item operations and4
Cart Audits per journey including creation, without duplicate effects.
Cookie flags, cleared entry URL, empty browser storage, reflow and no page errors pass. Actual
Identity/Dining/Ordering repositories are used; Staff/QR/abuse/Store, selection and lifecycle policy
fixtures remain explicitly synthetic and pricing/Order submission/Provider behavior is not claimed.

The seven added empty-Cart cases cover Pickup/DineIn, disabled/stale, pending/offline/unknown states.
Targeted CartPage24/24 passes. Fresh stage PWA646/646 (36 files), API563/563 (32) and Ordering829/829 (40)
pass. Stage evidence also covers Identity476 and Dining951, with valid cached unchanged tasks identified
in the logs. All required stage validation components pass, including architecture,
ownership/permissions, contracts,101 migrations, full root unit/database acceptance and40 builds
(33 cached). The generated Service Worker verifier passes on this same final build; no second build.
Secret scan2/2 passes. pnpm test:integration passes1 task,0 cached in2m0.3s, hash e013a80753f08d04;
the cache miss was normal input invalidation, with no --force or other cache bypass.

Composite execution is reported exactly: pnpm verify initially passed guidance/module/import/
ownership/permission/Domain/migration/foundation/helpers/OpenAPI/Event/format gates, then failed
root lint because the browser wait callback used bare document in a Node script. Qualifying that
same browser global as globalThis.document fixed lint without changing browser behavior. The
successful15.08s lab evidence remains valid for that equivalent expression; no duplicate lab run.
From the failed component, existing root lint, typecheck, screen-registry:check, test and build were
run in their original order and all passed. Earlier passing components were not restarted; changed
script formatting was reapplied and checked. Thus complete equivalent stage evidence passes,
but pnpm verify itself is not misreported as a single successful invocation.

Local evidence: /private/tmp/bop-wp2325-lab-empty-state-failure.log records the first real UI failure;
lab.log records the successful run; cart-unit.log records24 cases; verify-lint-failure.log records
the partial composite; verify-remainder.log records all remaining passing components; integration.log
and secret.log record their results. All are under /private/tmp/bop-wp2325-* and tested a660e0f plus
the ten listed WP edits. The only later executable change after the successful lab was the equivalent
global qualifier above, covered by final root lint/type checks. No failure is hidden behind reuse.

Frozen install reused: WP-2321 /private/tmp/bop-wp2321-install.log, pinned pnpm install --offline
--frozen-lockfile --store-dir /private/tmp/bop-rms-wp2288-dependency-cache/v11, exit0/41 projects,
tested5271460 plus seven edits committed26a9fe4. The same installation, Node24.18.0/pnpm11.13.0,
manifests/lockfile/pins and dependency tree remain unchanged. No install ran in this WP. Build reports
existing no-output cache warnings for no-emit Domain packages; all40 tasks exit successfully and no
unrelated Turbo configuration change was made. No additional full Playwright suite is claimed.

Final ten-file scope/LF/diff/security review passes. Owned test containers/volumes are zero and the
HTTPS lab listener5184 is closed; main remains cedc44c9. Separate AGENTS/skill changes stay outside
the business commit. No unresolved in-scope security blocker, production registration, Order/Batch/
payment, fake quote/Provider outcome, generic redesign, Figma or external operation is introduced.
Remove lab opt-in to roll back activation and preserve all history. External launch gates remain
separate; current-session continuation and broader production readiness are not claimed complete.
