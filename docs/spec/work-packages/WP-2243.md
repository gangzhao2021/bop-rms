# WP-2243 - Safe Pickup Cart Display Query

## Authority and scope

Owner authorizes continued implementation and verified local commits. Baseline78f2288,
branch codex/wp-2243-cart-view. Composite Sections0–97, WP-1000/1201–1205 and WP-2241/2242
supply the public branding, Cart/Quote, current authorization and pinned display contracts.

Produce the existing WP-1205 Cart view shape in Ordering, using authorized Pickup reads, Catalog
historical display, Store public branding and the latest attached Quote for the exact Cart version.
Read again before delivery and reject changed Cart or Session context. Missing/other-reference
Cart stays null; dependency or incomplete display evidence fails bounded. Internal Actor/scope,
Catalog rule/digest, Session credential, Audit and quote intent details never enter the view.
Money uses exact decimal strings; line estimates remain explicitly unavailable because attachments
do not contain authoritative per-line prices. Preserve effective lifecycle and Quote expiry.

Add CustomerCart as an explicit consumer purpose of Store's existing public branding profile.
Its underlying CUSTOMER_ENTRY publication/effective-period configuration remains the source of
public journey branding; its guards are preserved, not bypassed or reinterpreted. This introduces
no duplicate configuration, new Store fact, private-profile access or publication approval. Only
Brand/Store display names and currency are used in the Cart view. Identity supplies the current
public Store reference and locale through the internal authorized-reader context.

Catalog gets a bounded batch facade with request-local version-read reuse only. No cross-request
or browser cache. Ordering's Quote adapter gets an owner-scoped latest-attachment read for an exact
Cart version and observation time. No mutations or history changes are made by these reads.

## Screen contract

CUST-CART remains the Phase1 standalone Customer /cart, exact Guest/Store scope, side-effect-free
query, existing field groups and permissions under Section88 and WP-1205. Existing loading/empty,
permission/not-found, disabled/stale/conflict/failure and offline-readonly UI behavior is unchanged.
This package supplies the data producer only; no route, React, CSS, navigation, analytics, responsive,
accessibility or Figma change. Existing 44px, keyboard/focus, reflow/zoom and reduced-motion rules
remain with that UI. No browser/visual acceptance is claimed here.

## Allowlist

- docs/spec/README.md, docs/spec/work-packages/WP-2243.md, docs/spec/design/business-scenario-coverage.md
- packages/rms/catalog/src/application/selection-display-query-service.ts
- packages/rms/catalog/src/tests/selection-display-query.test.ts
- packages/rms/store/src/contracts/public-store-profile.ts
- packages/rms/store/src/tests/public-store-profile.test.ts
- packages/rms/ordering/src/application/pickup-cart-read-service.ts
- packages/rms/ordering/src/tests/pickup-cart-read-service.test.ts
- packages/rms/ordering/src/infrastructure/persistence/cart-quote-store.ts
- packages/rms/ordering/src/tests/cart-quote-store.test.ts
- packages/rms/ordering/src/application/customer-cart-view-query.ts
- packages/rms/ordering/src/tests/customer-cart-view-query.test.ts
- packages/rms/ordering/src/index.ts
- packages/database/test/cart-quote-store-acceptance.test.mjs
- packages/database/test/cart-binding-store-acceptance.test.mjs

## Acceptance and verification

1. Canonical safe Cart DTO only after current authorized reads; exact Catalog echo, Store public
   branding, immutable Pickup creator, Quote scope/version/line/amount evidence; no private leakage.
2. Revocation or changed Cart/context during dependent queries denies delivery. No Session renewal,
   cart/history writes or fabricated price. Empty/unavailable/expired cases remain explicit.
3. Bounded Catalog batch and request-only reuse; unchanged public profile publishing guards; latest
   Quote queries preserve absence, exact amounts, RLS, scope and deterministic original attachments.
4. Affected unit and real PostgreSQL tests; pinned frozen install; full pnpm verify, uncached
   pnpm test:integration, secret/privacy and final staged scope/LF/documentation checks.

No migration, dependency, production activation, DineIn/recovery implementation or external mutation.
Local commits only; no main merge, push, deploy or remote CI claim. Real Store/Provider/legal and
operational evidence remains external. Rollback is local source revert with no persisted change.

## Final local results

Acceptance1–4 PASS locally. Catalog109/109, Store84/84 and Ordering354/354 pass, including
request-local batch reuse, unchanged publication guards, read reauthorization, exact decimal money,
private-field exclusion, changed Cart/context rejection and effective Cart/Quote expiry.

Cart PostgreSQL6/6 passes. The real Identity/Ordering/HTTP continuation serves the authorized current
Cart, rejects revoked credentials and another Cart reference, preserves Session/history, and emits
no-store safe DTOs. Store/QR/policy fixtures remain synthetic; the HTTP case has an empty Cart and
synthetic branding. Nonempty display and Quote boundary behavior has unit evidence; latest Quote
hydration has real PostgreSQL evidence. No browser or production journey acceptance is claimed.

Pinned frozen offline install, full CI=true pnpm verify (40 successful builds), final uncached
CI=true pnpm test:integration (cache miss3afdd11b1a732a47, 1m58.035s), Secret2/2 and staged
16-file scope/LF/privacy/documentation checks pass. Existing Turbo no-output warnings are
informational. Initial test fixture typing issues were repaired before final verification.
No migrations, dependency changes, ownership exemptions, production activation or external actions.

Logs: /private/tmp/bop-wp2243-verify.log, bop-wp2243-integration.log, bop-wp2243-secret.log,
bop-wp2243-install.log, bop-wp2243-catalog.log, bop-wp2243-store.log and bop-wp2243-cart.log
in the same directory. Local commit only. Runtime/PWA composition, recovery, DineIn and external
operational evidence remain incomplete; this package does not complete the overall project.
