# WP-2202 — Restore Finite GitHub Cold-start Verification

## Identity and authority

- Work Package: `WP-2202`
- Type: CI verification infrastructure repair
- Baseline: exact WP-2201 head `32010a839dcf92367bd6557b1c95b6f6c9d23833`
- Branch: `codex/wp-2202`
- Canonical source: `BOP-RMS-HANDOFF` version `0.5.3`, composite Sections `0–97`
- Applicable accepted contract: WP-0006 finite, self-cleaning Linux environment verification and
  the unique read-only `bootstrap / verify` workflow
- Authorization: Owner direction on `2026-09-02` to continue the remaining GitHub work

Decision precedence follows the Specification Index. This brief does not reopen WP-0006 startup,
security or cleanup behavior; it adjusts only finite time budgets after current clean GitHub runners
demonstrated that the repository has outgrown the older limits.

## Evidence and Definition of Ready

- PR #170 run `33586898910` and PR #171 run `33586944954` each completed dependency installation
  and WP-0005 PostgreSQL verification, then timed out in `pnpm environment:verify` after 180 seconds.
  The captured supervisor tails showed active cold TypeScript builds, while runtime state truthfully
  remained stopped because application processes had not started.
- Earlier PR #170 run `33585461764` completed environment startup and the workspace baseline, then
  the job's fixed 20-minute limit cancelled the repository-wide quality step while Merchant Web
  tests were passing. No assertion failure was reported.
- Merged PR #86 previously accepted the same bounded remedy when repository growth exceeded the
  earlier 90-second cold-start allowance: extend the finite wait without weakening validation.
- The working tree was clean at exact WP-2201 head before this branch was created.

**DoR result: `PASS`.**

## Goal and acceptance contract

Restore a conclusive GitHub verification result without removing, skipping, reordering or weakening
any existing gate.

1. A clean Linux runner may spend up to 360 seconds completing the pre-runtime workspace build
   before WP-0006 health verification fails closed.
2. The unique `bootstrap / verify` job remains finite but may run for up to 45 minutes so every
   existing environment, quality, realtime/eventing, frozen-install and isolated-database step can
   finish.
3. Supervisor exit, unhealthy services, invalid configuration, secret disclosure, assertion
   failure and cleanup failure continue to fail the workflow exactly as before.
4. The exact WP-2202 PR head must complete the unchanged GitHub verification sequence successfully;
   a timeout, cancellation or skipped gate is not acceptance evidence.

## Exact tracked-file allowlist

- `.github/workflows/bootstrap.yml`
- `tooling/environment/verify-local.mjs`
- `docs/spec/README.md`
- `docs/spec/work-packages/WP-2202.md`

No application, Domain, package, dependency, lockfile, database, migration, fixture, authentication,
authorization, secret, production configuration or deployment change is authorized.

## Verification and evidence map

- Parse the workflow YAML and run Prettier over all four allowlisted files.
- Run ESLint and Node syntax validation for `tooling/environment/verify-local.mjs`.
- Run the tracked-file secret scan and `git diff --check`.
- Review the exact diff and allowlist for skipped/reordered checks, permission changes, secret output,
  `pull_request_target`, production settings or unrelated scope.
- Push one bounded PR on top of `codex/wp-2201` and require its exact-head `bootstrap / verify` run
  to pass every existing step.

The Docker-backed end-to-end verifier is Linux/WSL-specific and cannot be claimed from this macOS
host. GitHub's clean Ubuntu runner is the required acceptance environment.

## Risks, rollback and external boundary

- Risk: a larger limit can expose a genuinely stuck job for longer. Both limits remain explicit and
  finite; failures remain visible, and no retry loop is added.
- Rollback: revert the WP-2202 commit to restore the prior 180-second and 20-minute limits.
- External Evidence: no real Tenant, Store, Actor, credential, Provider result, business fact or
  production activation is introduced or claimed.
- Local implementation, verification, commit, push and stacked PR publication are authorized.
  Merge, deployment and Provider actions remain unauthorized until exact-head CI is green and a
  separate integration decision is made.

## Status

- Specification: `Resolved for WP-2202 execution`
- Implementation: `Completed locally on codex/wp-2202`
- Verification: workflow YAML parsing, Prettier, ESLint, Node syntax, tracked-file secret scan
  `2/2`, `git diff --check`, exact allowlist and final scope review passed. The Linux-only
  environment lifecycle and every unchanged workflow gate passed at implementation head `049de27`
  in run/job `33629139991 / 100243961313` in `34m27s`. A documentation-only evidence head must pass
  the same complete workflow before WP-2202 is Ready.
- GitHub publication: PR #172 is published against `codex/wp-2201`.
- Integration/deployment: `Not authorized`
