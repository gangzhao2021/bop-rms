# WP-2221 — Unquoted Pickup Cart Item HTTP Composition

## Authority and scope

Verified clean baseline WP-2220 `d73aec2`, branch `codex/wp-2221`. Continued implementation and
verified local commits are authorized; no push, merge, deploy or production activation.
Composite Handoff 0.5.3 Sections 24.2/24.5, 44.9, 52, 87–97 and accepted WP-1201–1205,
WP-2218–2220 govern this increment. Existing CUST-CART routes, permissions, CSRF and response schema
remain canonical; no Screen or component changes.

Compose optional Pickup Add/Update/Remove HTTP commands with actual Identity authorization,
Ordering commands/persistence, public Catalog query label preparation and original snapshots.
The opt-in repository checks Quote absence after acquiring the Cart lock, before any mutation,
and atomically wraps the existing snapshot JSON with versioned absence evidence. Legacy unwrapped
snapshots remain readable but cannot produce a Quote-null command response. No new migration or
historical rewrite. Any attachment, including old versions, denies a new unquoted command.
Replays reauthorize, validate intent/expiry and use original durable evidence without fresh sources.
Foreign Cart/Item objects use uniform NotFound; safe conflict/selection/lifecycle errors retain the
existing transport contract. Preparation happens before commit and produces no Quote claim.

## Acceptance

1. Scoped lock-before-Quote check denies concurrent/old attachments without mutation and preserves
   original atomic operation evidence; legacy results cannot be interpreted as Quote absence.
2. HTTP Add/Update/Remove return safe original views with explicit unavailable prices; retries after
   source failure or later Cart changes return the same body without duplicate Cart/Audit writes.
3. CSRF, Session revocation, foreign scope, stale version, invalid selection and source failures
   fail safely; existing create/read behavior and default unavailable runtime remain unchanged.
4. Frozen install, affected format/lint/type/tests, ownership and complete verify pass; scope/privacy,
   links and isolated resource cleanup are reviewed.

## Allowlist

- This brief, README.md, docs/spec/README.md, docs/spec/project-status.md
- Ordering src/application/customer-cart-presentation-service.ts,
  src/application/cart-item-presentation-snapshot.ts, src/application/cart-item-command-service.ts,
  src/application/ports/cart-item-command-ports.ts, src/infrastructure/persistence/cart-item-store.ts,
  src/infrastructure/persistence/database-access.manifest.ts, src/tests/cart-item-command.test.ts
- API src/customer-cart-composition.ts, src/customer-cart-composition.test.ts
- packages/database/test/customer-cart-presentation-acceptance.test.mjs

## Verification and boundaries

Use pinned Node/pnpm, frozen install, existing Ordering/API/database checks,
pnpm customer-cart-presentation:acceptance and complete
DOCKER_HOST=unix:///Users/ryanzhao/.colima/default/docker.sock TMPDIR=/private/tmp CI=true pnpm verify.
Modified isolated acceptance uses WP-2221 resources and synthetic source facts; no real publication,
Quote creation, browser, Provider or production evidence. Rollback disables optional Item composition;
immutable operation evidence stays intact. HTTP activation here means an opt-in tested composition,
not the ordinary API entry point or a deployed service.

## Status

Implemented and locally verified on `2026-09-05`. Default runtime and production remain inactive.

## Observed evidence and contract review

Pinned Node `24.18.0` and pnpm `11.13.0`; frozen install passed without dependency/lockfile changes.
Ordering 227/227 and API 189/189 tests passed; affected typechecks and lint passed. Expanded real
HTTP/PostgreSQL acceptance passed in 8.89 seconds, then passed again inside the complete gate in
8.95 seconds with final old-Quote, separate Catalog failure, ETag and private-field assertions.
Complete local verification subsequently passed.

The existing transport maps foreign Origin to 400, invalid CSRF/Session to 401, foreign object to
404, version/idempotency conflict to 409 and rejected Catalog selection to 422. A test initially
expected 403 for Origin and was corrected to the existing accepted 400 contract; no handler or
security gate was relaxed. Early type/lint failures were limited to cross-Domain branded-reference
comparisons and unused/undeclared fixture variables and were corrected without disabling checks.

Reviewed CUST-CART in the Screen Registry and the existing WP-1205 transport contract. Route, public
wire schema, permissions, no-store responses, unavailable-price state and UI components remain
unchanged. No new browser/visual evidence is claimed. Prepared names use the public Catalog query;
Catalog receives no Note, Cart/Item ID, Actor or credential. Restricted notes come only from the
stored original Cart aggregate in the authorized response, never from display evidence or Audit.

A repository must explicitly advertise guarded presentation before HTTP writes can proceed. Only
that mode records the versioned envelope after the Cart lock and separate Quote query. Incoming
commands cannot supply absence evidence; direct repository commits with that field are rejected.
Original snapshots without an envelope stay readable but cannot construct an unquoted response.
The result view is reconstructed synchronously from durable evidence, avoiding source calls after
commit. Replays preserve original historical response semantics even if a Quote is attached later;
current GET still rejects that quoted state until Quote-aware reads are implemented.

The default API runtime is not activated. Test Store/display/admission/selection and Catalog
publication inputs remain explicitly synthetic; no real price, tax, Quote issuance, Store readiness
or Provider result is inferred. This package adds no migration, table, dependency or external write.

The first full run stopped at root lint on a test diagnostic that caught an error without preserving
its cause. Restored the existing phase-only catch pattern so raw database errors cannot become a
logged cause; no lint exception was introduced. The rerun completed successfully.

## Acceptance evidence

1. **PASS** — real concurrent lock waiting and post-attachment denial; old-version attachments also
   deny new commands, with unchanged Cart/operation/Audit counts. Original snapshot response remains
   identical after a later attachment. Pure view checks reject absent durable proof.
2. **PASS** — real HTTP Add/Update/Remove, fresh persisted reads, exact ETag and original full-body
   replay after source failure/later Cart changes. Replay appends no duplicate Cart operation or Audit.
3. **PASS** — existing Origin 400, invalid CSRF/revoked Session 401, foreign Cart/Item 404, stale or
   changed intent 409, Catalog rejection 422 and source/Quote failure 503. Optional guard capability
   and Pickup scope are required before Cart reads/writes. Response/log scans exclude credentials,
   internal scope, attribution, Note in logs, and private snapshot/proof fields.
4. **PASS** — frozen install, affected checks and complete verification passed. Final scope and
   privacy review found no unaccepted finding; documentation and resource cleanup are recorded below.

## Final verification

`DOCKER_HOST=unix:///Users/ryanzhao/.colima/default/docker.sock TMPDIR=/private/tmp CI=true pnpm verify`
exited 0 against the final implementation: root 358/358, Ordering 227/227, API 189/189, ownership
45/45 and all 40 package builds; all existing architecture, contract, migration, permission,
security and isolated PostgreSQL gates passed. Log: `/private/tmp/bop-wp2221-verify.log`.
The 87-migration catalog is unchanged. Existing Turbo output-file warnings for four Domain builds
remain non-failing; each TypeScript build exited successfully. No required local WP check was skipped.

Final scope contains 14 allowed files, 63 valid local Markdown links, no lockfile/dependency or
migration changes, and no credential-pattern matches in the changed-file scan. Public command
activation is limited to callers explicitly supplying the new dependencies. No browser, remote CI,
real source publication, Provider, push, merge or deployment evidence is claimed.

Cleanup completed: isolated test databases/roles, HTTP listener and pools were released; Docker
container inventory was empty, and Colima was restored to its initial stopped state.
