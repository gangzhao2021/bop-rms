# WP-0014 — Domain Layer ORM / Infrastructure Test

## Identity and authority

- Work Package: `WP-0014`; Epic: `EPIC-00 / F00.2 Architecture Enforcement`; Priority: `P0`
- Owner role: Architecture / Platform Engineer
- Authorized baseline: `main@d3221393cf5bba4d4d8bda8a4c7b482541d28760`
- Canonical source: `BOP-RMS Complete Handoff Package.md`, `BOP-RMS-HANDOFF 0.5.5`, architecture baseline `v1.0`
- Accepted decisions: Canonical Section 93, ADR-0030, IDR-0043
- Explicit local brief / implementation authorization: `2026-07-17`
- External action authority: one WP-0014 commit、push and Draft Pull Request were separately authorized by the Owner on `2026-07-17`；Ready、merge、deploy and Provider mutation remain unauthorized

Higher Section 93 is authoritative for the Domain scan root、source forms、type-only equivalence、Domain-safe classification、alias / package / relative resolution、Node runtime treatment、closed diagnostics、deterministic output and `0 | 1 | 2` exit behavior。Sections 0、44.5.1、46.9–46.11、48.1–48.2、54.4 F00.2、56.4–56.6、56.8–56.14、56.20、56.39–56.40 and 89–92 remain applicable where Section 93 does not supersede them。

## Bounded responsibility

Build a deterministic static Architecture Test over every discovered Canonical Module's exact real `src/domain/**` root。Reuse WP-0010 Manifest identity、WP-0011 layout and WP-0012 Module discovery / TypeScript-aware source-reference parser。Reject Domain dependencies on Application、Infrastructure、Interface、ORM / database、HTTP / transport、Provider SDK、Node runtime / I/O or an unclassified package，including type-only、re-export、import-type、import-equals、literal dynamic import and `require` edges。

Materialize one pure-literal version-`1` dependency-classification registry。It classifies technical safety only and cannot create or modify Module identity、Layer、Manifest dependency、package dependency、export map、business fact or public contract。Unknown dependencies and unsupported / ambiguous aliases fail closed。

## Scope and owned files

- `tooling/domain-layer-boundary/`：classification contract / canonical empty registry、validator / CLI and synthetic temporary-fixture tests。
- `tooling/import-boundary/validate.mjs`：only the minimum exports / refactor required to reuse source extensions、file discovery、reference parsing and existing Module discovery；WP-0012 behavior must remain byte-compatible。
- `tooling/repository-guidance/validate.mjs`：only the finite ADR registry update required to recognize accepted ADR-0030。
- `package.json` and the unique `.github/workflows/bootstrap.yml`：focused check in root verification and CI。
- `docs/adr/ADR-0030-domain-layer-technology-dependency-boundary.md`、this brief、`docs/spec/README.md` and root `README.md`：bounded authority、active status、usage、evidence and next gate。

No real Canonical Module、Domain object、Aggregate、Command、Event、Projection、Repository、ORM / driver、schema / table、migration、connection、seed、Provider adapter / SDK integration、API、UI、Figma、deployment、production or later-WP artifact is authorized。No dependency or lockfile change is expected or authorized。

## Closed implementation contract

- Source extensions：`.ts | .tsx | .mts | .cts | .js | .jsx | .mjs | .cjs`；`.d.ts` is included。
- Source edges：static import、re-export、`import type`、TS import-type、external import-equals、one-literal dynamic import and one-literal `require`。
- Type-only and declaration-only edges obey the same rules as runtime edges。
- A relative or exact alias target is legal only when final exact resolution remains in the same `src/domain/**` root。
- `package.json#imports` accepts one exact non-pattern key / repository-relative string target only。
- TypeScript `paths` accepts one exact non-pattern key / one string target resolved from effective `baseUrl` only。
- Canonical workspace package imports must pass WP-0012 and have an exact `domain-safe` package / export subpath record。
- Closed classifications：`domain-safe | orm-database | http-transport | provider-sdk | runtime-io`。
- Node `24.18.0` built-ins in `node:` or exact bare form are intrinsically `runtime-io`。
- Computed targets are `DOMAIN_DYNAMIC_REFERENCE`；unresolved / ambiguous literal targets are `DOMAIN_UNRESOLVED_REFERENCE`。
- Format：`<path>:<line> [<CODE>] <message>`；sort by path、numeric line、code、message。
- Exit：`0` success / help；`1` Architecture violation；`2` usage、missing / unreadable root / Domain source or internal failure。

Closed WP-0014 codes：`DOMAIN_TO_APPLICATION`、`DOMAIN_TO_INFRASTRUCTURE`、`DOMAIN_TO_INTERFACE`、`DOMAIN_ORM_DATABASE_DEPENDENCY`、`DOMAIN_HTTP_DEPENDENCY`、`DOMAIN_PROVIDER_SDK_DEPENDENCY`、`DOMAIN_RUNTIME_IO_DEPENDENCY`、`DOMAIN_UNCLASSIFIED_DEPENDENCY`、`DOMAIN_DYNAMIC_REFERENCE`、`DOMAIN_UNRESOLVED_REFERENCE`、`CASE_CONFLICT`、`PATH_ESCAPE`、`SYMLINK_PATH`、`UNREADABLE_DOMAIN_SOURCE`。

## Acceptance and evidence map

1. **Authority / identity reuse:** Section 93、ADR-0030 and IDR-0043 are referenced；no second Module / Layer / export authority exists；WP-0010–0012 identity mismatches still fail。
2. **Canonical scan:** missing Domain root passes；exact real `src/domain/**` scans all eight extensions including `.d.ts`；unreadable / case / symlink roots fail with the accepted behavior。
3. **Reference coverage:** all seven source-reference forms are observed；type-only and declaration-only illegal edges fail identically to runtime edges。
4. **Legal Domain dependencies:** same-Domain relative edges and exact registry-approved `domain-safe` package / subpaths pass。
5. **Layer protection:** Domain → Application、Infrastructure and Interface each emit their closed code；other same-Module non-Domain targets are unclassified。
6. **Technology protection:** every `orm-database`、`http-transport`、`provider-sdk`、`runtime-io` and unknown package case emits its exact code；Node built-ins require no registry entry。
7. **Workspace protection:** canonical package edges still require exact Manifest dependency、Layer direction and public export；private / undeclared / unexported targets remain rejected by WP-0012。
8. **Alias / resolution safety:** exact allowed `#imports` and TS `paths` aliases pass only after final-target validation；wildcard、conditional、multiple、escaping、ambiguous and missing targets fail closed。
9. **Static safety:** computed targets、case conflicts、path escape and symlink source / target behavior match Section 93。
10. **Registry safety:** version、closed shape / vocabulary、unique exact package identity、allowed-subpath rules and pure-literal extraction are enforced；the committed registry contains no invented package approval。
11. **Diagnostics / exit:** exact format、numeric-line ordering、closed codes、help and `0 | 1 | 2` behavior are tested；unreadable source uses line `1` when safely attributable。
12. **Determinism / cleanup:** repeated runs over identical bytes are byte-identical；at least 5 positive and 25 independently asserted negative / boundary synthetic scenarios run only in temporary roots and fully clean after success / failure。
13. **Root / CI integration:** `pnpm domain-layer-boundary:check` is discoverable、root `pnpm verify` invokes it and the sole `bootstrap / verify` workflow invokes the same finite check。
14. **Baseline / scope / security:** WP-0010–0013、root tests、forced workspace tasks、PostgreSQL / environment lifecycles、structured files、graph、diff and security / scope / cleanup reviews pass with no real Module、database、Provider、deployment or later-WP asset。

## Verification commands

Run from the dedicated WSL worktree root：

```bash
test "$(node --version)" = "v24.18.0"
test "$(corepack --version)" = "0.35.0"
test "$(pnpm --version)" = "11.13.0"
test "$(pnpm exec turbo --version)" = "2.10.5"

pnpm install --frozen-lockfile
pnpm install --frozen-lockfile

pnpm module-manifest:check
pnpm module-generator:check
pnpm import-boundary:check
pnpm database-ownership:check
pnpm domain-layer-boundary:check
pnpm verify

pnpm exec turbo run format:check lint typecheck test build --force
./tooling/postgres/verify-local.sh
pnpm environment:verify
pnpm list --depth 0
pnpm exec turbo run build --dry=json
git diff --check
```

Also record the lockfile SHA-256 before / after both frozen installs；parse every changed JSON / YAML；prove one bootstrap workflow and a valid workspace graph；verify CLI help and `0 | 1 | 2`；prove all synthetic roots are removed；and inspect the complete diff / untracked set for secrets、PII、real Module / database / Provider facts、dependency / lockfile churn、generated files、production / deploy / Figma or later-WP content。

## Risks、rollback、External Evidence and next action

- Risk：a broad Domain-safe grant could silently admit technical dependencies。Mitigation：exact package plus exact subpath、no wildcard / prefix / transitive approval and fail-closed unknowns。
- Risk：duplicating WP-0012 would create divergent resolution。Mitigation：minimal exports / refactor and unchanged WP-0012 focused tests。
- Risk：alias resolution could hide private or escaping paths。Mitigation：finite alias forms、exact final resolution、case / symlink / containment checks before classification。
- Rollback：normal reviewed Git revert of the future implementation；changing Section 93 semantics requires accepted ADR / IDR / Handoff supersession。There is no data、Provider、deployment or Figma rollback。
- External Evidence：real package need / license / security review、real Module owner / Domain behavior、Store / Provider / legal / production facts and runtime enforcement remain later-WP or external gates。
- Next allowed action after complete local evidence：publish the separately authorized single WP-0014 commit、push and Draft PR，then wait for explicit Owner authorization before any Ready、merge、deploy or Provider action。

## Status

- Specification: `Bounded and Active`
- Preflight: `PASS` for exact local / fresh origin / authorized baseline `d3221393cf5bba4d4d8bda8a4c7b482541d28760`、Canonical `0.5.5` / Section 93、ADR-0030 / IDR-0043、protected Canonical Evidence and independent WSL worktree。
- Implementation / verification: `Complete — local evidence only`。
- Focused evidence: WP-0014 `56 / 56` synthetic tests and repository scan pass；WP-0010 Manifest `2` legal / `15` rejected、WP-0011 Generator `16 / 16`、WP-0012 Import Boundary `19 / 19` and WP-0013 Database Ownership `25 / 25` remain green。
- Root evidence: `pnpm verify` passes，including repository guidance `30` ADRs、Screen Registry `210` records、root Vitest `7` files / `140` tests and all workspace checks；forced Turbo verification passes `25 / 25` with `0` cached tasks。
- Lifecycle / graph evidence: PostgreSQL and root environment verification pass with cleanup；the build dry-run reports the exact `5` workspace packages and accepted UI dependency edges；`pnpm list --depth 0` reports `45` packages in `6` projects。
- Integrity evidence: both frozen installs pass；`pnpm-lock.yaml` remains byte-identical at `d335cbf1484efc657ff04772d4c775ded92355fd0b52a558e73d67cc3149e547`；JSON / YAML parse、single-workflow、diff and Canonical Evidence hash reviews pass。
- Local execution note: inherited Windows `TMP` / `TEMP` values were overridden with `/tmp` for WSL consistency；Docker、loopback-listener and pnpm-store checks required execution outside the restricted Codex sandbox。These are environment permissions, not skipped repository checks。
- Publication authority: one commit、push and Draft PR are explicitly authorized；Ready、merge、deploy and Provider actions remain prohibited without a later explicit Owner authorization。
