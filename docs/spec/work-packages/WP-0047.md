# WP-0047 — React Router 8.3.0 Security Remediation

## Identity and authority

- Work Package: `WP-0047`
- Type: dependency-security remediation inserted before resuming `WP-0044`
- Priority: `P0 / High advisory remediation`
- Owner role: Dependency Governance Owner with Frontend Engineering and Security review
- Exact readiness baseline: `origin/main@3f3af5dcee496997bf0ee842ee24726bb3e614b4`
- Readiness branch: `codex/wp-0047-readiness`
- Readiness squash integration: `5bbe3e0f3f76ec617249b2a5f1aceff48b8f9319`
- Exact implementation baseline: `origin/main@5bbe3e0f3f76ec617249b2a5f1aceff48b8f9319`
- Implementation branch: `codex/wp-0047`
- Canonical authority: Handoff Section 0 decision precedence；Section 80.1.1 / accepted `IDR-0018`
  dependency governance；Sections 86.5–86.6 / accepted `IDR-0021` frontend foundation；Sections
  89–91 repository execution and GitHub Free solo governance；accepted `WP-0004`
  application-skeleton frontend baseline.
- External security evidence: GitHub-reviewed advisory `GHSA-qwww-vcr4-c8h2`, High / CVSS
  7.1；affected `react-router >=7.12.0 <8.3.0`；patched version `8.3.0`；published and reviewed by
  GitHub on `2026-07-24`.
- Explicit Owner authorization: `2026-07-28` — insert an independent security WP before continuing
  WP-0044, upgrade both frontends to exact React Router `8.3.0`, synchronize the lockfile and
  authoritative version baseline, and run related verification.

The Canonical backlog contains no existing `WP-0047` identity. The Owner-authorized insertion
therefore adopts `WP-0047` as the smallest unused four-digit identifier adjacent to the current
platform sequence. This identifier does not reorder or expand the accepted meanings of WP-0043–0046
or WP-0100+.

## Trigger and predecessor state

WP-0044 implementation started from exact `origin/main@3f3af5dcee496997bf0ee842ee24726bb3e614b4`.
Its required production dependency audit found one open High advisory:

- `GHSA-qwww-vcr4-c8h2` — React Router RSC Mode CSRF Bypass Allows Action Execution Before 400
  Response;
- both `apps/customer-pwa` and `apps/merchant-web` directly pin vulnerable `react-router@8.2.0`;
- both pins and their lockfile resolution existed at the exact WP-0044 baseline and were not
  introduced by WP-0044;
- remediation requires the advisory's exact patched version `8.3.0`.

The advisory notes that exploitation requires unstable RSC APIs. That condition is not accepted as a
waiver: the repository's dependency gate rejects the open High and the Owner explicitly selected
upgrade rather than risk acceptance.

WP-0044's in-allowlist uncommitted implementation is parked in the local named stash
`wp-0044-paused-for-wp-0047-security-remediation`. The stash excludes the protected Owner file.
WP-0044 remains paused and may be restored only after WP-0047 readiness, implementation, exact-head
CI, merge, post-merge exact-main verification and cleanup complete.

## Goal and bounded outcome

Remove the known High advisory from both BOP frontend runtime dependency graphs while preserving the
accepted frontend architecture:

1. change both direct `react-router` pins from exact `8.2.0` to exact `8.3.0`;
2. regenerate only the required pnpm lockfile resolution;
3. supersede the stale React Router value in the repository-accepted WP-0004 frontend baseline;
4. prove both frontends still format, lint, typecheck, test and build;
5. prove the production dependency audit has no High-or-higher finding;
6. make no UI, route, Screen, authentication, permission, Tenant, Store, API, Worker or
   observability behavior change.

## Recommended Owner Decisions

**Status: `OWNER AUTHORIZED AND ADOPTED — 2026-07-28`.**

1. **WP identity:** use the unoccupied `WP-0047` identifier for this inserted remediation.
2. **Exact target:** pin `react-router` to exact `8.3.0`; no range, alternate router or
   opportunistic newer version.
3. **Affected applications:** update both `apps/customer-pwa` and `apps/merchant-web`; no other
   direct dependency is changed.
4. **Authoritative baseline:** update the accepted WP-0004 React Router baseline to `8.3.0` and
   record that WP-0047 security evidence supersedes its original `8.2.0` value only.
5. **Lockfile:** allow only pnpm's deterministic resolution changes required by the two exact
   manifest edits; no unrelated refresh or deduplication churn.
6. **Source behavior:** change no TS/TSX/CSS/config source unless verification proves an unavoidable
   compatibility defect; any such need is an out-of-allowlist hard stop for a new Owner decision.
7. **Security posture:** do not waive the High based on current RSC usage. The patched dependency
   and a clean High-or-higher production audit are required.
8. **Compatibility:** retain React `19.2.7`, React DOM `19.2.7`, Vite `8.1.4` and all other accepted
   frontend pins.
9. **Evidence:** use synthetic repository tests/builds and the registry/advisory record only; no
   real Customer/Employee data, production traffic or deployment evidence.
10. **Rollback:** before deployment, use a normal reviewed Git revert. Reintroducing `8.2.0` while
    the advisory remains open is not an acceptable steady-state rollback; a forward patched
    correction is required.

## Readiness tracked-file allowlist

- `docs/spec/README.md`
- `docs/spec/work-packages/WP-0047.md`

No other tracked file may change on the readiness branch.

## Accepted implementation tracked-file allowlist

- `docs/spec/README.md`
- `docs/spec/work-packages/WP-0004.md`
- `docs/spec/work-packages/WP-0047.md`
- `apps/customer-pwa/package.json`
- `apps/merchant-web/package.json`
- `pnpm-lock.yaml`

No wildcard path and no other tracked file is authorized. Application source, tests, configs, shared
UI, root manifest, workflow, migration, database, API, Worker, observability, authentication,
infrastructure and deployment files are excluded. Generated `dist`, coverage, cache, store, logs and
temporary files are never deliverables.

## Acceptance and evidence matrix

| Acceptance                        | Owned evidence                               | Required result                                                                           | Non-goal / boundary               |
| --------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------- |
| exact baseline and pause boundary | Git state, stash inventory, brief            | WP-0044 changes parked；exact `3f3af5d…`；Owner file unchanged                            | no mixed-WP diff                  |
| exact direct pins                 | two frontend manifests                       | both equal `8.3.0`；no range                                                              | no alternate router               |
| deterministic lock resolution     | `pnpm-lock.yaml`, two frozen installs        | both importers and package resolution equal `8.3.0`；second install is byte-stable        | no unrelated dependency refresh   |
| authoritative baseline            | WP-0004 and this brief                       | original `8.2.0` superseded only for React Router                                         | no other accepted baseline change |
| advisory remediation              | production dependency audit                  | no High-or-higher finding；`GHSA-qwww-vcr4-c8h2` absent                                   | no risk waiver                    |
| dependency compatibility          | pnpm inventory, license/engine/peer review   | one direct React Router version；accepted React/Vite pins unchanged；no incompatible peer | no framework upgrade              |
| Customer PWA regression           | package format/lint/typecheck/test/build     | all pass                                                                                  | no business page implementation   |
| Merchant Web regression           | package format/lint/typecheck/test/build     | all pass                                                                                  | no Screen/route change            |
| repository regression             | affected and root gates, exact-head Linux CI | all applicable existing checks pass；platform-only limits recorded                        | no disabled gate                  |
| scope/security/cleanup            | final diff and resource inventory            | exact allowlist；no secrets/PII/production action/generated resource                      | no deployment                     |

## Verification contract

Run from the repository root with the pinned Node and pnpm versions:

```text
test "$(node --version)" = "v24.18.0"
test "$(corepack --version)" = "0.35.0"
test "$(pnpm --version)" = "11.13.0"
pnpm install --frozen-lockfile
pnpm install --frozen-lockfile
pnpm repository-guidance:check
pnpm module-manifest:check
pnpm import-boundary:check
pnpm database-ownership:check
pnpm domain-layer-boundary:check
pnpm --filter @bop-rms/customer-pwa format:check
pnpm --filter @bop-rms/customer-pwa lint
pnpm --filter @bop-rms/customer-pwa typecheck
pnpm --filter @bop-rms/customer-pwa test
pnpm --filter @bop-rms/customer-pwa build
pnpm --filter @bop-rms/merchant-web format:check
pnpm --filter @bop-rms/merchant-web lint
pnpm --filter @bop-rms/merchant-web typecheck
pnpm --filter @bop-rms/merchant-web test
pnpm --filter @bop-rms/merchant-web build
CI=true TMPDIR=/private/tmp pnpm audit --audit-level high --prod
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm build
CI=true TMPDIR=/private/tmp pnpm verify
git diff --check
```

Also parse both changed JSON manifests, parse the lockfile, compare lockfile SHA-256 across the
second frozen install, inspect the exact dependency graph and final diff, and scan for
secret/PII/security/permission/Tenant/Store/production/generated drift. Linux-only or macOS APFS
differences are recorded accurately；exact-head pinned-Linux CI owns the complete repository
evidence.

## Risks and controls

- **Unrelated lockfile churn:** install from exact baseline with pinned pnpm, then inspect every
  changed lockfile entry.
- **Hidden compatibility break:** run both frontend package gates and full repository verification
  before Ready.
- **False mitigation claim:** require the exact patched version and clean production audit even
  though the advisory describes unstable RSC paths.
- **Authority drift:** WP-0004 changes only its two React Router `8.2.0` references and records the
  WP-0047 supersession.
- **Mixed WP state:** WP-0044 stays in the named stash；WP-0047 branches contain only their own
  allowlists.
- **Generated-resource residue:** remove build/cache/store outputs and confirm final inventory
  before merge and before restoring WP-0044.

## External Evidence and non-goals

External Evidence is limited to the public GitHub-reviewed advisory and npm package resolution. No
production deployment, browser traffic, Customer/Employee/Payment/Health data, credential, Provider,
cloud resource, real Tenant/Store fact, legal/privacy acceptance or paid service is required or
claimed.

No feature, route, Screen, UI behavior, authentication/session/CSRF design, permission, Tenant
middleware, API/Worker/observability code, database/migration, workflow or infrastructure change is
in scope.

## Hard stops

Stop on baseline or authority drift, unknown dirty state, Owner-file modification/staging, any
required tracked file outside the accepted allowlist, a different target version, unrelated lockfile
churn, incompatible peer/engine/license evidence, remaining High-or-higher production advisory,
failed required check not repairable inside scope, open Blocker/High/unresolved review thread, or
non-empty final resource inventory.

## Definition of Ready

- exact baseline and WP-0044 pause boundary: `PASS`
- Owner authorization and exact target `8.3.0`: `PASS`
- responsibility, non-goals and External Evidence: `PASS`
- decisions `1–10`: `OWNER AUTHORIZED / ADOPTED`
- readiness and implementation allowlists: `OWNER AUTHORIZED / ADOPTED`
- acceptance, verification, risk and rollback: `RECORDED`
- readiness formatting/local verification: `PASS WITH RECORDED LOCAL PLATFORM LIMIT`
- readiness PR, exact-head CI, merge and cleanup: `PASS / INTEGRATED / CLEANED`
- implementation: `LOCALLY VERIFIED / PUBLICATION PENDING`

## Readiness verification evidence

Documentation-only readiness verification on `2026-07-28` produced:

- exact branch/HEAD/main/origin/main baseline: `3f3af5dcee496997bf0ee842ee24726bb3e614b4`;
- readiness tracked diff: exactly `docs/spec/README.md` and this brief;
- protected Owner file: untracked、unstaged and unchanged at SHA-256
  `b4e7ce40a75c05397aefeca5c46789640877c45543adeb8aff3d7ac6cf844892`;
- `CI=true TMPDIR=/private/tmp pnpm install --frozen-lockfile`: `PASS`, with no lockfile change;
- focused and full Prettier、`pnpm repository-guidance:check` and `git diff --check`: `PASS`;
- direct `pnpm repository-guidance:check` without `CI=true`: `FAIL / RECORDED` before its validator
  because pnpm refused non-interactive `node_modules` coordination; the canonical `CI=true` rerun
  passed;
- first canonical-TMPDIR `pnpm verify`: `FAIL / RECORDED` after Repository Guidance、Module Manifest
  and Module Generator `16/16`; Import Boundary found the unchanged generated Eventing export target
  missing;
- existing `pnpm --filter @bop/eventing build`: `PASS`; the complete gate was restarted from the
  beginning;
- restarted canonical-TMPDIR `pnpm verify`: Repository Guidance、Module Manifest、Module Generator
  `16/16`、Import Boundary `19/19`、Database Ownership `28/28` and Domain Layer Boundary `56/56`
  passed；Migration stopped at the known macOS APFS case-fold and `/var` versus `/private/var`
  realpath boundary with `26/30`;
- continued Foundation `6/6`、Helpers `4/4`、Event Catalog parser/CLI、Format、all ten workspace
  Lint/Typecheck、Screen Registry `210` and all ten workspace builds: `PASS`;
- canonical-TMPDIR root tests: `179/183` passed；only the same four existing APFS/realpath fixtures
  failed and were not changed、skipped or represented as passes;
- exact-head pinned-Linux CI: `PENDING` until the readiness commit and Draft PR exist; it is the
  owning complete repository evidence;
- no dependency、manifest、lockfile、application source、workflow、cloud、credential、production or
  external-service mutation occurred on the readiness branch.

Readiness delivery evidence:

- readiness exact head: `aaa46553dccf46b4720a178b8b2141938b5662c3`;
- Draft/Ready PR: `#47`;
- exact-head pinned-Linux run/job: `30339661359` / `90212140486`, `SUCCESS`;
- Section 91 solo self-review: open Blocker `0`、open High `0`、reviews `0`、unresolved threads `0`;
- squash merge: `5bbe3e0f3f76ec617249b2a5f1aceff48b8f9319`;
- post-merge exact-main run/job: `30340263932` / `90214036024`, `SUCCESS`;
- local `main` synchronized；local/remote readiness branches and readiness resources removed.

## Implementation verification evidence

Local implementation verification on `2026-07-28` produced:

- exact implementation baseline: `origin/main@5bbe3e0f3f76ec617249b2a5f1aceff48b8f9319`;
- both direct manifests、both lockfile importers and the unique package resolution equal exact
  `react-router@8.3.0`;
- installed package evidence: MIT license、Node engine `>=22.22.0` and React/React DOM peers
  `>=19.2.7`, satisfied by Node `24.18.0` and accepted React/React DOM `19.2.7`;
- lockfile diff: only two importer references、one package resolution/integrity and one snapshot key
  changed from `8.2.0` to `8.3.0`; no unrelated package refresh;
- two canonical frozen installs: `PASS`; lockfile SHA-256 remained
  `2486e374b5e76bd64bd71837fdd75f394742759fba6b6c5fa62dedd408a9c754`;
- `CI=true TMPDIR=/private/tmp pnpm audit --audit-level high --prod`: `PASS` — no known
  vulnerabilities found and `GHSA-qwww-vcr4-c8h2` is absent;
- Customer PWA: format、lint、typecheck、`2/2` tests and Vite build `PASS`;
- Merchant Web: format、lint、typecheck、`1/1` test and Vite build `PASS`;
- repository guidance、Module Manifest、Module Generator `16/16`、Import Boundary
  `19/19`、Database Ownership `28/28`、Domain Layer Boundary `56/56`、Foundation
  `6/6`、Helpers `4/4`、Event Catalog、full Format/Lint/Typecheck、Screen Registry `210` and all ten
  workspace builds: `PASS`;
- canonical-TMPDIR complete verify/root tests retain the known macOS APFS/realpath boundary:
  Migration `26/30` and root tests `179/183`; the same four fixtures failed and were not changed,
  skipped or represented as passes;
- one initial root-level direct `require('./node_modules/react-router/package.json')` inventory probe
  failed because pnpm places the direct dependency under each owning app；both actual app-linked
  package manifests and the escalated pnpm inventory then confirmed exact `8.3.0`;
- the first sandboxed pnpm inventory could not open the global store SQLite index；the authorized
  read-only rerun passed and showed React/React DOM `19.2.7`, React Router `8.3.0` and Vite `8.1.4`
  for both apps;
- exact-head pinned-Linux implementation CI: `PENDING` until the implementation commit and Draft PR
  exist;
- no source、route、Screen、authentication、permission、Tenant、Store、database、workflow、cloud,
  credential、production or external-service change occurred.

## Next action

Complete the exact six-file implementation diff and cleanup, then commit、push、open a Draft PR,
wait for exact-head CI, perform Section 91 solo self-review, mark Ready, squash merge, verify
post-merge exact-main CI, synchronize main and remove the implementation branch/resources. Only then
restore WP-0044 from its named stash onto a new branch created from the newly verified exact
`origin/main`.
