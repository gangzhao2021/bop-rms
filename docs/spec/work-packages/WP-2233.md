# WP-2233 - Cart Lifecycle Authority and Stable Retry

## Authority and scope

Owner authorizes continued implementation and verified local commits. Baseline: WP-2232 58e0394;
branch codex/wp-2233-cart-lifecycle-retry. WP-1204 owns Abandon/Expire, irreversible terminal
history, exact version, current authorization/Audit and 24-hour idempotency. Composite authority
remains as recorded in the index. Existing WP-2225 retry semantics apply without changing hashes.

Exact allowlist: docs/spec/README.md, docs/spec/work-packages/WP-2233.md,
packages/rms/ordering/src/application/cart-lifecycle-command-service.ts and
packages/rms/ordering/src/tests/cart-lifecycle.test.ts. No new schema, policy, endpoint, UI or
credential protocol. Ordering remains the owner; Identity validates Session via its public contract.

## Acceptance

1. Reject wrong action authority and invalid/revoked/expired Guest Session before any repository
   reads. Reject substituted Cart identity and scope before version disclosure or mutation.
2. Delayed exact replay compares business intent at the persisted observation time, retaining the
   original hash encoding, immutable result and expiry. Current time still governs authority/Audit
   and expiry; changed action/version/Session conflicts and no duplicate commit occurs.
3. Reject malformed or inconsistent stored operation and commit responses with bounded errors,
   including substituted action/Session/scope and inconsistent terminal timestamps.
4. Add behavioral regression cases demonstrating failures before the repair and passing afterward.
   Run pinned frozen offline install, Ordering tests/lint/typecheck, complete pnpm verify,
   pnpm test:integration and pnpm secret-scan:check; inspect exact diff/LF and documentation.

Rollback is restoring the previous service; no historical record is rewritten. Local commits are
authorized; no push, main merge, deployment or external mutation. Browser/remote CI and real Store
evidence are not applicable to this bounded application-layer repair and are not claimed. Cart
creation/binding and recovery still require separate implementation; whole-project completion is
not claimed.

## Results

Acceptance 1-4 PASS locally. The first regression run reproduced 13 failures against the original
service (229 passed); after repair and two additional replay-authority/identity cases, Ordering
passed244/244 across24 files. Denied authority performs no repository read; substituted Cart
identity/scope is rejected before version disclosure. Delayed Abandon/Expire return the original
snapshot once, while expiry, changed intent and current Audit/Session checks remain enforced.
Commit verification rejects altered Guest, scope, timestamps or item content. Persisted timestamps
must agree with terminal snapshots. Existing fingerprint encoding and operation history are unchanged.

Pinned frozen offline install passed. Ordering lint/typecheck and complete CI=true pnpm verify
passed with exit0, including Cart PostgreSQL3/3, all architecture/contract/migration/permission and
Screen Registry checks, and40 successful build tasks (36 cached). The four existing no-output Turbo
warnings are unchanged. pnpm test:integration passed using its valid existing cached database task;
it is not claimed as a fresh isolated-database lifecycle run. The Cart PostgreSQL suite did run fresh
inside full verification. Secret scan2/2, final documentation format/guidance, exact four-file scope,
LF and diff reviews passed. No gate was disabled and no dependency/migration/generated drift occurred.

Security review: only synthetic fixtures, no new credentials, transport, logs, foreign private SQL,
permission grants or production facts. Identity's current Session validity precedes reads; Store,
Actor, action, operation, Audit and result integrity remain explicit. No unresolved high-severity
finding remains within this repair. Browser and fresh remote CI were not run for this application
service change; real Store/Provider evidence and whole-project completion are not claimed.

Local evidence: /private/tmp/bop-wp2233-before.log, bop-wp2233-ordering.log,
bop-wp2233-install.log, bop-wp2233-verify.log and bop-wp2233-integration.log in the same directory.
The preliminary targeted run had242 tests; the final full run includes both additional cases244/244.
Next creation/binding work must connect Identity credential preparation/activation with Ordering's
binding authority and the existing Customer Cart port, preserving the accepted recovery goal.
