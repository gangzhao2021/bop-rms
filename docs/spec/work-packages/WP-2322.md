# WP-2322 - Dining Cart Item Authority

## Authority and scope

Baseline26a9fe47cedfa68003b67be296ca7a2d9efed950; branch codex/wp-2322-dining-cart-item-authority.
Composite Sections0-97, WP-1201/1205 and accepted WP-2228 govern. Continuous local implementation
and verified local commits are authorized. Preserve the three separate AGENTS/skill document edits.
Reuse the existing Cart item command service and current Dining Cart authority guard for Add,
Update and Remove. Current Identity CSRF and Cart-purpose Dining membership precede any Cart or
operation-history access, precede actual commit again, and guard result publication. Compare the
full captured authority across awaits. This is observation-based authorization, not an atomic
cross-domain Closing write lease. No new Order/Batch permission, Host override or attribution change.

Capture closed input and audit before awaits; use a session-scoped repository factory so production
composition can use WP-2321 instead of retrieving foreign-session restricted aggregates. Require
scope validation on current and historical results. Preserve version/idempotency/lifecycle checks,
own-participant edits, original-operation recovery and bounded failure after a possibly committed
effect. Return only minimal internal receipts, never restricted Cart aggregates or credentials.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2322.md
- packages/rms/ordering/src/application/dining-cart-item-service.ts
- packages/rms/ordering/src/tests/dining-cart-item-service.test.ts
- packages/rms/ordering/src/index.ts

## Acceptance and verification

Test Add/Update/Remove, foreign participant denial, CSRF/current membership before reads/replay,
authority changes during Catalog before commit, postcommit denial and original-operation recovery,
version conflict, immutable attribution/input/audit, bounded errors and foreign result rejection.
Fresh Ordering format/lint/typecheck/test/build, API dependent typecheck, import/domain boundaries,
guidance and secret checks. The implementation is application-only; SQL/migrations remain exactly
WP-2321 and its recorded Cart PostgreSQL9/9 still covers those unchanged adapters. New service
PostgreSQL/API composition will be verified when actually wired, not claimed here. Full shared Cart
browser journey remains the named pnpm verify plus pnpm test:integration milestone, without default
cache bypass. Broaden for concrete unresolved cross-module risk. Reuse frozen installation only
after confirming same manifests/lockfile/pins and installation as WP-2321 install log (41 projects,
Node24.18.0/pnpm11.13.0, unchanged installed dependency tree); otherwise run fresh frozen install.
Final exact-file review, credentials/permissions/error minimization and no unrelated staged files.

## Observed results

PASS for the application-only scope. Add/Update/Remove reuse the existing command service, with
frozen current authority before owner reads, another full check around historical lookup and commit,
and final publication checks. Current lifecycle is checked again immediately before commit after
Catalog waits. Saved history is captured before post-effect callbacks and matched to Cart, Guest,
session, participant attribution and timestamps. Credentials never enter the repository or receipt.

Fresh Ordering unit run passes816/816 across40 files (20 new cases). Typecheck then identified a
readonly synthetic test variable and TypeScript closure narrowing of the receipt-expiry holder;
both were corrected without disabling checks. Lint identified seven non-null assertions in the new
tests; explicit fixture-presence checks replaced them. The affected20/20 tests pass again on final
code; the other796 unchanged tests retain the successful full unit-run evidence, not a new run.
Final Ordering lint/build (the same tsc project also covers typecheck), dependent API typecheck,
Ordering format and new-file formatting pass. Import22, Domain57, guidance33 ADRs/5 skills and
secret2 pass. No useful running check was restarted and no full uncached regression was repeated.

Commands/results are recorded in /private/tmp/bop-wp2322-{unit,type,lint,build,api-type,gates,
focused-final}.log, using the WP-2321 pinned setup. The successful full unit run tested baseline
26a9fe4 plus the six WP files before receipt-holder/test-fixture cleanup; final20/20, build and lint
cover those later edits. The original type/lint failures above are disclosed even though their
log paths now contain the successful reruns. Final docs-format and exact-file diff review complete
closeout. The separate AGENTS/skill edits remain outside the business commit.

Reused install evidence: WP-2321 /private/tmp/bop-wp2321-install.log, pnpm install --offline
--frozen-lockfile --store-dir /private/tmp/bop-rms-wp2288-dependency-cache/v11, exit0/41 projects,
tested5271460 plus its seven WP edits subsequently committed26a9fe4. The same local installation,
Node24.18.0/pnpm11.13.0 and manifests/lockfile/pins remain unchanged; no install ran in WP-2322.
WP-2321's exec vitest run --config packages/database/vitest.cart-aggregate.config.ts, exit0/9 tests,
tested that same revision plus its listed edits, remains evidence only for unchanged SQL adapters.
No PostgreSQL test of this new service is claimed. No SQL/migration or installed dependency changed.
No full pnpm verify/test:integration, API runtime suite or PWA suite ran in this application-only WP.

Security review finds no unresolved blocker in this inactive application scope. A production
repository factory must restrict aggregate reads to the authorized session and operation snapshots
to the exact Cart/Guest before retrieval; checked return values are defense in depth. The next
composition WP must supply and verify this history restriction before activation. Current authority
checks remain observations, not atomic cross-domain Closing fences. No API/UI activation, real
membership/provider claims, schema/dependency changes, remote operations or deployment. Rollback
removes the additive service before composition; no stored facts change. Follow-up is real owner-store
and API composition, then browser journey and milestone regression.
