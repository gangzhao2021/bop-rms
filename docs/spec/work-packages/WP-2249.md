# WP-2249 - Bound Complete Customer Cart Responses

## Authority and scope

Owner authorizes sustained bounded implementation and verified local commits. Baselined3ed69a,
branch codex/wp-2249-cart-response. Composite Sections0–97, WP-1205/1702/2248 preserve finite
foreground requests, private network-only Cart state, exact operation retry and unknown outcomes.

The shared boundedFetch helper releases its timer after response headers. Cart then awaits JSON
without a deadline. Replace only Cart transport's use with a single15-second deadline covering
headers and full streamed JSON, independent of whether a fetch/reader cooperates with abort.
Retain caller cancellation throughout. Reject redirects and send no referrer while retaining fixed
relative same-origin endpoints, cors Fetch Metadata, no-store and same-origin credentials.

Bound actual decoded response bytes to16MiB (technical memory ceiling, not Cart business policy),
validate UTF-8 before JSON and cancel/release the reader on every outcome. The ceiling accommodates
existing API100-item/50-option responses with bounded names/notes/warnings; test a maximal structural
Cart. Late results after timeout/cancellation cannot parse an error or clear current CSRF.
No timer/listener/reader remains owned after settlement, and no cancellation queues/replays intent.

Preserve WP-2248 mapping: transport ambiguity is network_unknown, malformed/unavailable writes
are unknown, definitive matching server errors retain their existing meaning, and malformed reads
are unavailable. The controller retains the exact original mutation for explicit retry. No new
public DTO, route, field, persistent state, Domain rule, dependency, migration or shared-helper
behavior. CUST-CART and contextual CUST-SELLABLE-CONFIGURE retain their existing visible states,
permissions, disabled controls, accessibility and layout. No visual redesign or browser sign-off.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2249.md
- docs/spec/design/business-scenario-coverage.md
- apps/customer-pwa/src/cart/cart-client.ts
- apps/customer-pwa/src/cart/cart-client.test.ts

## Acceptance and verification

1. The existing15-second ceiling bounds headers and body including abort-insensitive fetch/read;
   caller cancellation before/during headers/body does not wait for that ceiling or mutate CSRF.
2. Oversized, invalid UTF-8, truncated/malformed and redirected responses fail boundedly. Valid
   maximal structural Cart and matching server errors retain behavior. Read/write uncertainty
   mapping remains exact and no automatic request follows any failure.
3. Timers, caller listeners and body readers clean up on success, rejection, timeout/cancellation;
   late header/body results cannot clear credentials or publish success. All data stays in memory.
4. Pinned frozen install, affected format/lint/typecheck/unit/build, pwa-security:acceptance, full
   pnpm verify, forced uncached pnpm test:integration --force, Secret/privacy and final5-file
   scope/LF/diff review pass. Existing browser demo is not real transport evidence and unchanged.

Rollback reverts only this transport. No main merge, push, deployment, external action or real Store/
Provider evidence is inferred. Cart write composition, page coordinator and reload recovery remain
separate implementation work.

## Final local verification

Acceptance1–3 PASS: PWA300/300 across29 files, affected format/lint/typecheck/build and
pwa-security:acceptance pass. Deadline tests cover abort-insensitive headers and stalled bodies;
caller cancellation before headers, during headers and during body removes listeners/timers. Late
header and timeout-completed Session-error bodies cannot clear CSRF. Oversize/invalid UTF-8 and
redirected responses reject with the established read/write error mapping; a100-item/50-option
Cart with bounded maximum names/notes/warnings parses successfully. Success, known rejection and
failure release body readers. Request metadata retains same-origin/no-store, rejects redirects and
suppresses referrers. No automatic retry or private persistence was introduced.

Acceptance4 PASS: pinned frozen install, full CI=true pnpm verify (40 successful builds), forced
uncached CI=true pnpm test:integration --force (cache bypassbe6f420caf4fd37e), Secret2/2 and exact
5-file allowlist/LF/privacy/final diff review pass. All checks passed on their first attempt in this
WP. Database acceptance includes isolation, migration, failure injection, timeout, signal, redaction
and owned cleanup. No dependency/migration/shared-helper or generated tracked file changed.

Logs under /private/tmp: bop-wp2249-install.log, bop-wp2249-pwa.log, bop-wp2249-format.log,
bop-wp2249-build.log, bop-wp2249-security.log, bop-wp2249-verify.log,
bop-wp2249-integration.log and bop-wp2249-secret.log. Transport evidence is synthetic unit/stream
evidence; the unchanged preview browser suite is not rerun or claimed as new live transport evidence.

All criteria pass locally. Local commit only; no main merge, push, deploy or remote CI claim.
Pickup creation coordinator, full item writes and general reload recovery remain unfinished.
