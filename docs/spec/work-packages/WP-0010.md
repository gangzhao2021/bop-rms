# WP-0010 — Establish Module Manifest Schema

## Identity and source

- Work Package: `WP-0010`
- Epic: `EPIC-00 / F00.2 Architecture Enforcement`
- Priority: `P0`
- Owner role: Architecture / Platform Engineer
- Repository baseline: `main@6d8e7aa415edc434a82dcc4bf4c4f37ab8694b8b`
- Corrective baseline: `main@b921321d6adf579b5c0e1b9e4d9636efbfb28adf`
- Canonical source: `BOP-RMS Complete Handoff Package.md`, document `BOP-RMS-HANDOFF` version `0.5.3`, architecture baseline `v1.0`
- Explicit user authorization: 2026-07-16, including the Canonical Module Identity Naming corrective follow-up

The original bounded decisions are Handoff Sections 0, 48.1–48.2, 54.4 F00.2, 54.7, 89.3–89.6, 90.1–90.5, 90.11–90.17, and 91. The corrective brief additionally resolves Sections 48.3–48.8 and 56.4–56.5 against Section 48.2 at the exact corrective baseline above. Sections 48.6 and 48.8 distinguish a logical Module Name from Package Name, while Section 56.5 fixes canonical package namespaces as `@bop/<kebab-case>` and `@rms/<kebab-case>`. Sections 89–91 govern repository, verification, and solo GitHub execution. No authoritative version drift was observed: the external Canonical Handoff remains `0.5.3` with architecture baseline `v1.0`.

## Goal

Establish a type-safe, runtime-verifiable `module.manifest.ts` contract and deterministic fixtures/tests for the declarations required by Section 48.2. Connect the finite validator to root verification and the unique `bootstrap / verify` workflow without creating a business Module or implementing the later architecture-enforcement Work Packages.

The corrective goal is to replace the merged `@bop-rms/*` identity assumption with the uniquely resolved Canonical contract: Module Name is an unscoped kebab-case logical name; Package Name is the separate `@bop/<moduleName>` or `@rms/<moduleName>` identity; Layer selects and must match that namespace; and synchronous dependency references declare and obey the same logical name, package identity, and layer relationship. This correction must preserve all original WP-0010 fields and safety checks.

## Scope and files

- `tooling/module-manifest/`: the TypeScript contract, JSON Schema, deterministic validator/CLI, synthetic fixtures, and tests.
- `package.json` and the unique `.github/workflows/bootstrap.yml`: a root verification entry and CI invocation for the committed fixtures and validator.
- `docs/spec/README.md`, root `README.md`, and this brief: bounded repository status and usage guidance.

No business package, Domain behavior, module generator, import-boundary scan, database ownership scan, Domain-layer ORM scan, migration, schema, table, ORM model, seed, fixture framework, database connection, Repository implementation, Provider, production infrastructure, deployment, or Figma change is in scope.

## Manifest contract

Each future business Module root will own a `module.manifest.ts` that uses the tooling contract and declares:

- an unscoped kebab-case logical Module Name;
- a separate Package Name exactly equal to `@bop/<moduleName>` or `@rms/<moduleName>`, matching the `BOP | RMS` Layer;
- `Phase 0 | Phase 1 | Later` lifecycle;
- public export subpaths and allowed synchronous Module dependencies;
- consumed and published event contract names;
- future database schema/table ownership declarations only;
- owned jobs, feature flags, kill switches, PII classification, and an accountable owner role.

Allowed synchronous dependencies carry their own logical Module Name, canonical Package Name, and Layer. Those values must agree, dependency Package Names form a deterministic set, a Module cannot depend on its own Package Name, and a BOP Module cannot declare an RMS synchronous dependency. Other arrays are also deterministic sets: duplicates are invalid. Public exports must be package export subpaths and cannot expose source, private, internal, Domain, infrastructure, persistence, adapter, ORM, or database paths. Data classification is an explicit closed vocabulary with safe handling declarations; `none` cannot be combined with another class, and sensitive classes cannot be declared safe for logs, URLs, analytics, or non-synthetic fixtures.

Owned database fields are metadata for future ownership enforcement only. Validation never connects to a database and creates no schema, table, migration, model, seed, or Repository.

## Verification and acceptance evidence

Run from the dedicated WSL worktree root:

```bash
test "$(node --version)" = "v24.18.0"
test "$(corepack --version)" = "0.35.0"
test "$(pnpm --version)" = "11.13.0"
test "$(pnpm exec turbo --version)" = "2.10.5"
pnpm install --frozen-lockfile
pnpm install --frozen-lockfile
pnpm module-manifest:check
pnpm format:check
pnpm lint
pnpm typecheck
pnpm screen-registry:check
pnpm test
pnpm build
./tooling/postgres/verify-local.sh
pnpm environment:verify
pnpm list --depth 0
pnpm exec turbo run build --dry=json
git diff --check
```

Also prove the second frozen install leaves `pnpm-lock.yaml` unchanged, parse every changed JSON/YAML file, run the affected tooling workspace uncached, inspect the unique CI workflow, and review the final diff/tracked files for secrets, PII, real Store/Provider/owner facts, generated artifacts, migration/schema/table/ORM/seed/database integration, business Module or vertical-slice code, production/deploy/Figma changes, or WP-0011–0014 scope.

1. **Contract completeness — PASS evidence required:** typecheck and validator cover every Section 48.2 field with closed layer/lifecycle/classification vocabularies.
2. **Fixture coverage — PASS evidence required:** minimal legal and boundary legal manifests validate; committed invalid cases deterministically fail for missing fields, invalid Module/Package naming, Layer/namespace mismatch, invalid dependency identity, duplicates, self-dependency, BOP-to-RMS dependency, private exports, and unsafe classification.
3. **Type-safe authoring — PASS evidence required:** `defineModuleManifest` preserves literal types and compile-time negative cases are asserted without weakening runtime validation.
4. **Deterministic root/CI integration — PASS evidence required:** `pnpm module-manifest:check`, root `pnpm verify`, and the sole `bootstrap / verify` workflow invoke the same finite check.
5. **Persistence boundary — PASS evidence required:** database declarations are inert metadata only; no migration, real schema/table, ORM, driver, connection, seed, Repository, or database fixture framework exists in the diff.
6. **Existing baseline remains healthy — PASS evidence required:** frozen install, format, lint, typecheck, Screen Registry, tests, builds, PostgreSQL lifecycle, five-service environment lifecycle, workspace graph, and diff checks pass.
7. **Governance — PASS evidence required:** explicit self-review and successful owning CI are recorded without claiming independent approval or server-side branch protection; Ready and squash merge use a freshly fetched exact expected head.

## Risks, rollback, and boundaries

- Risk: a contract that is too permissive would defer unsafe declarations to later WPs. Mitigation: closed vocabularies plus schema and semantic validation with negative fixtures.
- Risk: this WP could accidentally become WP-0012 or WP-0013. Mitigation: validate only supplied/committed Manifest values; do not scan imports, module directories, database catalogs, migrations, or ORM code.
- Rollback: normal reviewed Git revert. There is no data, Provider, deployment, or Figma rollback.
- External Evidence: real Module owners, actual schemas/tables, business events/jobs/flags, Provider/account/legal/Store facts, production readiness, and future architecture-test results remain later-WP or external gates.
- Next allowed Work Package: `WP-0011` only after WP-0010 is integrated and separately authorized.

## Status

- Specification: `Bounded for WP-0010 Canonical Module Identity Naming corrective execution`
- Corrective baseline preflight: `PASS` for exact local/fresh `origin/main`/authorized baseline `b921321d6adf579b5c0e1b9e4d9636efbfb28adf`, WP-0010 squash merge and main push CI, dedicated WSL worktree, Canonical Handoff `0.5.3`, and authenticated GitHub CLI.
- Protected external evidence: the main worktree retains untracked `BOP-RMS Complete Handoff Package.md` and `BOP-RMS Complete Handoff Package.md:Zone.Identifier`; this follow-up must not move, modify, delete, or commit them.
- Corrective implementation: `Complete and locally verified` at `fb6c084cc08f7d091ad85539574a28cbd1f3274f` on `codex/wp-0010-manifest-module-identity-correction`.
- Corrective local verification: `PASS` for exact Node/Corepack/pnpm/Turbo pins, two frozen installs with unchanged lockfile hash `d335cbf1484efc657ff04772d4c775ded92355fd0b52a558e73d67cc3149e547`, 2 legal and 15 rejected Manifest fixtures, 18 focused and 24 root tests, repository guidance, format, lint, typecheck, 210-screen Registry, builds, 25/25 forced uncached workspace tasks, PostgreSQL lifecycle, five-service environment lifecycle, 19 JSON and 10 YAML parses, 45-package/6-project workspace graph, unique workflow, diff, Docker cleanup, secret/PII and scope review.
- Corrective GitHub CI: `PASS` on Draft PR #9 for `bootstrap / verify` run `29538809749`, job `87756326891`, at head `cfa96b02195feb2cae0ef25a7f239ce59a889e8a`; the evidence-only head that records this result must pass the same check before Ready.
- Original WP-0010 implementation: `Integrated` at squash commit `b921321d6adf579b5c0e1b9e4d9636efbfb28adf`; prior implementation evidence remains below for traceability.
- Implementation: `Complete and locally verified` at commit `2fe59c5c485a6daefcd0e89e03ca3dcb0502eb87`.
- Local verification: `PASS` for exact Node/Corepack/pnpm/Turbo pins, two frozen installs with unchanged lockfile hash, 2 legal and 8 rejected Manifest fixtures, 11 focused and 17 root tests, repository guidance, format, lint, typecheck, 210-screen Registry, builds, 25/25 forced uncached workspace tasks, PostgreSQL lifecycle, five-service environment lifecycle, JSON/YAML parsing, workspace/build graph, unique workflow, diff, and cleanup checks.
- GitHub CI: `PASS` on Draft PR #8 for `bootstrap / verify` run `29536116645`, job `87747736232`, at implementation head `2fe59c5c485a6daefcd0e89e03ca3dcb0502eb87`; this evidence-only head must pass the same check before Ready.
- Governance: explicit Process-enforced / GitHub Free solo self-review recorded on PR #8 for the exact implementation head; no independent approval or server-side branch protection is claimed.
- External actions: authorized for commit, push, Draft PR, WP-scoped CI repair, Ready transition, exact-expected-head squash merge, post-merge `main` CI verification, and main-worktree fast-forward.
