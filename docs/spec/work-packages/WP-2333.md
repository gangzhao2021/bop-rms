# WP-2333 - Closed Catalog Selection Collections

Baseline1d7ac8b; branch codex/wp-2333-catalog-selection-boundary. Owner authorizes sustained local
implementation and verified commits. Composite Sections0-97, WP-1202 and WP-2332 retain Catalog
selection ownership, exact scope/time/version evidence and immutable Ordering replay. Preserve the
three separate guidance edits. Current display snapshots lack conditional activation data and are
not an accepted replacement for current selection/commercial source producers.

## Scope and acceptance

The selection validator invokes map on caller/provider arrays and coerces channel/order type with
String. Close selection, rule, option, activation and conflict arrays before processing: ordinary
arrays only, dense own enumerable data elements, bounded length, no extra/symbol properties or
accessors. Copy values before validation and freeze parsed collections. Reject non-string channel
and order-type values without coercion. Existing closed records and domain parsers remain owners
of exact identifiers, quantities, scopes and graph rules. No new DTO, price, SQL, policy or schema.

Tests must reproduce custom methods/accessors/sparse arrays and invalid prototypes at every
collection boundary without invoking hooks, deny before source lookup for malformed caller input,
retain conditional/conflict/quantity behavior and prove input ownership across source awaits.
No credentials or personal data are introduced. No real menu readiness or producer implementation
is claimed. Malformed input/snapshot keeps existing CATALOG_INPUT_INVALID; dependency exceptions
retain CATALOG_DEPENDENCY_UNAVAILABLE. No shared parser refactor beyond this service.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2333.md
- packages/rms/catalog/src/application/selection-validation-service.ts
- packages/rms/catalog/src/tests/selection-validation.test.ts
- packages/rms/ordering/src/tests/cart-item-command.test.ts

## Verification plan

First run targeted new tests against original implementation, then after repair. Run Catalog full
unit/lint/typecheck/build, Ordering full test/typecheck, API typecheck, changed-file Prettier,
import-boundary:check, domain-layer-boundary:check, repository-guidance:check, secret-scan:check
and exact diff/LF review. Public selection boundary warrants full Catalog/Ordering regression.
Reuse WP-2332 actual Cart PostgreSQL9/9 and API571/571 from /private/tmp/bop-wp2332-{cart,api}.log
at093da89 plus its four files: persistence/HTTP source, fixtures and dependencies are unchanged;
those suites use their own synthetic selection ports, so do not prove this parser. Add a pure actual Catalog-validator/Ordering-command composition scenario using explicit synthetic
source snapshots; cover accepted evidence, denied update, hostile provider hooks and original replay.
Run Ordering lint as well. This is not a complete operational journey or a new DB/HTTP behavior claim. Same WP-2331 frozen installation, manifests/lock/pins and Node24.18.0 /
pnpm11.13.0 remain unchanged. Full pnpm verify and pnpm test:integration remain the complete
cross-domain journey/release milestone. Historical WP-1202 full integration gates belong to its
original integration; current guidance permits this bounded affected-check repair.
No browser/schema/remote/deployment work. Rollback is an application forward fix only.

## Results

PASS: five collection boundaries reject custom methods, indexed getters, holes, additional string
or symbol properties and nonstandard prototypes using copied dense data elements. Channel/order
values must be strings before membership checks. Selection quantities are copied before source
awaits. Existing conditional activation, conflicts, scope/time, version and quantity decisions remain
unchanged. No production source, policy, sensitive data or private-table access was introduced.
The actual Catalog validator/Ordering composition test proves exact persisted validation evidence,
invalid Update denial, hostile provider denial without hook execution, and original Add replay with
one allocation/operation. Its menu/rules remain explicitly synthetic public-port facts.

Fresh Catalog117/117 (13 files) and Ordering879/879 (40 files) pass. Selection coverage includes
30 hostile collection combinations plus noncoercion and source-await ownership checks. Fresh
Catalog lint/typecheck/build, Ordering lint/typecheck, API typecheck, changed-file Prettier,
import22/22, Domain57/57, guidance and secret2/2 pass. Tested1d7ac8b plus this five-file WP and
preserved three guidance edits. Logs: /private/tmp/bop-wp2333-{red,catalog-final,ordering-final,
composition,lint,type,build,ordering-lint,ordering-type,api-type,format,import-boundary,
domain-layer-boundary,repository-guidance,secret-scan}.log.

Expected original-code regression:7 new test groups failed and8 passed. The first repair left the
activation/conflict reference helper unchanged;2 groups still failed until that helper used the
same collection parser. The new composition fixture initially supplied an extra observedAt field;
the closed source parser correctly rejected it. The fixture now enumerates only snapshot fields,
and the final entire Ordering run passes. No production check or business rule was relaxed.
Final scope, LF and privacy diff review pass. No resources were created. Frozen install and
unchanged PostgreSQL/API evidence are reused only as detailed above; no fresh DB/HTTP/browser,
full verify, generic integration, remote CI or deployment is claimed. Source-ready Catalog producer
work still requires authoritative current availability and complete conditional rule publication;
display snapshots and synthetic fixtures are not substituted as production authority.
