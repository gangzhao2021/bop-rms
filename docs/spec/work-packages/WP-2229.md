# WP-2229 - Cart Item Authority Before Persistence

## Authority and readiness

Owner authorized continued completion and local commits after verification. Composite Handoff
0.5.3 Sections 0-91 plus accepted Sections 92-97 remain authoritative. WP-1003 Session validity,
WP-1201 exact Cart/Guest/Participant scope and WP-2225 stable retry behavior govern this repair.
The accepted shared Dining Cart choice does not grant foreign-item mutation or bypass authorization.

Baseline: WP-2225 `d425bc9`, branch `codex/wp-2229-cart-authority`. Other verified packages remain
on separate local branches; no merge or remote action is performed. DoR passes for this bounded
existing-contract repair. This package neither invents nor activates the unresolved recovery protocol.

## Exact allowlist

- `docs/spec/README.md`
- `docs/spec/work-packages/WP-2229.md`
- `packages/rms/ordering/src/application/cart-item-command-service.ts`
- `packages/rms/ordering/src/tests/cart-item-command.test.ts`

## Implementation and acceptance

1. Add, Update and Remove ask the existing authorization port before any Cart or operation read.
   Denied, expired or malformed Session evidence cannot reach persistence or Catalog.
2. Check returned Cart identity against the requested Cart before using its contents, version or
   audit context. A mismatched dependency result fails with the bounded dependency error.
3. Retain current exact scope, Participant item ownership, Audit, expected-version, operation
   fingerprint, immutable history and delayed-retry behavior. No new permissions or public input.
4. Tests prove denied/expired evidence causes no persistence reads, valid calls preserve ordering,
   and substituted Cart identity fails before Catalog validation or commit. All Ordering tests pass.

Ordering remains the fact owner. Identity owns Session evidence through its public contract.
Authorization is necessary before source access; object scope and Audit remain validated after the
exact Cart is loaded. No schema, UI, HTTP, dependency, credentials, pricing or external evidence.
Rollback reverts this bounded service repair without rewriting persisted records.

## Verification plan

Use exact Node 24.18.0 / pnpm 11.13.0 and existing root commands: frozen offline install,
`pnpm --filter @rms/ordering test`, lint, typecheck and build; `pnpm ordering-cart:acceptance`;
`pnpm verify`; `pnpm secret-scan:check`; final allowlist/whitespace/privacy review including new brief.
Full verification owns architecture, contracts, migrations, integration and build checks. Browser
acceptance is not required for this Domain-only repair. Remote CI, merge and deployment not claimed.

## Results

Acceptance 1-4 PASS. Frozen offline install, Ordering lint/typecheck/build, Ordering 192/192
tests in 22 files, Cart isolated PostgreSQL 2/2, complete `CI=true pnpm verify` (exit 0),
and secret scan 2/2 passed. Full verification ran all configured architecture, contract, migration,
format/lint/typecheck, unit/integration and build gates. Final four-file scope, LF, whitespace and
supplemental secret-pattern inspection passed, including the new brief.

The first ten added regressions failed against the original implementation while all 176 existing
cases passed. After repair and six additional malformed-Session/dependency-failure cases, all 192
passed. An initial test non-null assertion failed lint; it was replaced with explicit presence
checks and lint/typecheck/full verification passed. No gate was disabled.

Security review: no source read occurs after denied, expired or malformed Session evidence; exact
Cart identity is validated before content use. Existing scope, Participant ownership and stable
retry assertions pass. No credentials, PII, permission, history, migration or external-service
changes. Browser tests and fresh remote CI were not run for this Domain-only repair.
