# WP-2290 - Atomic Dining Admission Consumption Store

## Authority and readiness

Baseline9f235d8a7127fb870a81c5f55fe8df296a11fadf; branch codex/wp-2290-dining-admission-store.
Continuous bounded local implementation and verified local commits remain authorized.
Composite Sections0-97, WP-1006 rules14,16-21, WP-2278 immutable Guest-bound Join,
WP-2282/2284 Closing/Move serialization and WP-2288/2289 consumption protocol govern.

## Scope and acceptance

Implement the Dining-owned PostgreSQL adapter for WP-2289, binding Tenant/Brand/Store on every
query. A single coherent read returns current Session/Participant/admission/current Table facts
plus immutable original Join and its Guest reference. Parse complete records and derive current
Table eligibility without confusing Table aggregate version with Session assignment version.

Validate complete consume commands, original Guest/Join digest, consumption digest and WP-2288
transition before writes. Lock scoped consumption operation, shared Table advisory and Table row,
Session, Participant and admission in established order. Re-read complete current facts under locks;
current Session/Participant/Table facts and immutable original Join must match the captured command.
Original writer retries also require eligible current Dining facts. Same operation converges on
its original immutable receipt; another operation cannot consume the admission again.

Atomically change Active1 admission to Consumed2, insert scoped immutable operation history and
append validated SystemRestricted public Audit. Preserve original Join operation/hash and history.
Explicit action is DINING_IDENTITY_ADMISSION_CONSUME; target DiningIdentityAdmission, reason
AUTHORIZED_DINING_ADMISSION_CONSUMPTION, source CUSTOMER_PWA, classification Restricted.
No Guest identifier is cast to a User or Service Actor. Audit failure/read-only/unknown commit
acknowledgement must retain atomicity and recover only the original result.

Migration1001_008 creates dining_admission_consumption_operation with strict closed record shape,
scoped identity constraints, admission uniqueness, and foreign keys to the exact admission and
original Guest/Join operation. Supporting unique indexes preserve existing immutable records.
History has forced scope RLS, PUBLIC denial and append-only rules. No applied migration is edited.
The catalog becomes99 migrations in23 namespaces. Narrow owner-asset admission requires the exact
Dining owner/schema/tables and still denies driver access.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2290.md
- package.json
- migrations/1001-rms-dining/1001_008_create_dining_admission_consumption.sql
- packages/rms/dining/src/infrastructure/persistence/database-access.manifest.ts
- packages/rms/dining/src/module.manifest.ts
- packages/rms/dining/src/index.ts
- packages/rms/dining/src/infrastructure/persistence/dining-admission-consumption-store.ts
- packages/rms/dining/src/tests/dining-admission-consumption-store.test.ts
- packages/database/vitest.dining-admission-consumption.config.ts
- packages/database/test/dining-admission-consumption-store-acceptance.test.mjs
- packages/database/src/catalog.test.ts
- packages/database/test/isolated-database-acceptance.mjs
- tooling/database-ownership/validate.mjs
- tooling/database-ownership/validate.test.mjs

## Verification and boundaries

Frozen offline install with the external cache; affected format/lint/type/unit, ownership/permission,
migration/catalog and actual new PostgreSQL acceptance; full pnpm verify; sequential forced
pnpm test:integration --force; final guidance/Secret/exact16-file LF/diff/security review.
Tests prove actual Start/Join/consume/recovery, current Guest binding, same/different-operation races,
Audit rollback/read-only/lost acknowledgement, current Closing/Move and pending-write fences,
foreign Tenant/Store/RLS/original Guest FK, immutable history, operation/Audit counts and cleanup.
No skipped check is a pass. Rollback is a forward fix preserving applied history.
No Identity rotation/evidence, public Table mapping, TTL, shared Cart, HTTP/UI activation, live
Store/Provider facts, permission change, dependency upgrade, main merge, push or deployment.
Implementation and required verification passed locally.

The allowlist includes the required database governance manifest for the new owner table.
Initial full verification correctly rejected its missing governance record; it is supplied with
append-only ownership, audit-security retention and indirect identifier/credential classification.

## Verification evidence

- Frozen offline install passed using the existing external dependency cache; lockfile unchanged.
- Dining lint/typecheck and785/785 unit tests across18 files passed, including25 new writer cases.
- Actual isolated PostgreSQL consumption acceptance passed; full Dining acceptance is9/9.
- Full pnpm verify passed, including ownership156/156, migration/catalog, permissions,
  architecture, format/lint/typecheck, all test suites and40/40 builds.
- Forced pnpm test:integration --force passed with cache bypass ab0a40e372056380,
  zero cached tasks,1m59.031s:99 migrations,23 namespaces, isolation, injected failures,
  signal/timeout, redaction and owned cleanup.
- Initial PostgreSQL fixture needed participant UPDATE privilege for its SELECT FOR UPDATE;
  the foreign-scope insert test was corrected to mismatch row scope and context rather than
  testing a matching foreign scope rejected by its FK. No production permission was changed.
- An initial format invocation without CI stopped at pnpm's no-TTY guard; the corrected
  CI invocation passed. No bypass, dependency version change or lockfile change was used.
- Original Join remains immutable; same-operation concurrent writes converge, different
  operations cannot consume twice, Audit failure/read-only roll back, lost acknowledgements
  recover, and Closing/Move block pending commands and original retries.
- Tenant/Store queries, forced RLS, exact Guest FK, closed JSON constraints and append-only
  history have actual PostgreSQL evidence. No external operational readiness is claimed.
- Final guidance and Secret2/2 passed; exact16-file LF/diff review passed; isolated
  containers, volumes and roots are zero. Main remains cedc44c9b6a6a1b389f77342078cc6f2d540dabe.
