# WP-2327 - Shared Table Browser Proof

Baseline08ee1748b2975893eae9113fc60290746091c21c; branch codex/wp-2327-shared-table-browser-proof.
Composite Sections0-97, WP-2228 shared Cart/own-participant edits, WP-2277 current Join regeneration,
WP-2320 foreign controls and WP-2326 runtime binding govern. Local implementation, isolated tests
and verified local commits are authorized. Preserve the three separate agent-guidance edits.

## Scope and allowlist

Extend the existing isolated browser lab with two simultaneous contexts on one DiningSession.
Issue the second invitation through the actual current-version regeneration service/store after the
first invitation is consumed. Never reuse consumed credentials or mutate business tables directly.
Use existing UI to add both participants' lines, verify foreign controls and notes, refresh views,
edit only own lines and remove both. Verify shared persistence, exact versions and Audit counts.
Existing synthetic Staff/QR/abuse/selection/lifecycle fixtures remain explicitly synthetic.

- docs/spec/README.md
- docs/spec/work-packages/WP-2327.md
- apps/customer-pwa/scripts/local-customer-lab.mjs
- packages/database/test-support/local-customer-dining-seed.mjs
- packages/database/test-support/local-customer-cart-seed.mjs

## Verification

Run customer-lab:acceptance against real isolated stores/HTTP/browser, changed formatting and script
lint, import-boundary:check, repository-guidance:check and secret-scan:check. Reuse WP-2325 stage and
WP-2326 API571/571/build evidence for unchanged application/Domain source. The same valid frozen
installation and unchanged manifests/lock/pins retain WP-2321 evidence. No duplicate application
unit/build or forced full integration for this test-only extension. Broaden if it discovers an
application defect, after updating this allowlist. Next full verify plus test:integration milestone
remains another new cross-domain implementation or release/merge gate. No production, pricing,
checkout/payment, new credential mechanics, screen route or external operation. Final diff/security/
resource review required. Rollback removes only this test scenario, preserving owner history.

## Results

PASS: the fresh customer-lab:acceptance run passes1/1 in22.08s, including the existing two separate
viewport journeys and two simultaneous desktop/mobile contexts on one additional table. The second
invitation uses actual current Session/capability/generation versions and the persisted regeneration
service. Both contexts retain valid consumed-admission identity after regeneration. Both read the same
Cart; foreign controls are disabled and foreign synthetic preparation notes are absent. Own Update and
Remove work after explicit menu/Cart navigation refresh. The shared Cart reaches version6 and zero
lines. Across three Carts there are11 item operations,3 Cart creations and14 Cart Audits. The second
participant's existing Cart is found by configure-state.loadCurrent, so no extra selection write occurs.

Fresh logs: /private/tmp/bop-wp2327-shared-final.log, format.log, lint.log, import.log, guidance.log,
secret.log; static gates pass with22 import tests and2 secret tests. Tested08ee174 plus five listed
files. The initial lab.log passed only old journeys because stopAfter excluded the new scenario;
that run is not counted as shared evidence. shared-lab.log then exercised the new scenario but failed
an incorrect expected fourth selection record. Source inspection confirms loadCurrent bypasses
create when a Cart exists; the corrected final run passes all browser and exact persistence checks.
No application test was weakened and no owner history was changed to satisfy the assertion.

Application/owner/migration/build evidence reused from WP-2325 at50b877e and WP-2326 atffed14e
(document-only08ee174 follows), with sources recorded in those briefs. Only lab scripts changed;
no runtime, UI, Domain, dependency, manifest, lock or pin changed. The same WP-2321 frozen installation
remains valid. No fresh API/PWA/Domain unit/build or full integration run is claimed. Final scope and
privacy review uses synthetic notes, protected invitation entry and bounded failures; no screenshot,
credential persistence, real Store data or external operation is introduced.
