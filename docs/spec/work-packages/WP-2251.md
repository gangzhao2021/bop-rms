# WP-2251 - Authorized Local Pickup Cart Removal

## Authority and scope

Owner authorizes sustained bounded implementation and verified local commits. Baseline412dcd0,
branch codex/wp-2251-pickup-removal. Composite Sections0–97 and WP-1205/2228/2231/2241/2245
retain immutable Pickup creator, current Session/CSRF authorization, own-item changes, versioned
atomic removal, stable operation history/Audit and safe current-Cart views.

Add an Ordering-owned removal gateway. Strict closed credential/Cart/item/version/operation input
uses server time only. Authorize Session+CSRF before a current-binding read and again afterward;
require exact copied Brand/Store, Pickup ContextOnly identity and stable Session/QR context. A
foreign requested Cart gets uniform absence before arbitrary Cart loading or Audit production.
Resolve fresh evidence per request, then delegate to the existing Cart Item command service;
all item ownership, lifecycle, expected version, idempotency and atomic Audit/history stay there.
Required Audit metadata and intent-hash providers have no invented defaults. Unexposed Add/Update
Catalog/reference dependencies deny if accidentally invoked; this gateway exposes removal only.

API composes real public Identity/Ordering stores and gateway, with separate bounded read/write
transaction runners. Preserve read/create/add/update behavior; only optional removal becomes active.
After a successful command, re-read the authorized public Cart; mismatched/older/missing view or
failed read returns Unavailable because the mutation may have committed. Map known pre-command
errors to existing bounded transport states; Version conflict uses a fresh authorized current view.
No Aggregate, credentials, Audit data, raw error or private scope reaches HTTP.

The development/test loopback runtime exposes optional cartRemoval configuration only alongside
Cart reads. Existing default unavailability, environment/readiness/Origin/CSRF/Fetch Metadata guards
remain unchanged. CUST-CART /cart retains customer.public scope, fields, buttons, errors, keyboard,
reflow and accessibility; no UI/route change or new permission. No Catalog availability is inferred
from display history. Add/Update, default App composition, shared DineIn and recovery remain separate.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2251.md
- docs/spec/design/business-scenario-coverage.md
- packages/rms/ordering/src/application/pickup-cart-removal-service.ts
- packages/rms/ordering/src/tests/pickup-cart-removal.test.ts
- packages/rms/ordering/src/index.ts
- apps/api/src/customer-cart-removal-composition.ts
- apps/api/src/customer-cart-removal-composition.test.ts
- apps/api/src/local-customer-runtime.ts
- apps/api/src/local-customer-runtime.test.ts
- packages/database/test/cart-binding-store-acceptance.test.mjs

## Acceptance and verification

1. Malformed/foreign/expired/CSRF-invalid or interveningly revoked Session denies before mutation.
   Fresh scoped binding and Audit evidence feeds the existing removal service; success/replay retain
   version, own-item, lifecycle and no-duplicate-history semantics. No Catalog write eligibility is fabricated.
2. API/local composition acquires no resources at construction, requires reads and explicit providers,
   retains default/security guards and bounds errors. Post-commit read failure remains uncertain.
3. Real isolated HTTP/Identity/Ordering removal and same-key retry prove one effect and matching
   Audit/history, safe DTO, revocation/foreign/context denial and existing rollback/RLS regressions.
   Store/Catalog seed and policy providers remain explicitly synthetic; no production/browser claim.
4. Pinned frozen install, affected format/lint/typecheck/unit/build, ordering-cart:acceptance, full
   pnpm verify, forced pnpm test:integration --force, Secret/privacy and final11-file scope/LF
   checks pass. Required checks are real commands from the current repository.

Rollback removes optional composition; no schema/data rewrite. No dependency, migration, default
write grant, external service, main merge, push, deployment or real operational approval.

## Verification record

Acceptance1 PASS: Ordering390/390 passes. The new gateway denies malformed/accessor input,
wrong CSRF, expired/foreign/interveningly changed Session, inconsistent binding, backward time,
foreign Cart, wrong version/ownership and missing Audit evidence before writes. Server time is
rechecked after binding lookup. Same-key retry reauthorizes and returns a minimal receipt with one
committed removal/history. Existing command semantics and Catalog denial remain intact.

Acceptance2 PASS: API279/279, affected lint/typecheck/build and format pass. Optional runtime
requires reads, acquires no resources during construction, preserves default unavailability and
Origin/Session guards. A missing/older/mismatched post-command view, invalid receipt or failed
post-command authorization returns Unavailable; known rejection and fresh version conflict remain
bounded. Public owner contracts compose all persistence, with no SQL in the API.

Acceptance3 PASS: Cart PostgreSQL6/6 passes, including all prior binding/rollback/RLS assertions.
An explicitly synthetic Catalog seed is added through the real command store. Real HTTP removal
commits before an injected display-read outage and returns503; same-key retry/repeat returns the
safe empty version3 Cart with exactly one new operation and one new Audit. Wrong CSRF, foreign
Cart and public Identity-store revocation deny. Sessions, HTTP servers and isolated database
resources clean up. No production Store/Catalog eligibility or native-browser claim is made.

Acceptance4 PASS: pinned CI=true pnpm install --frozen-lockfile, full CI=true pnpm verify
(40 successful builds), forced uncached CI=true pnpm test:integration --force
(cache bypassfb392e5f1786ea4e,1m57.139s), Secret2/2 and final11-file scope/LF review pass.
Initial API typecheck found a missing required hash-provider argument; initial PostgreSQL fixture
used a shadowed reader. Both were corrected and affected checks rerun. A restricted-environment
pnpm invocation stopped before checks because no TTY was available; the final scan/build used the
same authorized environment as the frozen install and passed. No failed attempt is counted.
Existing informational AsyncAPI and Turbo output warnings remain; no check was disabled.
No migration, dependency, generated file, main merge, push, deployment or external service changed.
UI is unchanged, so no additional native-browser/visual run was required or claimed.

Logs under /private/tmp: bop-wp2251-install.log, bop-wp2251-ordering.log, bop-wp2251-api.log,
bop-wp2251-cart-final.log, bop-wp2251-verify.log, bop-wp2251-integration.log and
bop-wp2251-secret-final.log. All criteria pass locally. Default App integration, current Catalog
eligibility and Add/Update remain separate unfinished work.
