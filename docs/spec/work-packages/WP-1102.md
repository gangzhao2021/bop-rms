# WP-1102 — Price Resolution

## Status and authority

- Status: `Implemented, verified, integrated and cleaned`
- Branch: `codex/wp-1102-implementation` (deleted after integration)
- Exact predecessor: integrated WP-1101 squash
  `main@61f20b46463cab02b99df8307c6a1e04e81c47a1`
- Owner source: `BOP-RMS-HANDOFF` version `0.5.3`
- Governing sections: `27.1–27.4`, `54.5 F11.1`, `88` Price Record/Book controls
- External Evidence: `NOT AVAILABLE / NOT CLAIMED`

## Purpose

Resolve exactly one version-pinned Money amount for a Catalog-owned Sellable from a published,
single-Currency Price Book using Brand, Region, Store Group, Store, Channel, Order Type and
effective time.

## Canonical rules

1. Store + Channel/Order Type;
2. Store;
3. Store Group + Channel/Order Type;
4. Store Group;
5. Region + Channel/Order Type;
6. Region;
7. Brand + Channel/Order Type; and
8. Brand Default.

Effective Period determines eligibility and never breaks a priority tie. Two matches at the chosen
priority are a configuration conflict. No match rejects sale as missing coverage.

## Ownership and boundaries

- Pricing owns Price Book/Entry versions and resolution; Catalog owns Sellable facts; Store/Tenant
  supply membership context.
- Each book has one Currency and every Entry Money must match it. No FX conversion exists.
- Option/Bundle upgrade rules, quantity pricing, Promotion, manual price, Quote/API, UI, Event and
  provider behavior are absent.
- All acceptance values and reason codes are synthetic; External Evidence is not claimed.

## Implementation and local acceptance

- strict closed Domain contracts, overlap validation, explicit coverage matrix and replay evidence;
- Brand-RLS persistence for Price Book aggregate, immutable versions/entries and append-only
  operations;
- Pricing tests: `52/52 PASS`; isolated PostgreSQL acceptance: `1/1 PASS`;
- canonical migration catalog: `32` immutable migrations, `14` namespaces;
- package lint/typecheck and architecture/ownership validators: `PASS`.

The pre-existing macOS/APFS case-fold and realpath fixtures remain platform-limited; pinned Linux
exact-head CI is the authoritative repository gate.

GitHub evidence:

- PR `#118` exact head `493fb4b9c27e4a10e50875dec575fdc86bd0e0f5` passed run/job
  `30740828380 / 91477839747` in `13m57s`;
- squash merge `757c48568335da5ebcf18ed4fe131cdaa52423d1`;
- exact-main run/job `30741320452 / 91479174923` passed in `14m27s`.

## Next action

WP-1103 may consume this contract from exact predecessor
`main@757c48568335da5ebcf18ed4fe131cdaa52423d1`.
