# WP-0030 — Transactional Outbox Contract and Table

## Authority and authorization

- Work Package: `WP-0030`
- Status: `Implemented、verified and integrated into main`
- Owner authorization: `2026-07-23`
- Baseline commit: `origin/main@9e251de9bbb5d9bdb910a1c4403f440658adcbad`
- Composite authority accepted by the Owner:
  - local `BOP-RMS Complete Handoff Package.md` version `0.5.3`, Sections `0–91`;
  - repository-accepted Sections `92–97`, represented by ADR-0029–ADR-0033 and WP-0013、WP-0014、WP-0020–WP-0025.
- Governing Handoff sections: `48.3.8`、`50.7`、`50.13–50.15`、`52.15`、`54.4 F00.4`、`56.13`、`80.6.3`、`86`、`87.7.4–87.7.6` and `89–91`.
- Governing repository decisions: Section 92 / ADR-0029 database ownership evidence；Section 93 / ADR-0030 dependency boundaries；Section 94 / ADR-0031 migration contract；Section 95 / ADR-0032 platform schema and privilege boundary；Section 96 / ADR-0033 UUID、Tenant Context and helper boundary；Section 97 / WP-0024 isolated database lifecycle.

The repository does not contain or claim a Canonical Handoff file newer than the Owner-supplied `0.5.3` copy. The Owner explicitly accepted this composite baseline after confirming that no newer file is available. Later repository-accepted Sections 92–97 supersede conflicting older text only within their recorded decisions.

The Owner authorized this documentation-only closeout on `2026-07-23` after implementation、publication、Ready transition、squash merge、post-merge verification and local cleanup completed. The closeout may change only `docs/spec/README.md` and this brief；it does not authorize WP-0031、new code or migrations、deployment、runtime grants or any non-local database action.

## Goal and bounded responsibility

Deliver the smallest Eventing reliability increment:

1. materialize the stable, business-agnostic Domain Event Envelope and `Append Event in Transaction` contract in `@bop/eventing`;
2. create the shared technical control table `platform_eventing.outbox_event`;
3. prove a synthetic business write and its Outbox event commit or roll back atomically in one caller-owned PostgreSQL transaction;
4. preserve default-deny schema privileges、Tenant isolation、minimal payload and deterministic migration/runtime verification.

The eventing package does not generate IDs、open transactions、commit、roll back or own business facts. The caller supplies an already-open transaction and application-generated UUIDv7 identifiers. The package performs one parameterized insert only. A failure is returned to the caller so the caller can roll back the complete business transaction.

## Accepted contract

### Public envelope

The pre-publication envelope contains:

- `eventId`: application-generated UUIDv7;
- stable PascalCase `eventType`;
- positive `schemaVersion`;
- `occurredAt`: UTC instant;
- `producerModule`: canonical `@bop/*` or `@rms/*` identity;
- `tenantId` and optional `storeId`;
- `aggregateType`、`aggregateId` and positive `aggregateVersion`;
- `correlationId` and optional `causationId`;
- either an Actor reference or the `System` actor classification;
- a JSON-object payload containing stable IDs and the minimum business fact;
- an explicit redaction classification and required JSON-object replay metadata.

The generic contract does not define a concrete business Event or payload schema. Before any producer is added, WP-0035 must register the Event type/version、payload schema、compatibility rule、consumer contract、retention and replay semantics. Secrets、credentials、payment data、health/allergy facts and unnecessary PII are prohibited.

Section 80.6.3 is the later naming authority: `schemaVersion` / `schema_version` supersedes the older generic `eventVersion` label. `publishedAt` is absent before publication and remains nullable in the Outbox until WP-0031 publishes the event.

### Table

`platform_eventing.outbox_event` is a shared infrastructure technical control record owned by `shared-infrastructure/eventing`, not a business Module table. It stores the exact envelope fields plus:

- server-recorded `recorded_at`;
- `available_at`;
- nullable `published_at`;
- non-negative `attempt_count`, initially zero;
- nullable stable `last_error_code`.

Event facts are inserted once and retained. WP-0030 exposes no update or delete operation. The bounded delivery-state columns exist for the future WP-0031 dispatcher and WP-0033 retry/dead-letter implementation; their mutation contract is not implemented here.

The table uses the accepted `platform_helpers.uuid_v7` domains, exact constraints, PUBLIC denial, `ENABLE ROW LEVEL SECURITY` and `FORCE ROW LEVEL SECURITY`. Its policy uses `platform_helpers.current_brand_id()` and `current_store_id()` with transaction-local server-resolved context. Missing or invalid context denies rows/writes for an ordinary non-owner runtime. No runtime role、grant、login or bypass authority is created.

Tenant-scoped indexes lead with `brand_id`. WP-0030 adds only:

- the UUIDv7 primary key on `event_id`;
- a partial publishable index on `(brand_id, available_at, recorded_at, event_id)` where unpublished;
- aggregate-order lookup on `(brand_id, aggregate_type, aggregate_id, aggregate_version, event_id)`;
- correlation lookup on `(brand_id, correlation_id, recorded_at, event_id)`.

These are contract indexes, not production query-plan evidence.

## Implementation allowlist

The implementation may change only:

- `docs/spec/README.md`
- `docs/spec/work-packages/WP-0030.md`
- `migrations/0000-platform/0000_010_create_outbox_event.sql`
- `packages/bop/eventing/**`
- `packages/database/src/catalog.ts`
- `packages/database/src/catalog.test.ts`
- `packages/database/src/foundation.ts`
- `packages/database/src/foundation.test.ts`
- `packages/database/test/isolated-database-acceptance.mjs`
- `packages/database/test/outbox-acceptance.test.mjs`
- `packages/database/vitest.outbox.config.ts`
- `tooling/database-ownership/validate.mjs`
- `tooling/database-ownership/validate.test.mjs`
- `package.json`
- `pnpm-lock.yaml`

No other file is authorized. Generated build output and local logs are not deliverables.

## Acceptance criteria

1. The composite authority、fresh baseline and exact scope are recorded without claiming a nonexistent `0.5.9` Library file.
2. The migration catalog contains exactly the new ordered `0000_010` migration with Eventing ownership and immutable bytes.
3. Fresh apply creates the exact table、columns、types、defaults、constraints、indexes、RLS flags and policy; PUBLIC retains no table privilege and no runtime grant exists.
4. UUIDv7、PascalCase Event type、positive versions、canonical producer identity、UTC time、Actor shape、JSON object payload/replay metadata and redaction classification fail closed.
5. A synthetic business write and Outbox insert commit together; an injected failure rolls both back; retry with the same `event_id` cannot create a duplicate.
6. Tenant-context positive and negative paths are proved with a synthetic non-owner role; Brand and optional Store scope cannot cross.
7. The `@bop/eventing` API accepts a caller-owned transaction、uses parameterized SQL、does not expose an ORM entity and never commits or rolls back.
8. Database ownership enforcement accepts only the exact declared shared-infrastructure access and continues to reject undeclared Module persistence、foreign writes and dynamic targets.
9. Existing foundation verification delegates only the exact accepted Eventing objects to WP-0030 and continues to reject all other unexpected foundation objects.
10. Focused tests、affected architecture checks、frozen install、format、lint、typecheck、unit、integration、migration、security、root verify and build pass.
11. Final diff review finds no secret、credential、real Tenant/Store/Actor fact、unnecessary PII、specific business Event、Provider、workflow、deployment or later-WP implementation.

## Verification plan

From the repository root:

```text
pnpm install --frozen-lockfile
pnpm module-manifest:check
pnpm import-boundary:check
pnpm database-ownership:check
pnpm domain-layer-boundary:check
pnpm migration:check
pnpm foundation:check
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm isolated-database:acceptance
pnpm outbox:acceptance
pnpm verify
git diff --check
```

The isolated suites may create only synthetic local databases and uniquely named synthetic non-login roles and must clean them after success、failure or signal.

## Risks、recovery and external evidence

- Risk: a generic envelope becomes an ungoverned payload bag. Control: JSON-object shape、explicit classification、no concrete producer and mandatory WP-0035 registration before first producer.
- Risk: shared infrastructure becomes a cross-Domain write path. Control: a single parameterized Outbox insert and no API that writes business tables.
- Risk: delivery metadata is mistaken for permission to build a dispatcher. Control: no claim/lease/update API in this WP.
- Risk: RLS is mistaken for primary authorization. Control: application authorization remains mandatory；RLS is defense in depth.
- Risk: applied migration rollback damages retained Events. Control: before application, normal Git revert；after application, reviewed forward-fix or separately approved restore only.

External Evidence remains gated and unclaimed: actual API/Worker/Migration roles and grants、RDS compatibility、production query plans/volume/partitioning/retention、payload size threshold/object-storage reference、Event Catalog consumer approvals、staging apply、backup/restore and production authorization.

## Explicit non-goals

No WP-0031 publisher、lease、`FOR UPDATE SKIP LOCKED`、transport or broker；no WP-0032 Inbox/idempotent consumer；no WP-0033 retry scheduler、dead-letter or poison handling；no WP-0034 trace propagation expansion；no WP-0035 Event Catalog tooling or concrete producer；no WP-0036 SSE.

No business table、Aggregate、Repository、API endpoint、UI、Provider、runtime role/grant、seed/fixture business fact、ORM、dependency upgrade、workflow、deployment、commit、push、PR or merge.

## Definition of Ready

- Authority and Owner authorization: `PASS`
- Domain ownership and interface boundary: `PASS`
- Data/RLS/privilege contract: `PASS`
- Security/privacy review plan: `PASS`
- Exact allowlist、acceptance、verification、risks and non-goals: `PASS`
- Implementation: `PASS`

## Local verification evidence

- Exact toolchain: Node `24.18.0`、pnpm `11.13.0`.
- Frozen install and supply-chain policy check: `PASS` for all 8 workspace projects and 312 lockfile entries.
- Linux root `pnpm verify`: `PASS` with exit `0`；181 root tests plus all 7 workspace format、lint、typecheck、test and build tasks passed.
- Eventing contract tests: `PASS` — 11/11.
- Database ownership tests: `PASS` — 27/27 and live repository validation.
- Migration/config tests: `PASS` — 29/29；catalog reports 10 immutable migrations and 14 Canonical namespaces.
- Foundation tests: `PASS` — 6/6.
- WP-0030 isolated PostgreSQL acceptance: `PASS` for exact schema/domain/index/RLS/ACL evidence、missing/cross-Tenant denial、atomic commit/rollback、duplicate Event denial and cleanup.
- WP-0024 isolated PostgreSQL regression: `PASS` for parallel isolation、failure injection、signal、timeout、redaction and cleanup with all 10 migrations.
- Security/privacy review: `PASS` for secret、credential、Tenant/Store/Actor、PII、payload、permission、RLS and log/error scope；no production or Provider fact was claimed.

## Delivery and closeout evidence

- Implementation commit: `6e5b7778cbc15a5368ce7d9a8dc74c7f020f9013` on `codex/wp-0030-transactional-outbox`.
- Pull Request: GitHub PR `#23`；the Owner separately authorized commit、push、Draft creation and the later Ready transition.
- Owning PR CI: run `30021535796` / job `89255222004` completed successfully against the exact implementation head.
- Governance: GitHub Free solo self-review completed；the PR was `CLEAN` and `MERGEABLE` with no unresolved review comment before merge.
- Integration: the Owner authorized an exact-head squash merge；PR #23 merged into `main` as `9e267c2fe13e8e9fa509cc82e286d0289638d567` on `2026-07-23T15:54:08Z`.
- Post-merge evidence: `main` push run `30022673489` / job `89259156985` completed successfully, including exact tool versions、frozen install、WP-0005、WP-0006、workspace/type/quality checks and WP-0024 isolated PostgreSQL lifecycle.
- Cleanup: local `main` fast-forwarded to the squash commit；the clean WP-0030 worktree and merged local/remote implementation branches were then removed after exact-tree verification. The Owner-supplied untracked Handoff file remained unchanged.
- External Evidence remains gated and unclaimed. Deployment、runtime grants、staging/production migration、Provider mutation and non-local database action did not occur and remain unauthorized.

## Documentation closeout verification

- Exact closeout baseline: `origin/main@9e267c2fe13e8e9fa509cc82e286d0289638d567`.
- Allowlist and whitespace: `PASS`；the diff contains only `docs/spec/README.md` and this brief，and `git diff --check` passes.
- Frozen install: `PASS` with Node `24.18.0`、pnpm `11.13.0`、all 8 workspace projects and the unchanged lockfile.
- Repository guidance and formatting: `PASS`；33 ADRs、5 skills、templates and boundary smoke scenarios validate，and all root / workspace Prettier checks pass.
- Full local `pnpm verify`: `ATTEMPTED, NOT CLAIMED AS PASS` on macOS. The default temporary path exposed the `/var` versus `/private/var` alias in 2 Module Generator assertions；with `TMPDIR=/private/tmp`, Module Generator 16/16、Import Boundary 19/19、Database Ownership 27/27 and Domain Layer 56/56 passed before 4 of 29 migration catalog/config assertions reached their Linux case-sensitive filesystem / canonical-path boundary. No out-of-scope test change was made. The exact implementation head and squash commit retain the successful Linux CI evidence recorded above；the documentation closeout still requires its own owning CI if published.

## Closeout

All WP-0030 acceptance criteria have recorded local、owning-PR and post-merge evidence. The accepted contract and non-goals are unchanged. `WP-0031 — Worker Outbox Dispatcher` is the next roadmap candidate only；it requires a fresh exact-baseline brief、resolved dispatcher decisions and separate explicit Owner authorization before any implementation.
