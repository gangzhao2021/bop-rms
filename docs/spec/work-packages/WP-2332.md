# WP-2332 - Pickup Removal Current Authority Across Awaits

Baseline093da89; branch codex/wp-2332-pickup-removal-current-authority. Owner authorizes continuous
local implementation/verified commits. Composite Sections0-97, WP-1201/1204, WP-2251 and WP-2331
retain Ordering-owned removal, Identity Session/CSRF, exact expected version, immutable intent,
atomic owner persistence/Audit and current-authorized original recovery. Preserve guidance edits.

## Scope and acceptance

Removal currently delegates persistence and original-history reads after only the binding-time
Session check. Wrap new commit with fresh exact Session authorization and original loaded Cart
lifecycle checks. Verify monotonic server time and Session expiry after authorization completes.
Reauthorize successful new/replayed result disclosure without treating expired historical Cart
state as new work. Retain bounded existing errors through the generic dependency wrapper. No
Catalog call, reference allocation, public contract, schema, money, cross-owner access or policy.

Tests cover revocation/version/QR drift and clock faults during load/history waits, Cart expiry
just before/at/after the boundary, Session expiry during authorization, post-commit denial and
original retry after Cart expiry with one removal and unchanged immutable history. Credentials
remain Identity-only and no notes/options enter diagnostics. No atomic commit-time lease claim.

## Allowlist

- docs/spec/README.md
- docs/spec/work-packages/WP-2332.md
- packages/rms/ordering/src/application/pickup-cart-removal-service.ts
- packages/rms/ordering/src/tests/pickup-cart-removal.test.ts

## Verification plan

Run affected removal tests, full Ordering/API regression and actual Cart PostgreSQL via existing
ordering-cart:acceptance; Ordering lint/typecheck/build, API typecheck, changed-file Prettier,
import-boundary:check, domain-layer-boundary:check, repository-guidance:check, secret-scan:check
and final scoped diff/LF/resource checks. Fresh authorization changes justify whole owner/API
and persistence coverage. Use permitted local loopback/Docker execution after WP-2331 established
the sandbox limitation. Reuse WP-2331 CI=true pnpm install --frozen-lockfile at13eedf7 plus its four
files: same installation, Node24.18.0/pnpm11.13.0, unchanged manifests/lock/configuration/toolchain.
No UI/schema change: no new browser or migration run. Full pnpm verify and pnpm test:integration
remain the next complete cross-domain journey/release milestone, per current guidance; historical
WP-2251 full/forced integration gates describe that original integration, not every bounded repair.
No remote, deployment or real operational evidence. Rollback uses a forward application correction,
never historical-data rewrite. All failed/skipped checks will be recorded without invented passes.

## Results

PASS: new removal commits reauthorize exact current Session/CSRF at monotonic fresh server time,
check expiry after authorization awaits and validate the original loaded Cart lifecycle. New and
historical successful responses reauthorize before disclosure. Removal remains owner-local with
unchanged expected-version, own-item, intent, operation and Audit contracts. Post-commit revocation
returns bounded denial; an authorized original retry after Cart expiry returns AlreadyApplied with
one write and unchanged history. No Catalog call, credential propagation or sensitive diagnostics.
Security review closes the stale-authority write/disclosure finding; no commit-time lease is claimed.

Fresh affected removal32/32, full Ordering878/878, API571/571 and actual PostgreSQL9/9 in7 files
(68.90s) pass. Fresh Ordering lint/typecheck/build, API typecheck, changed-file Prettier,
import22/22, Domain57/57, guidance and secret2/2 pass. All commands exited0; no failed run occurred
in this WP. Tested093da89 plus this four-file WP and the preserved three guidance edits. Logs under
/private/tmp/bop-wp2332-{unit,cart,api,lint,type,build,api-type,format,import-boundary,
domain-layer-boundary,repository-guidance,secret-scan}.log. Frozen-install evidence is reused from
WP-2331 as scoped above, not a new installation. Final four-file diff/LF and privacy review pass;
owned Docker container inventory is empty after acceptance. No release/remote CI, full verify,
generic integration or browser run is claimed; reasons and next milestone are above.

Next source-ready feature work remains Catalog selection/commercial-input owner composition and
Dining Quote readiness. DEC-H03 capacity ownership and other unresolved Order/refund decisions
remain gates, not permission to invent facts or claim the whole project is complete.
