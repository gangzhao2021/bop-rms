# WP-2271 - Dining Table Replay Isolation

## Authority and readiness

Baseline3ef5efa024fdb33771f9098b647a6171f52800d9; branch codex/wp-2271-dining-replay-isolation.
Continued bounded implementation and verified local commits are authorized. Composite Sections0–97,
WP-1006 and WP-2112 require exact Tenant/Brand/Store/Actor, object references, expected versions,
immutable results and idempotent Table configuration/Session move. Current replay branches return
unvalidated repository records after only comparing intent digests. Move loads also omit exact
requested-object identity checks. These are owner-service validation defects.

## Scope and allowlist

Copy current authorization before later repository callbacks. Validate and freeze complete historical
Table and Move records before exposing them. Bind operation, digest, exact scoped object identities,
versions, timestamps, minimal Event and Audit to the accepted command. Reject foreign/malformed
records with bounded dependency errors; preserve changed-intent conflict and current permission denial.
Move reads must resolve the requested Session/source/target identities. No historical record rewrite,
new idempotency duration or request-time semantics change.

- docs/spec/README.md
- docs/spec/work-packages/WP-2271.md
- docs/spec/design/business-scenario-coverage.md
- packages/rms/dining/src/application/dining-table-service.ts
- packages/rms/dining/src/tests/dining-table-service.test.ts

## Acceptance and checks

1. Exact valid Table and Move retry returns immutable original results without another commit.
   Changed intent, wrong operation/object/scope/version/Event/Audit, malformed/getter-bearing results,
   dependency failure and current authorization denial cannot expose or mutate another object.
2. Wrong object returned for a requested Move load is rejected before mutation. Captured authorization
   and historical results cannot be changed by later callbacks.
3. Frozen install; affected Dining format/lint/typecheck/test/build; pnpm verify;
   pnpm test:integration --force; Secret; exact5-file scope/LF, final diff, guidance and cleanup.
   Actual evidence is required; no skipped check is represented as passed.

## Boundaries and rollback

No dependency, schema, namespace, migration, Event catalog, API/UI, credential, live grant or external
change. WP-2112 explicitly lacks Dining schema/namespace authority; persistence requires its own
accepted architecture decision and is not implied by this hardening. Closing, shared Cart binding,
record retention and stable-time retry protocol remain separate. Test data is synthetic; Domain
results are internal, not HTTP DTOs. No main merge/push/deployment. Uncommitted changes may be
removed; committed history uses forward fixes.

## Verification record

Acceptance1–2 PASS: Dining154/154 includes57 new cases. Table and Move histories are copied
through bounded descriptor-only snapshots, then checked against exact operation/object/scope,
versions, assignment, Event and Audit/Actor facts. Exact retry returns frozen originals without
another commit; changed digest retains idempotency conflict. Coherent but wrong Session/source/target
loads are rejected before Move mutation. Current authorization is captured before repository work;
historical data is captured before the equality callback. Accessors and private dependency errors
are not exposed. Existing Table/Move behavior tests remain.

Acceptance3 PASS: pinned frozen install, final Dining format/typecheck/lint/tests, full pnpm verify,
all40 build tasks and Secret2/2 pass. Forced integration passes with cache bypass058903109f2b592b,0cached,1m59.21s,
covering91 migrations, concurrency, redaction, parallel isolation, fault/signal/timeout handling and
owned cleanup. The first full verification found an omitted explicit return index signature in the
new record helper; it is fixed and the independently rerun typecheck and final full gate pass.
The initial failure is not claimed as a pass. Exact5-file scope/LF, final diff and guidance pass;
no checks were skipped or waived. The existing Dining build-output warning does not change its
successful exit. No schema, migration, dependency or generated changes are included.

Security/privacy review: original records remain append-only and no HTTP surface is added. Current
Staff permission remains mandatory before historical access. Exact IDs and scope are validated even
when a repository digest matches; returned objects and nested Audit data are immutable copies.
Snapshots reject accessors, sparse/extended arrays, non-plain objects and excessive structures before
public Audit validation. Existing authorized Audit summary size/depth semantics remain bounded by its
owner validator. No unaccepted Blocker/High remains within this service correction. No database
integrity, real Store, current Dining persistence or live application readiness is inferred.

Logs under /private/tmp: bop-wp2271-install.log, bop-wp2271-types.log, bop-wp2271-lint.log,
bop-wp2271-dining.log, bop-wp2271-verify.log (initial failed run), bop-wp2271-verify-final.log,
bop-wp2271-integration.log, bop-wp2271-secret.log.
