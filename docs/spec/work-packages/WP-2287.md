# WP-2287 - Dining Join Expected Generation Fence

## Authority and readiness

Baseline343b6fac908dce6d50572e9923f6a4f9633c812d; branch codex/wp-2287-dining-generation-fence.
Composite Sections0-97 and WP-2286 govern. Continued local implementation and verified commits
are authorized. Current Staff authorization remains mandatory before historical recovery.

## Scope and acceptance

Require expectedGeneration in the closed Staff regeneration command. Validate a positive safe
integer with a representable successor. Match the current predecessor before generation or write.
Original operation and returned receipt must identify exactly expectedGeneration + 1; altered
original generation conflicts. Preserve persisted history and operation digest bytes.
Reproduce the old version1 ABA bug in actual PostgreSQL using the old command shape before
changing production code. Prove delayed distinct operations cannot replace a newer generation,
while current-generation issuance and original metadata-only recovery remain valid.

## Exact allowlist

- docs/spec/README.md
- docs/spec/design/business-scenario-coverage.md
- docs/spec/work-packages/WP-2287.md
- packages/rms/dining/src/application/dining-session-service.ts
- packages/rms/dining/src/tests/dining-session.test.ts
- packages/rms/dining/src/tests/dining-moved-join-service.test.ts
- packages/database/test/dining-join-regeneration-store-acceptance.test.mjs
- packages/database/test/dining-session-join-store-acceptance.test.mjs
- packages/database/test/dining-participation-store-acceptance.test.mjs
- packages/database/test/dining-closing-store-acceptance.test.mjs
- packages/database/test/dining-move-store-acceptance.test.mjs
- packages/database/test/dining-moved-join-store-acceptance.test.mjs

## Verification and boundaries

Pinned frozen offline install; affected format/lint/type/unit and Dining PostgreSQL acceptance;
full pnpm verify; sequential forced pnpm test:integration --force; final Secret/guidance and
exact allowlist LF/diff/security review. Record actual failures and reruns. No unrun check is a pass.
No migration, private writer API change, permission change, Identity consumption, shared Cart,
UI/HTTP activation, real Store/Provider facts, dependency change, merge, push or deployment.
Rollback is a forward fix. Implementation and local verification are complete.

## Observed focused evidence

Pinned frozen offline install passed. The baseline PostgreSQL regression used the old command
shape with a different operation after generation3/version1 became generation4/version1. It failed
with Missing expected rejection: the delayed command actually issued another credential. This is
behavioral reproduction, not the closed parser rejecting a new field.

After the fix, all eight Dining PostgreSQL groups passed. The regression now denies the delayed
command and proves unchanged current capability, operation count and Audit count. Existing tests
exercise subsequent current-generation issuance, rollback, lost acknowledgement, simultaneous
same-operation recovery, distinct-operation competition, terminal renewal and Closing/Move fences.
A further normal history assertion verifies generation2 recovery after the current capability has
advanced to generation8. Unit additions cover malformed/omitted/overflow generation, stale moved
state, changed-generation original retries, wrong-generation receipts and denial before normal
credential generation or writing. Full verification passes with Dining623/623 and40/40 builds.

The command intentionally preserves existing digest bytes: immutable replacement generation
unambiguously identifies its predecessor. Both original-history and returned-receipt checks enforce
that identity; current Staff authorization remains before history. Existing historical records and migrations remain unchanged.

## Acceptance map and closeout

1. PASS: the old-shape PostgreSQL regression reproduces the delayed-command bug before the fix.
2. PASS: exact predecessor checks precede generation/write; altered original-generation and receipt
   identity conflict. Current-generation issuance and original metadata-only replay remain valid.
3. PASS: all eight Dining database groups,623 Dining unit tests, frozen install and full verification
   pass, including affected lint/type/architecture/permission/migration/security and40/40 builds.
4. PASS: sequential forced uncached integration passes with exit0, hash e2625ea5d5d535d0,
   zero cache and elapsed1m59.476s. Final documentation format,
   guidance, Secret and exact12-file LF/diff review accompany the local commit.

No required check was skipped. Existing non-failing Turbo no-output advisory remains unchanged.
No migration/history, dependencies, permissions, public routes or live service facts changed.
Main remains cedc44c9b6a6a1b389f77342078cc6f2d540dabe; no merge, push or deployment.
Next bounded work: the Dining-owned pure one-time admission-consumption transition, retaining
original Join identity and requiring current eligible Session/Participant/assignment facts.
Identity authorization, durable operation recovery and public Table/validity composition remain
explicit subsequent requirements, not authority inferred from an admission locator.
